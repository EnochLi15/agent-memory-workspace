截至 2026-09-09 07:28，LoCoMo `conv-43` 的恢复评测已完成：**36/47（76.60%）**，时间题 **21/24（87.50%）**、多跳题 **15/23（65.22%）**，47 题均完成判分、没有服务错误。43 块完整历史由先前 38 块真实回执和最后 5 块新写入组成。先前运行因第 39 块容量错误产生的 0/47 `service_error` 保留为独立旧结果；此前 MemoryOps 16 题的 10/16（62.5%）也分别记录。容量分批修补已冻结，713 项测试通过。本次是混合版本历史的恢复评测，圈定 972 题仍没有完整得分。

| 样本 | 完整历史回执 | 本次问答结果 | 当前问题 |
|---|---:|---|---|
| B03 / Remember | 50/50 | 5/6，p1、p2、p4、p5、p6 通过；旧轮为 4/6 | p3 证据完整，回答漏写评分要求的职称 |
| B30 / Forget | 51/51 | 3/5，p3、p4、p5 通过；旧轮为 1/5 | p1 检索遗漏仍在，p2 遗忘对象追溯不足 |
| A06 / Forget | 51/51 | 2/5，与旧轮相同 | 部分问答仍错，p4 有新增无关人物干扰 |

本次三项 Search 均使用同一冻结候选 `9c7cf1b`，数据库来自多版本真实历史前缀，属于恢复后的检索质量评测，不是统一版本从头写入的整体成绩。此前各样本分数来自不同运行，不能把差值全部归因于代码。圈定的 972 题仍没有完整得分。

本轮完成了三个主要写入修补：将模型已选中的跨范围删除目标拆为分别核验的操作；暂缓发布与历史删除边界冲突的本次可选聚合卡，保留原始事实、消息和旧记忆；拒绝把助手的句子式开场误识别为人名。来源引用修补和来源核验的类型契约也已纳入统一候选。统一基底为 `5ca2929d09e718c4aa5353460a1f9338ad0a9c30`，构建与 678 项测试通过，尚有下述已知问题。

| 真实单点验证 | add 耗时 | 结果 |
|---|---:|---|
| B30，第 30 块 | 58.79 秒 | 成功，revision 30→31；两个目标分别通过语义核验 |
| B03，第 34 块 | 4.48 秒 | 成功，revision 33→34；记录聚合卡暂缓标记 |
| B03，第 45 块，旧单点 01 | 65.43 秒 | 失败；8 条修补后事实通过核验，最后的来源核验输出格式无效，revision 44 未变 |
| B03，第 45 块，新单点 02 | 22.61 秒 | 成功；7 条事实和 5 项来源判定通过标准核验，revision 44→45 |

B30 后续 20 次 add 全部成功，合计 244.29 秒，平均 12.21 秒。完整尾部评测约 405.65 秒；5 次 search 合计约 0.68 秒，其余约 160.63 秒包含回答、判分和评测开销，现有记录不能再准确拆成独立 Answer/Judge 耗时。

在原 B30 的 1/5 轮中，两个称呼问题需要 Patricia 的新状态。相关事实、原句和来源段落仍可见，但未进入实际返回的 32 条证据；这不是删除修补把新状态删掉。时间题需要的 December 证据也仍存在，但未返回，回答随后把另一人的 November 信息推给 Marc。遗忘对象题则缺少可公开检索的对象身份记录，需单独处理身份追溯与已删细节之间的边界。

B03 新单点在统一基底上将 `verificationResponseFormat` 设为 `json_object`，仍对完整输出执行严格本地结构和语义校验，没有截取 JSON 前缀或绕过核验。实际配置和两次语义阶段审计均确认该模式。原 44 条回执不变，第 45 条精确落库；降级标记仅为本次可选聚合卡暂缓。新旧模型抽取分别产生 7 条、8 条事实，来源候选也不同，因此一次成功不能证明格式故障已全面消失，耗时变化也不能全部归因于配置。官方文档提供该 JSON 模式及本地校验示例：[结构化输出](https://docs.bigmodel.cn/cn/guide/capabilities/struct-output)。

随后 B03 尾部 02 复用了前 45 块真实回执，完成最后 5 次新 add 和 6 道原题。全部判分完成且没有服务错误，最终 revision 50、50 条精确回执及全部进程退出均已核验。标准评测耗时 179.35 秒，其中 5 次新 add 合计 53.73 秒、平均 10.75 秒，6 次 search 合计 0.86 秒；其余约 125 秒包括回答、判分和评测开销。旧尾部 01 的 0/6 service_error 和旧单点 01 的失败均作为独立历史结果保留。

检索诊断使用录制的原始查询向量，精确复现了 B30 与 A06 共 10 次 Search。第一人称词触发的主体降权会连同相关第三方人物事实一起惩罚，来源证据还会再次降权。直接放宽非当前状态查询虽恢复了 B30 的部分关键证据，并保留 A06 唯一的预订证据，却给 A06 另两题引入了第三方干扰，因此没有将该策略纳入候选。

原 B03 的 4/6 轮中，两道错题也指向检索选取：目标成员入职及职级、高管职级、临时组长身份、汇报链事实均仍为 active、confirmed，对应完整来源仍可用。p1 返回 32 条证据，其中 26 条属于 user、6 条为操作事件；p5 返回的 32 条全部属于 user。目标人物事实与完整来源被降权或因数量限制未选中，并非写入抽取遗漏或被遗忘操作删掉。记录中的融合候选名次与后续 pack 名次不同，不能直接用前者判断最终覆盖。

查询范围候选 `9c7cf1b` 已实现并冻结，692 项测试及构建通过。它只根据原查询判断属性属于用户、其他人物或无法确定；仅明确指向其他人物时取消实际人物事实的主体惩罚，原始来源和操作事件的权重、可见性规则保持。功能默认关闭，验证时显式启用；分类与向量计算并行，每次最多一次请求、512 个输出 token、8 秒上限，失败走原路径，有界缓存只保留有效结果。

一次 34 个查询的真实分类验证已完成，平均耗时 1.42 秒、最长约 3.03 秒，全部单次成功。18 个预先固定的独立对照中 17 个分类符合预期；唯一偏差是 unknown→self_state，两者均保留原检索路径，没有将用户自身或模糊范围错误扩大为其他人物。其余 16 个为原评测查询，模型只收到查询文本。

用实际分类结果和原向量进行配对检索后，B03 p1/p5、B30 p3/p5 的关键主张和完整来源得到补充，B30 p4 的原正确依据保留。A06 p1/p2/p3/p5 的完整结果逐项不变，p3 的唯一预订事实、两条原引用及第 26 条的位置都保留。B30 p1 被实际模型分类为 self_state，原遗漏仍在；A06 p4 保留了朋友相关依据，却新增无关配偶、宠物证据。

`priority-query-focus-qa-20260909-01` 于上海时间 05:57:23 启动、06:05:45 完成：固定上述 `9c7cf1b` 候选，152 次完整历史幂等回执、16 道原题，顺序为 B03、B30、A06，单 worker。新 Engine 正常运行分类、检索、回答和判分，没有用此前 34 次分类结果填充缓存。实际 16 次查询范围调用全部成功；完整回执、三份数据库全表不变及执行完整性通过，写入生成与写入 embedding 调用均为零。

本次标准评测实际耗时 497.35 秒（约 8 分 17 秒）。16 次 Search 合计 30.38 秒、平均 1.90 秒；其中查询范围分类平均 1.79 秒、最长 3.27 秒，分类与 embedding 并行。其余约 466.77 秒包含回答、判分和评测开销，不能据此分别估算 Answer 与 Judge 耗时。152 次 add 仅复用历史回执，合计约 0.20 秒，不能作为新写入性能指标。

B03 的 p1/p5 与 B30 的 p3/p5 在此次转对，但 B03 p3 转错。只读核验表明，p3 的新旧查询向量完全相同，返回内容与事先配对结果一致；管理关系及完整职称来源仍在，还新增了独立职称事实。新回答确认了同一人与管理链，却漏写职称；两次 Judge 均按同一必含项判定。一次结果不能区分上下文排序、组合与生成波动的影响。

另一个隔离候选 `d44a5bbe` 修补了分类器不可用时的删除降级：存在未核验的歧义事实时拒绝整次提交，避免仅凭共享来源或缺少值引用就删除独立邻居。689 项测试通过，本地对照验证拒绝后全表不变。这个缺口未触发最近 B14 的实际失败，因此它不代表 B14 已恢复；尚不适合再次启动 972 题全量。

两个修补已无冲突合成候选 `8717d918`，703 项测试及构建通过，144 个产物两份哈希一致。上述 16 题仍归属 `9c7cf1b`；`8717d918` 的真实新写入评测因下述容量问题结束，未部署到主服务。

`priority-conv43-20260909-01` 于上海时间 06:36:44 启动，使用集成候选 `8717d918` 和新数据库，保留原圈定 LoCoMo `conv-43` 的完整 29 个会话、680 条消息、43 次原分块 add 和 47 道原题，其中 24 道时间题、23 道多跳题。判分服务按原协议完成一次标准预检后，评测器于 06:37:06 开始写入；controller PID 为 80249，于 06:54:25 退出，全部自有子进程已关闭。实际配置已确认 queryFocus=true、8000 毫秒上限、json_object、32 条证据、6000 token 预算、候选池 200。该运行用于扩大本轮覆盖范围，不再仅围绕这 16 题调参。

本轮完整保留 LoCoMo 原 `upstream-reproduction + refined-python` 协议：本地 `qwen3:14b` Q4_K_M Judge、GLM-5.2 Answer，单 worker；本地量化 Judge 不代表官方榜单模型等价。运行计划及入口已冻结，三项本地测试通过，详见 [conv-43 准备与启动报告](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-preparation-20260909.json)。总运行 1061.61 秒，评测器实际运行 1039.18 秒；38 次成功 add 平均 26.52 秒，失败 add 为 31.06 秒。没有执行 Search、Answer 或正式 Judge；启动前的合成 Judge 预检约 21.26 秒。107 次写入生成中 106 次成功，另一次 JSON 错误按既有策略降级提交，与最后的容量拒绝分开记录。详见 [conv-43 实际结果](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-results-20260909.json)。

运行中的只读性能分析固定前 26 次成功 add：平均 26.71 秒，p50 为 23.83 秒，nearest-rank p95 为 48.60 秒，最长 58.53 秒，合计 694.36 秒。对应 72 次生成均在首次传输成功；26 次抽取合计 389.45 秒、32 次核验 190.45 秒、12 次修补 74.72 秒、2 次状态转换 9.31 秒。同一请求内各生成区间无可见重叠，区间并集约占 add 耗时 95.6%。这里的生成计时包含网络、provider 响应、解析与 trace 写入，不含 gate 等待，不能解读为纯模型计算时间；其余时间含 embedding、本地处理与 gate 等，尚未独立分解。

明确可继续定位的是结构输出稳定性：两次缺少 `operations` 数组、一次 `sources` 类型错误，触发 41.11 秒修补，占上述 add 总耗时 5.9%。其他修补涉及来源顺序、对象绑定或语义。六次再次核验已缩小输入范围，合计 12.04 秒；当前没有相同请求、目的和输入的重复调用，也没有全量重复核验的证据。本次仅定位与记录，未更改正在运行的候选。

导致本轮终止的是独立的本地容量问题。第 39 块的抽取、标准核验均成功，18 条事实通过后，状态比较生成 27 个独立候选对；它们没有重复，但完整事实在不同对中反复携带，使候选 JSON 达到 68,053 个 UTF-16 字符，超过旧 64,000 上限。完整模型输入为 76,206 字符。录制的两包 system/input 逐字重放再次触发同一检查，未调用新模型或提交数据库；原数据库仍为 revision 38、38 条精确回执，失败请求没有回执。

修补候选 `292ea743` 保留全局 64 对上限，将容量检查应用于实际发送的完整输入，每批最多 64,000 字符、最多 4 批；每批完整保留消息、事实、引用编号，最终合并并校验全局指纹和覆盖后才可提交。原失败输入能无损拆为 22+5 对、63,201/18,891 字符；三个历史单批输入逐字不变。10 项新增回归与全部 713 项测试通过，涵盖晚批坏引用、取消、容量拒绝无部分提交，以及传输失败全量保守降级。录制诊断中的状态判定使用明确标注的合成结果，在 embedding 前停止，不代表真实模型核验或写入已经成功。详见 [分批修补与复现](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-transition-batching-20260909.json)。

恢复运行 `priority-conv43-tail-qa-20260909-01` 于 07:16:09 启动，保留旧 namespace 和完整原请求，在独立数据库副本中复用前 38 块真实回执，由 `292ea743` 新写最后 5 块并执行原 47 题。07:19 已确认全部 43 次请求成功、revision 43、43 条回执，随后开始问答。5 次新 add 分别约 38.59、58.66、35.08、3.73、18.78 秒，合计约 154.84 秒，新尾部未记录降级。候选与原 LoCoMo 判分协议均已冻结；这属于恢复后的混合版本历史评测。详见 [恢复评测启动与完整写入核验](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-tail-preparation-20260909.json)。

本次第 39 块重新抽取后仅需 6 对状态比较、20,346 字符，一次标准模型调用后成功；另一次新写为 12 对、38,423 字符，也只需一批。本轮尾部尚未在真实模型上触发多批，不能将一次成功全部归因于分批修补。原 27 对超限输入的无损覆盖和提交契约已用精确录制输入及合成回归独立验证，本次 47 题的最终问答结果为 36/47。

恢复运行于 07:28:28 正常结束，全部自有进程已退出；415 个固定文件、144 个候选产物、源库与快照完整性均通过。总运行 738.00 秒（约 12 分 18 秒），评测器耗时 724.42 秒。5 次新 add 平均 30.97 秒，47 次 Search 平均 1.84 秒、合计 86.25 秒；其余约 483.25 秒包含 Answer、Judge 与其他开销，没有独立计时不能再拆分。38 次前缀 add 为幂等回执，不代表新写入速度。详见 [conv-43 恢复评测最终结果](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-tail-results-20260909.json)。

查询范围分类的 47 次模型传输均成功，应用层为 46 次有效分类和 1 次安全降级：q0012 提供的引用并非查询原文，精确引用校验将其拒绝并退回 unknown，该题最终仍答对。它不是超时，也不是合法 unknown 的正常分类；没有为提高成功率而放松引用要求。

时间题错 3 道（q0011、q0027、q0041），多跳题错 8 道（q0004、q0006、q0010、q0022、q0023、q0028、q0035、q0036），均按原题号留档，后续诊断不修改既有回答与判分。初步只读归因已区分两类问题：q0011 的中间历史事实仍存在并可见，但未进入 200 条候选池；q0027 的关键事实、时间和完整来源已经返回，回答拒绝补充原证据未明确写出的行政区常识关系，属于回答协议的推理边界。后一类不能归为时间解析或检索失败，也不能通过改评分规则解决。多跳题 q0006、q0022、q0023 的必要证据部分未返回，其中既有候选池外遗漏，也有进入候选池后被 32 条数量上限挡住；不能统一归因于 6000 token 预算。q0028、q0035、q0036 所需证据已经选中，仍存在跨句组合和历史时间判读的问题。q0004、q0010 另有参考答案范围与回答范围疑点，保留原判分，不能直接认定评分错误。详见 [多跳错题归因](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-multihop-errors-20260909.json)。

q0041 的早期事件事实、两条完整引用和整段原始来源已经位于首条结果，但后续回顾时间的原句虽然进入候选池，仍被数量上限挡住；当前 32 条证据只占 5080 token。回答也混淆了报告时间与事件时间，补回后续原句不保证转对。三道时间错题尚未发现已证实的时间锚点计算错误。详见 [时间错题归因](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-conv43-temporal-errors-20260909.json)。

下一步先固定本轮全部 47 题和既有 16 题的查询、向量与数据库，用离线检索对照检验两种机制：跨会话事件链的中间证据进入候选池，以及在相同 32 条、6000 token、200 候选预算内保留跨人物、跨时间的必要证据。正确题也一起检查，避免只围绕错题调参。得到机制证据和独立回归保护后，再冻结新候选做一次真实问答，并扩展到原圈定集的其他完整对话；当前不把本次 36/47 外推为 972 题整体分数。

最新记录：[16 题实际评测结果](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-query-focus-qa-results-20260909.json)、[B03 p3 回归归因](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-query-focus-B03-p3-regression-20260909.json)、[下一轮覆盖与协议](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-next-coverage-20260909.json)。

详细记录：[查询范围真实验证](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-query-focus-diagnostic-20260909.json)、[16 题配对审查](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-query-focus-paired-review-20260909.json)、[降级删除修补](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-erasure-fallback-guard-20260909.json)、[B03 最新评测](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-B03-tail02-qa-results-20260909.json)、[B03 两题归因](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-B03-tail02-error-attribution-20260909.json)、[B30 完整评测](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-B30-tail-qa-results-20260909.json)、[检索诊断](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-B30-A06-packing-diagnostic-20260909.json)、[B03 第 45 块成功验证](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-B03-segment45-json-object-results-20260909.json)、[格式配置依据](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-source-format-recovery-design-20260909.json)、[统一修复基底](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-unified-repair-baseline-20260909.json)。

本次离线优化检查已完成：使用实际原查询、向量、应用层分类和只读数据库，LoCoMo 47 题及 MemoryOps 16 题的完整 Search 均精确复现。实验仅在完整原文片段已被实际输出时对 raw 降权，6 项片段覆盖测试和 4 项真实 packing 行为测试通过；但全 63 题对照否定了该方案。LoCoMo 的重点缺失证据仍未补回，部分原正确题的正文和引用减少；MemoryOps A06 p3 的唯一完整预订证据被挤掉。因此实验 `493dfc7` 已归档为不采用，稳定候选保持 `292ea743`，未执行新 Answer/Judge、未改分。详见 [63 题离线对照及拒绝依据](/Users/enoch/Workspace/comp/agent-memory-workspace/reports/priority-raw-evidence-coverage-review-20260909.json)。
