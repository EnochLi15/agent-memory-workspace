# V1 阻塞修复记录（未封版）

更新：2026-09-07。当前候选 service `7a50d22`，370项服务回归通过；V1完整验收尚未完成。原始19次故障逐项现场与原因见 [原始故障映射](v1-blocker-followup.json)，本轮新增问题及修复证据见 [长背景跟进](v1-long-blocker-followup.json)。所有原始失败、诊断身份与评测分母保留。

## 已验证的工程修复

- 对HTTP200中的中断SSE错误做受限的传输分类；传输恢复、语义拒绝和请求截止保持分离。成功重放只有一次提交及唯一回执，失败不留下半状态。
- 修正直接停止跟踪指令的识别、文档标题被当作人物、唯一原文偏移的定位，以及抽取与核验的证据口径冲突。错误授权、无来源事实和不一致擦除继续拒绝。
- 核验缓存按相关消息的原始证据绑定，避免局部修复使无关消息被重复核验；不清除未改变的语义拒绝。
- 分组遗忘按实际传输工作量检查容量，保留完整来源与边界覆盖。失败现场297个配对对应61个来源、3批148821字符，原来的分组前计数造成误拒绝。每批及总传输上限没有提高。
- 核验增加未选中历史记录的范围上下文；修复沿用原有32条候选补充上限，为初始检索窗口外的相关历史记录提供可用别名。最终合法写入和存储范围仍待真实闭环确认。

## 当前真实验证结果

旧候选 `33a9e05` 的固定小集10/10及20项存储检查通过，原文基线9/10；助手复核发现其中存在判分差异，不能据此声称稳定语义收益。详见 [小集对照](v1-small-closed-loop.md)。

同一旧候选的4个完整长背景37题已经全部产生终态并通过审计，但验收失败：候选5/37正确，28题服务错误；原文19/37。详见 [长背景报告](v1-long-33a9e05-results.md)。这些不是1000题整体准确率。

conv-30隔离写入诊断通过。D22隔离写入及读回确认Hilton当前偏好、Marriott历史、Sarah的Conrad和用户Delta号码正确保留。D25先解除容量阻塞后返回200，但读回发现历史偏好仍在，因而未通过遗忘验收。范围核验现能拒绝该不完整提案；后续有界诊断安全拒绝而未提交。补齐候选可见性及目标关联后，最新隔离诊断完成写入并清除历史事实正文与原文；但已擦除事实、操作日志和遗忘标记的scope字段仍保留饮食细节，整体遗忘验收仍失败。见 [最新存储读回](v1-d25-target-links-storage.json)。诊断从复制的失败前状态出发，不能替代完整背景或改写旧得分。

## 运行与恢复边界

7502秒无模型后台验证已完成，主进程与工作进程退出0，无监控空档，所有自有子进程均有终态；运行脚本哈希与开始时一致。详见 [长时验收](v1-runtime-soak-acceptance.json)。12项运行身份/托管测试通过；归档工具10项测试通过，包含固定时钟下连续验证不冲突及篡改拒绝。

原四次恢复失效与920秒合盖、547秒维护睡眠窗口吻合，超过五分钟进程内缓存有效期。原runner后来消失的直接原因仍未知。后台托管使用现有运行入口并记录进程身份与退出信息；caffeinate防空闲睡眠不承诺阻止合盖或强制睡眠。进程重启后不能假装继续已丢失的模型缓存。

增强/离线部署、备份恢复和兼容回退已有旧候选证据，最终服务提交需重新验收。下一步仅处理当前遗忘阻塞，然后重跑固定小集与长背景；通过后冻结全量1000题和交付归档。人工标签仍为0，原中断全量和暂停监控保持原状。

## 范围元数据修复与最新边界

`9db2ce9` 已将擦除事实、操作日志和遗忘标记的scope正文改为摘要，保留边界键，并以s1格式拒绝不兼容旧库。原先泄漏可由回归复现；修复后重启、重放阻断、正确恢复与邻居保留通过，旧读者拒绝新格式且不改写数据。

随后一次独立HTTP诊断安全拒绝，未提交：核验器在无关事实移除后，把已由通过授权核验的操作覆盖的三条同请求事实判为遗漏。`7a50d22` 在当前提案行直接提供准确记录ID及操作关联，编号变动回归通过；尚无新的真实模型成功证据。不再将隔离诊断当成完整验收，下一步执行固定小集及相同长背景。详见 [修复证据](v1-scope-metadata-fix.json)。

新候选 `7a50d22` 固定小集闭环已通过：10/10、20项存储检查、5次合法写入、请求审计及托管退出正常。原文8/10，助手复核识别两道过严判分；不声称稳定收益，人工标签仍为0。见 [当前小集报告](v1-small-7a50d22-results.md)。4个完整长背景仍需验收。
