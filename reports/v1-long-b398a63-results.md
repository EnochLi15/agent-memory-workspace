# 当前候选长背景验收：失败，结果完整保留

service `b398a63`、eval `b8ae27a`、workspace `763f219`。固定4个已曝光完整背景、37题；候选与原文独立空库，同embedding、Answer/Judge、提示词及最终32条/6000估算token预算。四个运行均已结束，终态、来源及HTTP载荷审计通过。

| 指标 | 候选 | 原文基线 |
| --- | --- | --- |
| 正确 / 计划题数 | 0/37 | 14/37（37.84%） |
| 实际进入判分 | 0 | 37 |
| 服务错误题数 | 37 | 0 |
| 已提交 / 计划写入 | 112/174 | 174/174 |
| 终止失败 / 尚未尝试的写入 | 4 / 58 | 0 / 0 |
| HTTP写入尝试数 | 128 | 174 |
| 服务生成尝试 | 569（473成功、96错误） | 0 |
| 服务生成已报告token | 6,385,776 | 0 |
| 整轮耗时 | 22分57秒 | 5分30秒 |

**候选0/37是按完整计划分母保留的服务失败结果，不是回答模型准确率估计。** 没有候选答案可供判分，条件准确率为不可计算。写入成功率若按计划为112/174，不用已尝试分母隐藏58次未尝试写入。原文结果也不是1000题整体准确率。

## 四个终止写入

| 背景 | 失败请求 | 真实终止阶段 | 影响题数 |
| --- | --- | --- | --- |
| D22 | segment-16:0 | 一条确认立场被核验拒绝后，修复连续9次前置连接断开，无修复响应 | 8 |
| conv-30 | session-18:0 | 抽取、修复和核验完成，状态转换连续9次前置连接断开 | 10 |
| conv-43 | session-18:0 | 抽取19次连接错误、6次伴随取消，无抽取响应 | 9 |
| D25 | segment-47:0 | 抽取19次连接错误、6次伴随取消，无抽取响应 | 10 |

四个请求的三次HTTP尝试均保持相同ID和载荷；未追加尝试。只读快照确认失败请求均无回执，修订与已提交回执数一致，捕获期间原库不变。并行任务取消不重复归为独立网关失败。D25未到达segment-49遗忘指令，无法验收该操作及其邻居保留；D22的最终完整背景状态同样未验收。不能因失败库仍含待遗忘信息而将其当作“遗忘成功后泄漏”，也不能声称遗忘机制已经通过。

之前阻塞的conv-30 session-8两个块以及session-15在本轮前缀中已提交，但整个背景随后仍失败。完整生成记录包含81次连接错误、2次截止时间和13次取消；原始失败和旧成绩保留。

## 接口与判分边界

终态后的单次 `/models` 检查返回200；同一个最小JSON生成请求通过SDK和直接HTTP各执行一次，gpt-5.5均约半秒断连；同接口现用Answer模型gpt-5.4-mini也断连。无业务重放、无模型替换。这个结果排除了“只有长请求失败”及“仅我们的SDK路径失败”的简单解释，仍不能区分网关内部路由、后端可用性或其他网络路径原因。详见 [接口诊断](v1-generation-connectivity-b398a63.json)。

原文11题的针对性助手复核记录了6个判分争议、3个明确错误和2个安全拒答与严格rubric之间的差异。其中两个日期语义等价例证较明确，其余争议仍需进一步裁定。非随机、非盲审、非人工校准，人工标签0；不换算新分数，原文保持14/37。详见 [复核材料](v1-long-b398a63-assistant-review.json)。

本轮CPU/GPU、网络和模型运行在共享宿主机；初期还与候选镜像导出读回重叠，延迟不是隔离性能测量。token只统计服务生成已报告用量，不含embedding、Answer/Judge或失败请求的未知账单，不是金额或总计算量相同的对照。

## 交付下一步

当前长背景门槛未通过，暂停进入1000题全量和正式封版。先获取网关生成路由日志或可用替代接口，完成实际生成连通性确认，再在实质基础设施变化后重新冻结验收。维持原重试、截止时间、来源和遗忘校验；不以关闭遗忘/隔离、离线降级或改分母换取通过。

当前候选干净部署、两种模式备份恢复、小集20项存储检查和运行镜像归档证据仍保留，仅证明各自范围。详见 [完整结果](v1-long-b398a63-results.json) 与 [完成集审计](v1-long-b398a63-completed-audit.json)。
