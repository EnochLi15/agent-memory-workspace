# 100题串行评测结果（GLM-5.2）

本轮 `v1-public-100-glm52-02` 于2026-09-07完成，用时约7分23秒。沿用同一批50道LoCoMo、50道MemOps问题和完整背景，使用独立空命名空间。服务提交4790312，评测提交c1bca67，运行配置来自工作区2a5396f。

| 指标 | 结果 |
| --- | --- |
| 题目终态 | 100/100，全部service_error |
| 端到端准确率 | 0/100（0%） |
| 已判分答案 | 0；条件答案准确率不可计算 |
| 服务模型调用 | 47次：46次JSON解析成功，1次invalid_json |
| HTTP 429 | 0次；上轮为50次 |
| 审计推算峰值模型并发 | 1；上轮为15 |
| 成功写入 | LoCoMo 1段，MemOps 11段 |

7个背景均在中途写入失败：5个为分组参与者覆盖不完整，1个为核验覆盖不完整，最后1个在出现非法JSON后，其HTTP重试被恢复机制拒绝（Write continuation cannot resume this request）。成功返回JSON不代表通过语义核验。没有任何背景完成，因此没有检索、回答、判分，也没有重新验证遗忘、更新及邻居保留检查。

本轮验证了限流修正的效果，但未改善完整闭环成功率。下一项应集中处理分组/核验协议与模型输出的兼容问题，并用失败样本验证恢复机制的终止行为；无需继续扩大样本或盲目重复100题。保持现有预算及严格校验。

两个评测进程均正常退出，服务和防休眠进程已回收。HTTP输入审计及100个唯一问题的终态覆盖检查通过；这是结果完整性检查，不是质量验收通过。详细数据见[结果JSON](v1-public-100-glm52-02-results.json)和[审计记录](v1-public-100-glm52-02-audit.json)。旧结果保留，未自动追加评测。
