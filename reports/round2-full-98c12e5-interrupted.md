# 1000题评测中断记录（98c12e5）

本轮未完成，不能发布新的完整准确率。北京时间2026-09-07 01:14:32.359为最后一个模型审计事件；系统日志于01:14:33记录runner连接退出，01:47心跳发现runner、服务和评测进程均不存在。退出码和终止信号未取得，确切原因未知。机器已连续运行51天，可排除此次机器重启；最后一个模型调用成功，不能把进程退出归因于上游模型报错，也不能仅因运行约两小时而断定平台超时。

| 范围 | 计划 | 已判分 | 正确 | service_error | 无结果 |
|---|---:|---:|---:|---:|---:|
| LoCoMo（完成） | 500 | 140 | 83 | 360 | 0 |
| MemOps（中断） | 500 | 26 | 12 | 126 | 348 |
| 合计（未完成） | 1000 | 166 | 95 | 486 | 348 |

LoCoMo得分率83/500=16.6%，已判分条件准确率83/140=59.29%。全轮目前95题正确；95/1000=9.5%仅为已观察下界，166道已判分题的条件准确率为57.23%，两者都不能称为完整新成绩。pipeline_error和judge_error均为0。历史331/1000=33.1%保留。

## 现场与原因边界

- 原始两个manifest、HTTP记录、答案、判分、数据库和模型追踪保持原样。MemOps manifest中的running已陈旧，实际状态以本归档的interrupted为准。
- 两个写入请求没有ingest结果：B02_trajectory_ops/segment-15和B24_trajectory_ops/segment-11。只读数据库统计有591个成功回执、19条closed及2条running准备记录；进程内恢复缓存已经不可用。
- 既有写入失败与整轮进程中断分开记录：19次终止写入包括上游流错误3次、语义核验/修复失败8次、操作目标绑定2次、来源覆盖/擦除一致性2次、恢复不可继续4次。它们通过整背景写入前置条件影响486题；不能将486题等同于486次模型或HTTP故障。
- 不自动重启、不替换namespace或request_id补分。若后续恢复，需要独立记录协议及进程重启边界，不能将其悄悄合并成一次完整运行。

## 核验与恢复观察

现有完成集审计器已核验LoCoMo的500个唯一题目终态、准确HTTP载荷、已判分题的检索记录、返回证据预算以及评分源代码身份。双运行审计明确保留MemOps为pending。对MemOps单独调用最终HTTP审计时，审计器因manifest未完成而拒绝；没有修改该守卫或manifest。另行只读统计确认MemOps已有152条唯一且属于原计划的题目记录、已判分题都有检索与答案记录，重复add载荷一致，服务请求字段符合白名单。这不等于完整MemOps审计通过。

服务98c12e5、eval11da2a6、启动工作区9ecdbe8与归档前HEAD一致；源码未变。冻结spec、两份数据哈希及规范化服务配置哈希一致。相对历史轮，数据、Answer/Judge模型、提示词、top_k和分块身份逐项相同。manifest使用排序紧凑JSON配置哈希，与格式化配置文件的字节哈希按设计不同。

模型审计记录2612次生成尝试（2494成功、118错误），71个传输重试事件。错误分类为provider_stream 3、cancelled 12、connection 53、deadline 22、connection_timeout 28；这些是尝试事件数。

写入恢复事件为591 committed、19 closed、24 pending。按逻辑请求分组，13个请求出现pending后提交，10个出现pending后关闭；11个最终提交确实重放过已完成响应。pending事件并非当前待恢复数量。D19的三次同请求、重放5份响应和唯一回执证据保留；不能由这些成功个例推断普遍可靠性。

独立人工标签仍为0；此前助手复核是模型复核。没有读取封存MemOps数据。未新增功能、实验工具或模型调用。

证据：[中断统计与哈希](round2-full-98c12e5-interrupted.json)、[已完成部分审计](round2-full-98c12e5-partial-audit.json)。原始数据与模型追踪继续保存在本地忽略目录，不随摘要报告推送。
