# 972 题完整评测与独立召回优化

最新独立 QA 轮 `priority-A06-history-qa-20260909-01` 于 2026-09-09 **01:18:59–01:21:58（北京时间）**完成，耗时 **178.825 秒**。历史渲染候选 `8cc94c9` 使用 A06 已完整写入的混合版本末态，按原协议跑完固定 5 题，实际 **0/5，5 题均已判分、无服务错误**；原恢复轮 A06 的 **1/5** 保持不变。51 个历史 HTTP 回执完全一致，没有新摄入或写入模型调用，原库与运行克隆的逻辑内容均未变。第 1 题实际选入证据由 17 条增至 21 条，使用 5,928 tokens；其余 4 题完整 Answer 输入与原轮逐字相同。第 2 题从对变错发生于相同输入，不能归因于渲染；该候选尚无 QA 提分证据。5 次 Search 平均 **0.323 秒**，中位数 **0.195 秒**，不含 Answer/Judge 时间。[完整结果与输入散列对照](priority-A06-history-rendering-qa-results-20260909.json)。

使用这轮实际保存的 5 个查询向量作两版离线对照，新版完整输出及检索审计 **5/5** 与真实调用一致，两版候选池、分数与路径相同。第 1 题省下的 827 tokens 被 4 条新证据的 752 tokens 与一条原证据恢复的 50 tokens 回填，最终净少 25 tokens。新增联系朋友计划的来源上下文补到了 Basenji 犬种，实际答案也使用了它；Choi、妹妹订婚关系、海边行程等关键遗漏仍未补齐。因此存在局部证据收益，尚未转成该题得分。旧版本反事实输出恰好也与原保存输出一致，但这里使用的是新轮向量，不能声称恢复了旧轮未记录的向量。[同向量对照与新增证据审查](priority-A06-paired-retrieval-20260909.json)。

B14 的本地继承见证原型通过 **13/13**，既有范围边界用例 **15/15**；但两个必要安全反例仍在原 `50ccac8` 上失败：food 候选 0、1 的错误扩展删除可单独通过现有解码和计划校验。全包因聚合引用错误被拒，暂时掩盖了这层授权缺口。没有将仅补引用的版本作为修复放行，后续需独立核验完整请求的 keep 例外与最终来源切割、依赖关系一致性。这里的 `searchable=false` 表示抽取后不再从原文召回，不等于来源被擦除。[继承证据与删除范围设计](priority-B14-derived-evidence-design-20260909.json)。

原基线 `priority-full-20260908-01` 于 2026-09-08 23:21:48（北京时间）启动，2026-09-09 00:03:20 因首批门槛失败退出，所属子进程已停止。7 个高风险样本的 **37 题全部因写入失败记为 service_error，实际 0/37**；210 次 add 成功、7 次最终失败、145 次尾部 add 未尝试，成功提交的降级写入 1 次。没有进入 Answer/Judge。其余 935 题未运行，**尚无 972 题整体成绩**，也不能用特意圈出的高风险子集推算整体。

独立恢复轮 `priority-failed-tail-20260909-01` 于 00:05:32 启动、00:20:01 完成，持续约 **14 分 29 秒**，固定服务 `e9b5da4`，使用原历史快照验证 16 题，实际 **7/16（43.75%）**：B01_remember **6/6**，A06_forget **1/5**，B14_forget **0/5**（5 题服务错误）。A06、B01 均完成全部写入；B14 原失败段仍拒绝，保留 9 次未尝试尾部 add。116 条前缀 HTTP 回执逐条一致、前缀没有写入模型调用；新 add 29 次成功、1 次失败，成功提交的降级写入 0 次。控制器在汇总失败后退出，所属子进程全部停止；16 条题目记录完整且唯一。该轮与旧基线分开记分，详见 [恢复结果及耗时](priority-failed-tail-results-20260909.json)。

恢复轮成功的新 add 平均 **15.075 秒**，中位数 **10.082 秒**，p95 **40.362 秒**；11 次 search HTTP 平均 **0.152 秒**。Answer/Judge 单次耗时未被该冻结 evaluator 记录，不能将其推算进 search 耗时。A06 的必需历史事实仍保留，但部分从 semantic route 到 200 个合并候选之间被截断，另一些事实及原始 source 在 6,000 token 打包上限处被排除，并非单一打包故障。另有一题回答复制题目中的敏感干扰项，不能据此认定检索泄漏。

随后将三项写入修复及反馈体积上限整合为独立候选 **`50ccac8`**，完整测试 **565/565**，在 `priority-failed-adds-20260909-01` 对三个原始失败 HTTP add 各验证一次。00:41:25–00:43:49，约 **2 分 23 秒**：B03_remember **200 / 55.993 秒**、B30_forget **200 / 41.070 秒**，回执入库且 revision 分别 19→20、12→13；B14_forget **503 / 44.730 秒**，revision 43 保持，完整逐表内容散列与原快照一致。74 条历史回执、输入与产物哈希均保持，成功降级 0 次，所有所属进程退出。这是失败点写入验证，**不产生 QA 成绩，也不修改此前的 7/16**。[三请求结果](priority-fixed-failed-adds-results-20260909.json) · [集成与回归](priority-repair-integration-20260909.json)。

本次 B14 已正确覆盖两条 Portland 指令，授权与目标核验通过，随后在 `erasure_binding` 返回 `Invalid erasure scope witness`。一个历史聚合卡的 `source_quotes=[]`，模型对它返回 `uncertain`，却只能引用聚合文本，未满足原文见证约束。未进入 `source_erasure`。本次条件性 `AT_RISK_OPERATION_INSTRUCTIONS` 没有触发，不能将进展归因于该新提示；此前精确缺失指令反馈实际出现。只读核对发现，其两个真实父事实共有三条可在原来源中唯一定位的引用，可据此设计显式继承见证协议；不能将生成卡文案冒充原文。模型还提议删除用户要求保留的 food 卡片，完整 keep 语句确实已传入，故不是漏传上下文。补足引用后也不能直接放行原判定，必须同时解决范围误判并保留 uncertainty 拒绝。[B14 新证据报告](priority-B14-erasure-witness-incident-20260909.json)。B03、B30 此处只证明单个失败写入通过，尚未验证剩余历史与 QA。

A06 逐题定位见 [历史证据诊断](priority-A06-tail-retrieval-diagnosis-20260909.json)：候选合并截断、打包预算和回答复述干扰项分别处理。独立历史包装候选 **`8cc94c9`** 已通过 **550/550** 测试，仅对原文与值完全相等、或完整匹配服务聚合模板的历史卡片省掉重复旧值，保持原文、状态、时限、后继和来源支持。A06 第一题固定原选中集合从 5,953 降到 5,126 tokens，节省 **827（13.89%）**；其余四题不变。这是候选冻结时的静态体积比较；后续实际检索会用空出的预算装入其他证据，最新完整 5 题 QA 结果见本报告开头，不能将文本体积节省当成分数提升。[渲染候选报告](priority-history-rendering-fix-20260909.json)。

新 B14 失败发生在语义修补耗尽：第二条 Portland 命令先被错误的 food_preference 删除操作占据，语义核验要求删掉错误操作后，该命令才变成未覆盖，此时修补预算已尽。新版本明确返回 `OPERATION_INTENT`，未进入源切割；不能将此解释为源切割补丁失效或成功。[新 B14 逐状态诊断](priority-recovery-B14-feedback-20260909.json)。

**启动后的首个阻断（23:29:26）**：B14_forget 的 segment-43:0 写入耗时 47.703 秒后返回 HTTP 503 / `EVIDENCE_VALIDATION`：`Source erasure removed every witness for a retained fact`。该样本 5 题已记为 service_error，不重判或补分。控制器继续首批其他风险样本以收集故障范围；首批结束时门槛将失败，不启动剩余 935 题。此故障属于遗忘后事实与来源一致性校验，与下文可恢复的连接重试分别诊断。原运行保留，修复在新的独立 `priority-erasure-witness-fix-20260908/service` worktree 中验证。

**第二个阻断（截至 23:39:42 已记录）**：A06_forget 的 segment-37:0 写入耗时 69.132 秒后返回 HTTP 503 / `EVIDENCE_VALIDATION`：`Source erasure left every original witness intact for an erased fact`，该样本另 5 题记为 service_error。事实删除与原文切割计划的一致性正在单独取证，不与第一条误删原因混为一谈。

A06 随后用 4 个原始模型包完整复现，`prepared.degraded=[]`。失效点是提交期仅因为 `inferred` 事实与被删除内容共享 source ID，就将一条独立的关心朋友的行为模式连带删除；这条事实没有显式 `depends_on`，它自己的完整引用已被原文语义切割计划保留。提交期新增的删除不在准备期固定删除集合中，导致来源进度校验失败。修复需区分整条来源失效与同条消息的局部切割，并继续传播真正的显式依赖和实际见证损失。

**修复候选已冻结为 `e9b5da4`**，独立分支 `fix/priority-eval-recovery-20260909`，完整 service 测试 **538/538**。A06 用完全相同的 4 个原模型输出成功提交 revision 37→38：5 个直接删除目标清除，独立推断事实及其引用原样保留，原删除区间不再出现在 source 中。没有云调用或修改模型输出。B14 的精确指令反馈和 B01 的精确来源编号反馈已完成，但真实模型写入恢复仍待验证，不能从反馈测试通过推断样本已恢复。涉及未核验删除边界的降级会明确拒绝，不再把词语命中的候选当作删除授权。详见 [修复总报告](priority-erasure-witness-fix-20260908.json)。

**第三个阻断**：B01_remember segment-36 引用不存在的 `[2,1]`，消息 2 实际只有 slot 0。初稿先有一个多余 schema 字段，两轮修补后错误引用仍未变；该样本 6 题记为服务错误。新反馈列出错误引用所在 group、fact、具体编号及可用编号，一次列出多处错误并限制反馈大小，保持原引用严格拒绝。3 个原始模型输出在新旧 decoder 中都仍被拒绝，没有偷偷替换来源。[B01 事故报告](priority-full-B01-remember-incident-20260908.json)。

其余四个原始失败点已定位：B03_remember segment-20 同时有 `really` 措辞误拒、同消息引用顺序和独立绑定问题；B14_reflect segment-31 返回明确的 owner/scope `uncertain`；B30_forget segment-13 的修补修改了范围外 operation；B30_reflect segment-31 先违反来源编码约束，修正后仍存在 subject 不匹配。后两者详见 [B30 诊断](priority-full-B30-binding-incidents-20260909.json)。均不绕过核验或重新判分。

B03 的独立候选 `84577fed` 从 `e9b5da4` 派生，新增谨慎限定的 `really` 退休表达识别，拒绝不确定陈述、确认问句和否定反转，完整测试 **545/545**。原模型包只证明授权误拒消失，scope/replacement 问题仍在，尚未证明 B03 恢复；没有替换正在运行的 16 题服务。[B03 候选报告](priority-b03-intent-fix-20260909.json)。

定向恢复 `priority-failed-tail-20260909-01`：B14、A06、B01 三个完整样本共 **16 题**，原 evaluator 标准 HTTP 重发完整历史。计划验证 **116 个原前缀回执**，39 次尾部 add 真实写入；样本失败后未尝试的尾部请求如实保留。启动前原请求 payload/hash 全部匹配快照。候选 141 个产物文件 / 47 个 JavaScript 模块与 evaluator `7a2ecb4` 固定。15 项 launcher 本地测试通过。执行级检查曾实际拒绝在原 controller/子进程仍活跃或 gate 缺失时启动；门槛结束后已启动，不得复用该 run ID 重启。该验证复用旧版本写入的历史前缀，不能视作新版本的全新 972 题成绩。

B14 已通过 3 个原始模型包、完整历史备份精确重放，输入与 system 哈希全部一致，未发云请求。初稿只引用同条消息两次 Portland 删除指令中的第一句；泛化的缺失指令反馈让模型误补麸质限制删除，又被授权校验拒绝。修补耗尽后发生准备阶段降级，确定性 source fallback 将全部候选当作删除授权，一条无关生活经历仅因共享 `used/time` 被整条切除，最终触发回滚。`degraded_adds=0` 仅指成功提交的降级计数，不表示这条失败准备没有降级。详见 [B14 事故报告](priority-full-B14-forget-incident-20260908.json)。

## 冻结版本与范围

| 项目 | 固定值 |
| --- | --- |
| workspace | `06f32ddee608a066fb8ce16b3cd71341c365a7fc` |
| service | `6381bef2d705c3bc63b3616d529149d947a171f1` |
| eval | `7a2ecb42976220e70ff450d30c9569552a8ee1ab` |
| 总分母 | 972：MemOps 472 + LoCoMo 500 |
| plan SHA256 | `db5f4d0399034a4142f0bf3f39b9a695888702d20461f6ef19ae42540e6544be` |
| selection SHA256 | `4613e5fa0eca0ee03346659e0ae71afee8595e475c4ff8f570b9db28a1244c1f` |
| 写入粒度 | 每会话按最多 20 条消息 / 2,000 词分块 |
| 执行顺序 | 单 worker，37 → 435 → 500 题 |

独立 checkout 固定 70 个运行文件的 SHA256，包含 service/eval 编译产物、Python Judge、契约、锁文件与评测配置。详细配置和三个数据文件的散列见 [plan.json](../artifacts/priority-full-20260908-01/plan.json)。三批样本和题目没有交集，合并后与优先清单完整一致。

首批包含 B14_forget、A06_forget、B01_remember、B03_remember、B14_reflect、B30_forget、B30_reflect，共 7 个样本、37 题、362 次 add。它们覆盖此前离线回放的全部 9 个拒绝点。已将预期 362 个 request ID 与冻结评测器的实际分块函数逐一比较，完全一致。

首批结束后检查完整写入回执、样本状态和题目集合。存在最终写入失败、缺失回执或 service_error 时，保留结果并停止后续 935 题；通过后自动继续。中间连接重试成功不会被算作最终写入失败。Answer、Search 和 Judge 错误按 0 分保留，不能通过重新判分补掉；它们与写入失败分别报告。首批 37 题直接计入 972，不会重跑。

运行中的 `correct / 972` 只是当前累计分值；没有答题时不能将它描述为模型准确率 0%。完成报告将同时给出完整分母成绩、已判题数、错误数、分类成绩和分阶段耗时。

## 独立优化候选

候选位于 `artifacts/priority-retrieval-opt-20260908/service`，不替换正在运行的冻结服务。两个提交及编译产物分别保存，可以独立回退和对照。

| 候选 | 变更 | 完整 service 测试 |
| --- | --- | --- |
| `f6d4f29` | 原始证据仅在存在内容相同、来源重叠的事实副本时降权，保留未抽取细节的竞争力 | 511/511 |
| `c46ecd7`，包含上一项 | 时间题仅给已解析的事件时间额外权重，避免未解析观察锚点或合成顺序本身形成事实层优势 | 515/515 |

两组各新增 4 个定向回归用例，修改前均为 3 失败、1 通过，修改后均通过。原有生命周期、遗忘和历史边界测试通过。候选保持 200 个候选、最多 32 条证据、6,000 token 预算，没有扩大上下文。

在完整写入的 conv-30 快照上，使用固定的 72 个本地查询向量，原始版本的 72 题证据 ID 顺序全部复现。标注来源覆盖由 74/96 经首补丁到 75/96，再经时间排序补丁到 76/96；完整覆盖题数由 57/72 到 58/72，再到 59/72。新增覆盖分别来自 q0044、q0031，未发现原有标注来源丢失。

这些是检索覆盖指标，尚不是准确率增益。首补丁在 35 题替换了 70 次非标注来源，时间排序消融影响范围更大；非标注来源也可能是有效证据。观察时间对“何时提到”类问题仍可能有用，是否取消额外排序优势必须通过实际答案验证。q0018 的一处标注与问题对象不一致，不将机械补回它计为质量改善。详见 [诊断与逐题对照](priority-retrieval-next-diagnosis-20260908.json)。

后续质量验证应使用完整固定 72 题，包含证据被替换的题，而非只跑新增覆盖的两题；对冻结基线、首补丁和叠加补丁分别生成一次独立 Answer/Judge 结果，并检查 MemOps 历史/当前值/遗忘题的回归。复用只读历史快照，每个版本使用独立结果目录和服务实例，避免重写历史成本。当前优先定位真实写入失败与恢复轮的历史证据遗漏，不并行争用云模型。

## 连接重试诊断

截至 2026-09-08 23:28:53，99 次 generation 中有 8 次 `ECONNRESET` 和 1 次无效 JSON。8 次连接错误均发生在约 4 秒，响应流尚未开始、AbortSignal 未中止、没有 HTTP 状态；均经一次重试恢复。冻结配置没有 4 秒总超时，且有 34 次成功调用超过 4 秒。现有证据仅能确定响应前连接被重置，不能确定责任端。

连接失败调用和退避合计 36.187 秒，相当于全部 generation 调用耗时 404.077 秒的 8.96%。这解释了一部分额外耗时；该窗口中没有因此产生最终写入失败。数值是启动初期快照，不代表全程比例。完整脱敏诊断见 [连接错误报告](priority-full-20260908-01-transport-diagnostic-20260908.json)。

## 持续运行与检查

控制器具备子进程归属检查、睡眠防护、首批门槛和阶段完整性检查；12 个控制器回归测试通过。已有自动跟进已更新为每 5 分钟检查本次运行，常规进度不重复通知，在首批门槛、失败或完成时汇报。

```sh
python3 scripts/probes/run-priority-campaign.py --campaign priority-full-20260908-01 --status
```

原基线终止状态见 `artifacts/priority-full-20260908-01/phase-status.json` 与 `gate.json`。恢复轮通过 `scripts/experiment_runtime.py` 的 `read_status` 读取 `artifacts/priority-failed-tail-20260909-01/runtime.json`，核验进程身份及子进程；运行后的 preflight 会拒绝既有输出，不用于判断存活。每 5 分钟自动跟进继续处理已定位的独立候选与后续评测，正常进度保持安静，新故障或完成时通知。私有模型追踪保留在运行目录中，不写入公开报告。
