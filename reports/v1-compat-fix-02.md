# 模型协议兼容修复与固定小集闭环

2026-09-07。服务提交 `b36d888`，评测器保持 `c1bca67`。本轮未重跑100题，未修改任何旧分数。完整验证摘要及证据哈希见[JSON记录](v1-compat-fix-02.json)。

## 已完成

- 核验的可选说明允许null；布尔结论和证据引用仍严格检查。否定结论即使缺少理由也保留为否定，不能被下一次有利结果覆盖。
- 补丁只兼容唯一的对象包装 `{"answer":{...}}`，不递归拆包装、不解析内嵌字符串；内部补丁仍经过原有结构、修改范围、来源、时序和语义核验。
- JSON仅清除字符串外的尾逗号，保留全部字段、值、否定结论及引号内标点。审计记录规范化类型和数量，私有轨迹保留原始输出。缺值、缺括号、混乱结构和截断输出仍拒绝。
- 仅对已观测的mixed动作搭配erase原因标签申请复核。复核与原文引用修复共享原来的一次全局调用和最多8个问题的额度；固定动作和引用不得修改。后续不确定、引用缺陷、额外字段或范围变化均终止，不再增加调用。未知分类及其他动作矛盾不获得重试。

并发、间隔、HTTP重试和截止时间保持不变，未增加实验功能。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| 完整本地回归 | 430/430通过，包含拒绝保留、越界修改、原文与邻居保留、跨批次单次修复额度检查 |
| 原始null核验回放 | 2个失败请求的4条原始核验输出通过，无新模型调用 |
| 原始补丁真实复核 | 两份answer包装补丁均通过新的独立GLM-5.2核验 |
| 原始尾逗号响应 | 去除1个尾逗号后通过JSON与抽取结构解析；这不等同于已通过后续语义核验 |
| 原始擦除请求 | 进入一次原因复核，3处resolved、1处uncertain，仍拒绝提交；没有将不确定分区强行放行 |
| 原始结构损坏JSON | 继续终止，没有猜测补齐或重新采样 |
| 固定10题小集 | 5/5写入成功、10次检索、10/10完成回答判分 |
| 遗忘、更新、邻居保留 | 原有20项只读SQLite检查全部通过，检查前后数据库文件哈希不变 |

小集数据沿用 `configs/round2-small-closed-loop-v1.json`，SHA256为 `bae10aeba999800b18e093a80f232a4b7100ea48b08200d0e6e4187a85bbaa87`；使用独立空命名空间、单背景并发和相同GLM-5.2配置。没有补跑原文基线，也不据此主张相对基线的质量提升。

## 分数与未关闭的问题

评分器原始结果为 **5/10**，原样保留。本助手对照输入及检索证据复核后，对其中4条判错有异议：当前城市、工作浏览器、Kevin浏览器、Andrew爱好。评分器把实际存在的原话或观察日期当成了无依据补充。该复核不是独立人工标注，人工标签数仍为0，未另报“修正准确率”。

`small-tablet-sale` 确实答错：原始消息包含“I have not sold it”，但未生成对应结构化事实，原文段落索引也未保留这句，检索没有返回该证据。这是后续需要处理的内容覆盖问题。

本轮完成了具体协议修复和小集完整执行。不能宣称7个长背景均已恢复或整体准确率达标：结构损坏JSON、不确定擦除、评分依据校准和未售出状态遗漏仍有明确边界。下一步先处理这些具体样本，避免继续盲目重复100题。
