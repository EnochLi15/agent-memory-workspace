# 有限次数的核验恢复：修改与验证

2026-09-07。服务冻结 `2c2a5e2`，评测 `c1bca67`，运行时工作区 `3e37a6a`。机器可读结果见 [JSON](v1-verification-recovery-01.json)，请求边界见 [HTTP 审计](v1-small-verification-recovery-01-audit.json)。

## 修改及完成边界

核验 JSON 完全不可解析时，在原有最多两次核验调用内重新核验一次；不会猜测缺失字段。能解析但存在缺项或格式错误时，仅保留独立通过既有结构、证据和语义校验的唯一检查项，补验剩余部分。真实负面结论不会通过重新抽样改成通过，最终仍执行完整校验。

HTTP 续跑缓存核验解析结果，允许修复期间遇到临时传输错误后继续原请求；成功修复可以原子提交。重复、越界或冲突引用不得复用；修复用完、不确定删除、未知字段继续拒绝。提取阶段 JSON 损坏仍为终止失败。

默认仍使用 compact；核验最多两次，写入 115 秒、检索 55 秒、语义修复最多两次及传输尝试最多三次的预算均保持。未加入原文降级、额外实验框架或新的大样本评测。

## 验证结果

`make test` 共 **519 项通过，0 失败**：服务 451、评测 Node 24/Python 17、实验脚本 14、交付脚本 10、本地启动 3。新增回归覆盖局部补验、重复 ID、保留负面结论、缓存隔离、截止时间、替换项索引，以及真实 HTTP/SSE 的修复成功、耗尽后原子拒绝和传输续跑。

固定小集 `v1-small-verification-recovery-01` 完成 **5/5 写入、10/10 检索/回答/判分**。原始模型评分 **5/10（50%）**。复用的 20 项存储检查全部通过，包括遗忘内容及索引清理、旧值失效、新值生效和邻居保留；SQLite 原文件哈希前后不变。HTTP 审计通过，运行进程已退出。

服务生成共 11 次：提取 5、核验 5、来源遗忘 1；全部成功，未出现结构修复或限流重试。服务生成消耗 38,418 tokens（不含 Answer/Judge），五次写入合计 33.91 秒，整轮约 195.48 秒。

## 原始失败回放的实际结果

原始 A06 核验损坏输出先离线重现，再进行一次真实 GLM-5.2 补验。JSON 语法恢复，但模型额外输出未定义的 `source_coverage_checks`，因此仍被拒绝。没有第三次调用，没有数据库写入。本次改动提供有限恢复机会，不能宣称该历史请求已经修好。

固定 10 题没有触发结构错误，因此它证明正常路径没有回归，不能证明真实格式恢复成功率。修复分支由回归测试覆盖，真实失败样例的限制如实保留。

## 评分争议及限制

五道判错题为 `small-city`、`small-manager`、`small-work-browser`、`small-tablet-browser`、`small-tablet-sale`。判分器认为答案附带的引文、日期或归属没有依据；对照保存的检索结果和原始小集，这些内容有对应证据。当前 Judge 输入只有问题、参考答案、rubric 和回答，不含原始证据（`eval/src/runner.ts`）。

以上是助手复核发现的争议，**人工标签仍为 0，原始 50% 分数保持不变**。本轮未售出平板状态成功提取并被检索到；不能据此宣称先前的随机遗漏已稳定解决。没有扩大到 100 题，不能把本轮分数当成整体准确率或正式验收结果。逻辑存储检查不代表物理磁盘或私有日志擦除。
