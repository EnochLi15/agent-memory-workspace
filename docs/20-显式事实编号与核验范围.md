# 显式事实编号与核验范围

2026-09-06。候选服务提交 `8cb099f`，分支 `improvement/write-continuation`，工作区 `artifacts/round2-service-shard-repair`。运行中的主服务仍固定 `87d67b1`、主评测 `ddfab86`。此前两项候选修复继续保留。

## 原因与改动

LoCoMo conv44 会话10的原核验理由描述事实62“烹饪逐渐成为爱好”，编号却为63“烹饪令人放松”。原输入只有数组隐含位置；这可能增加计数歧义，但尚不能证明是唯一根因。

核验 PROPOSAL 和合法平面补丁修复 FAILED_PROPOSAL 现在由服务标注 fact_index、operation_index。编号取完整当前数组位置，增量核验仅检查少数项目时仍保留全局编号。提示要求理由对应同一个带编号条目。紧凑核验的来源 source_slot 和替换 check_index 不变。

编号仅存在于模型输入副本；持久化事实、响应结构、原提案、来源位置均不变。补丁不可编辑编号字段，范围外修改仍拒绝。缺字段分组继续原样进入完整格式修复；不把损坏的提案伪装成有效平面输入。没有按理由猜测62/63偏移，没有扩大修复范围，也没有新增模型重采样次数。

## 验证证据

- [专项54项](../reports/round2-explicit-proposal-index-targeted.txt)、[完整服务305项](../reports/round2-explicit-proposal-index-service-tests.txt)全部通过。新增测试覆盖71条长提案、仅第62条复核、verbose/compact两种协议、操作编号、编号不可编辑和邻居越权拒绝。
- [原样本前缀控制](../reports/round2-proposal-index-control-Uh4Bb7.json)精确匹配原3个抽取调用的 system/input 后重放结果。71条事实编号完整；移除新增标签后，核验JSON与原输入完全一致。源数据库与副本均未改变。
- 默认控制在新核验模型调用前主动停止。报告中的 VERIFICATION_UNAVAILABLE 是 Models.verify 对主动停止异常的包装，不是新发生的模型超时。没有新模型调用，也未宣称该样本已成功写入。

工具 `scripts/probes/proposal-index-control.mjs --live` 可重放同一抽取前缀后，让后续阶段重新调用模型，在私有数据库提交并检查回执、revision和降级状态。它给后续阶段新预算，不能用于全新请求时延比较。完整新灌入仍必要。

## 下一步

冻结回归继续保留全部60题计划分母与已发生的服务错误。结束后先运行LoCoMo编号及E10缺字段真实控制，再按结果决定合入候选和从空库重跑。当前输入改动必须以新代码身份重新灌入，旧失败灌入不能作为完整配对基线复用。

人工独立校准199条仍待填写。完整生命周期、查询质量阶梯、三次重复、封存确认集、新LoCoMo或官方隐藏数据与最终部署仍未完成。当前没有可替代历史331/1000的新总体准确率。
