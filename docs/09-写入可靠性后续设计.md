# 写入可靠性后续设计

本设计基于服务96f40bd及2542f2a完整开发集已保存的失败链。目标是在原90秒模型预算、115秒HTTP预算内完成正确原子写入。现阶段优先解决写入失败及遗忘残留，再开展检索配对实验。下列尚未实现的设计不能算作已有能力。

## 已有证据与优先级

|问题|直接证据|下一步实现|验收条件|
|---|---|---|---|
|前文遗漏，多轮补丁仍不完整|F07_update第6段；B05新抽取JTtJQK|抽取输入按原消息编号给出明确逐消息覆盖任务；在一次抽取中返回全部个人信息。独立核验保留，缺失反馈需完整列出已知遗漏|固定失败提案/原消息验证；保留无意义闲聊、纯知识问题控制，不能为追求覆盖存入所有句子|
|已删事实与原文保留冲突|QlWmpc保留Iris说明|已实施见证上下文和必要一致性守卫；继续验证源分区是否真正清掉相关解释，同时保住独立新承诺|实际提交后检查事实、原文、片段、词法/向量召回及后续回述，不能只看HTTP200|
|原文泛指照应无法判定归属|cRRGIT候选20|源核验增加有界相邻会话证据及明确所属会话；分清泛化提问、实际旧值回述、其他主体。只提供原数据，不注入预期答案|原未决保留；新上下文配置单独记录。加入同词不同人/不同记录/无法判定控制，不能自动把uncertain当retain|
|用户否定尚未提取为事实的助手错误|E10_reflect第44段|让原文成为可定位操作对象；“拒绝存入这条助手断言”与“删除已存用户属性”分别建模|必须删除或抑制错误助手断言，同时保留用户实际健身记录；没有匹配目标不能修改其他记忆|
|错误归因不充分|E10_remember第35段仅26.7秒失败，两个修复错误均无文本且只记录Error|增加不含凭据的HTTP状态、取消/连接/流中断等分类，准确区分耗时到期与模型调用失败|用受控HTTP错误和中断流验证分类；不得从通用错误倒推出限流或超时|
|串行模型调用耗尽预算|JTtJQK源阶段仅余2.7秒|先减少首轮遗漏与重复传输，再统计关键路径。仅无依赖的核验才可并行，所有结果仍在一个事务前验证|逐阶段实际耗时/token、重试和失败分母齐全；不得延长超时或关闭校验充当提速|

## 原文操作对象的实现边界

现在的操作目标以事实ID为核心。助手错误可能只存在于原始消息中，没有任何事实ID；把它硬映射到用户真实的同类属性会误删，省略操作又可能让错误原文进入检索。需要扩展内部准备计划，而非新增赛题HTTP接口。

拟引入可区分的目标引用：`fact`引用已存事实，`source_span`引用当前chunk或已存原文中的确定区间。请求继续使用原来的/add；所有内部引用必须由服务器分配并绑定用户、消息ID、内容hash和偏移。模型不能提交自由文本作为目标ID，也不能用相似词直接证明授权。

处理链：定位用户否定/删除指令及其指向的原文断言 → 核验主体、动作与作用范围 → 生成来源区间操作及受影响事实集合 → 同时校验正确用户事实的保留见证 → 原子更新事实、原文、片段、FTS、向量可见性、事件与receipt。已确认不是用户事实的助手断言不能成为当前状态；明确forget的区间还必须清理可恢复的内容及回述。

不把“否定了某条错误记录”升级成整个属性删除。若指代对象不明确，返回可追踪的明确失败，保留数据库原状。新内部表示需要新版本和新目录；不得直接修改旧评测库的source_format或复用旧计划。

## 控制成本与验证顺序

1. 固定现有失败输入、模型响应与源码hash，先用最小回归证明具体缺陷。修复后的新模型输入、协议及结果另存，所有失败保留。
2. 在单chunk及私有数据库副本验证准备和提交，再检查真实检索内容。初始提案/前级判定重放必须明确注明，不能拼接多个阶段时间称作端到端延迟。
3. 通过对应诊断与反例后，冻结服务版本，在五个已曝光背景新目录完整运行。2542f2a已完成且0/30，原失败与终止快照已归档；后续版本须重新完整灌入。
4. 全部写入成功之后才进行facts/sources/rerank/coverage固定Answer/Judge配对。若完整灌入失败，不复用成功子集做正式配对分数。
5. 完成E0评分校准、三个完整配对重复和未曝光新背景，再决定部署。正式赛题未提供的Answer/选择器仍需保持口径边界。

不以增加测试数、降低独立核验标准、反复采样直到通过或放大时间预算证明进步。198项测试证明的是已检查的工程行为；当前没有新的完整质量通过结果。
