# 共享授权表与完整评测

2026-09-06，服务检查点 `87d67b1`。联合原文核验协议升级为无损共享完整授权表，默认实验开关不变。原始候选、固定删除义务、存活邻居、语义拒绝和原子提交检查均保留。

## 传输与语义验证

每个批次带完整 `BOUNDARIES` 表，每个来源保留全部 `boundary_refs`，引用包含批次内编号及该来源自己的匹配词。只有完整边界及授权对象完全相同才共享；同key但不同授权不合并。批次之间各自携带完整表，不依赖其它调用的状态。原文、链接事实和精确引文不截断。

297项服务测试、28项专项通过。新增无损重建、同key不同授权、每批本地引用完整性；此前的漏删拒绝、同名邻居、取消、全局一次引文修复和候选完整映射继续通过。

| 控制 | 结果 | 限制 |
|---|---|---|
| 固定B05工作容量 | 74对候选→51来源完整保留，5批→2批，252,341→89,141字符，减少64.7% | 仅计传输，不等于语义通过 |
| 保存7个完全一致前序模型包＋真实原文核验＋副本提交 | 5/5通过；成功原文输入26,447 token，输出2,424 token | 有一次连接失败重试；约39.6秒；不是完整请求或稳定延迟改善 |
| 空库真实HTTP | 7/7通过；种子11.124秒、删除41.902秒；70条助手回述清除、兄弟同值访问码和浏览器保留 | 合成控制，不是完整背景或质量成绩 |

上一版固定控制的成功输入为92,616 token，本版下降71.4%；两个单次控制并不构成可靠成本/延迟分布估计，也不能忽略失败重试。前版副本控制约16.2秒、本版约39.6秒，减少输入没有同时证明更低延迟。全部证据见[共享表复核](../reports/round2-source-erasure-shared-review.json)。

## 完整评测配置

新增 `configs/round2-joint-source-quality.json`，预先固定 sources → rerank → coverage → events → conditional，以及每组 r2/r3 独立推理重复。最终32条/6000 token、Answer/Judge固定，只有所有原始写入成功且service/eval版本干净一致，才允许通过HTTP幂等回执复用灌入。原始失败保留计划分母。

source-first要求安全原文可检索，所以这里不把关闭原文的事实视图与它混作同一表示的检索消融；B1/旧U3的完整对照仍须独立记录版本及写入差异。配置存在不表示评测已完成。

完整旧开发输入包括MemOps五条完整操作变体（3个背景、250会话、30题），以及LoCoMo两个完整对话（58会话、30题）。它们都是已曝光回归数据，不能作为新的未见验证。新背景选择仍由曝光审计及预封存协议控制，人工校准与最终交付门槛保持开放。
