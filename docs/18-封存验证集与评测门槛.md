# 封存验证集与评测门槛

2026-09-06。完整实施的确认阶段需要开发期间未用于调参的背景，不能把旧500+500或已诊断B05/E10称为未见测试。

## 曝光审计与封存

[第二次曝光审计](../reports/round2-exposure-audit-v2.json)覆盖484份执行日志，原始目录有403个操作变体、100个背景，其中46个背景有记录曝光，54个没有记录曝光。审计按背景排除全部操作变体；没有记录曝光并不能排除未记日志的人工阅读或预训练接触。

`scripts/seal-unexposed-memops.py`在封存前逐一检查审计输入日志哈希。按固定种子`round2-confirmation-20260906-v1`和身份哈希选择，每类remember/forget/update/reflect/trajectory_ops各3条变体，15条来自15个不同背景。选取不依赖题目、参考答案、rubric、模型输出、分数或难度，原样复制每条的全部会话和全部题目。

[公开封存清单](../reports/round2-sealed-memops-v1.json)记录150题、750个会话、24个组合类别的数量及文件哈希。原始数据留在忽略目录`artifacts/round2-sealed-memops-v1/memops.json`，权限0600；清单与工具输出不包含问题、答案、rubric或对话正文。本次工具解析语料用于复制，但没有把这些内容展示给代理。3项选择器测试覆盖背景隔离、全部样本保留、各操作数量、排序及答案变化不影响选择，以及无足够背景时明确拒绝。

**这15个背景的所有操作变体现在均保留给确认阶段，不能再用于开发调参，也不能在看到结果后替换困难样本。** 若某个背景后来意外用于设计，应在原清单上明确记录污染，不伪称它仍未见；下一版确认集必须重新审计且保留旧记录。

## 使用顺序

1. 当前冻结`87d67b1`的开发回归先完成：LoCoMo30题、MemOps30题，全部输入从空库灌入，所有错误保留计划分母。
2. 只有原灌入全部成功且代码版本、数据哈希、写入配置一致，才运行预先固定的检索阶梯和r2/r3重复。配置存在、部分样本通过均不能代替完整结果。
3. 独立人工判分校准仍待完成。当前199条`reviews.jsonl`均为`pending_independent_review`，不能把模型判分或自动一致性检查填作人类标签。
4. 开发与校准完成、候选配置冻结后，才对封存数据进行确认。沿用固定Answer/Judge、证据预算和完整分母，按背景做配对统计及重复推理。若确认失败，需要保留结果并承认这一批已经曝光。
5. LoCoMo现有10个对话均已曝光，当前两个开发对话仅是回归。真正的新对话/正式隐藏集和最终部署归档仍未完成，本MemOps封存不能替代这些门槛。

## 当前运行

`round2-joint-87d67b1-dev-r1`使用`configs/round2-joint-source-quality.json`的sources配置，同时运行两套基准，每套并发1。服务`87d67b1`、评测`ddfab86`、启动时workspace`86875d2`均干净。运行期间服务与评测源码保持不变。进度见[记录](../reports/round2-joint-87d67b1-dev-r1-progress.json)，它是带时间戳的观察，不能代替进程状态或最终报告。
