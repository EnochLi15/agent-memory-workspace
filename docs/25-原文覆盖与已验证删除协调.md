# 原文覆盖与已验证删除协调

## 故障与目标

`round2-resilient-bfec674-dev-r1` 的 E10_reflect 第26段被事务守卫拒绝。原文覆盖核验把“之前说错日期、随后纠正”的附带说明作为 source-only witness；独立删除计划则依据同段用户“无需再记录该牙医预约”将该说明及预约信息一并删除。事务末尾仍要求该 witness 可检索，于是回滚。

这属于原文覆盖与合法生命周期删除的协调缺失。目标同时满足：删除已获授权的信息；保存不受删除影响的原文和结构化偏好。不能通过保留预约信息、忽略事务守卫、重采样已接受的核验或漏记失败题来解决。

## 修改

服务独立候选 `26ebc1a`（包含上一候选的真人角色提示修正）调整最终覆盖检查：

1. 事务验证 source erasure 与 source operation 两类计划后，复制其已验证的删除区间。后续确定性兜底产生的额外区间不进入覆盖豁免，防止误删邻居被自动合理化。
2. 对每个已接受原文 witness，在原 UTF-16 坐标上扣除这些区间。完全删除时，相关片段不能继续携带内容或存在于检索索引；部分删除时，全部剩余片段仍须逐字一致、独立于状态事实、active 且已索引。
3. 未解释的删除、其他来源的区间、额外删除、事实绑定、索引丢失和残留被删文字均继续拒绝。覆盖 plan 的身份、原核验及独立删除计划验证保持生效。
4. source coverage 提示同步说明最终生命周期检查规则；覆盖判定本身不授予删除权限。

主运行的 service `bfec674` / eval `11da2a6` 没有修改或重建。候选已推送，独立运行时保存在 `artifacts/round2-runtime-26ebc1a`，未合入主工作树或用于当前灌入。

## 验证

[最终证据汇总](../reports/round2-erasure-coverage-reconciliation.json)：

- 从原失败前 SQLite 副本出发，冻结 bfec674 运行时精确匹配8个模型调用的 system/input/purpose 与响应；在未改变的原守卫处重现拒绝，数据库回滚验证通过。
- 相同保存的模型决定，经冻结抽取代码准备后交给候选存储，私有提交成功。只使用固定测试向量，没有新模型或线上服务调用。
- 6项提交检查通过：revision只增加一次、回执持久化、无降级、预约背景删除、SMS偏好保留、存活原文已索引。
- 伪造删除 fingerprint 被拒绝；索引阶段故障注入后快照与回执均回滚。原失败数据库文件哈希未改变。
- 先在旧实现上重现新增测试失败，再修改实现；最终318项服务测试通过，其中10项原文覆盖测试、3项新增回归，覆盖完整/部分删除及越界、残留和缺索引情形。

复现命令：

```sh
# 冻结旧代码，捕获原守卫拒绝，不使用新模型。
node scripts/probes/raw-coverage-diagnostic.mjs
# 相同旧模型结果交给候选存储；在独立数据库验证提交与回滚。
node scripts/probes/raw-coverage-diagnostic.mjs \
  --commit-runtime=artifacts/round2-runtime-26ebc1a
```

需使用项目指定 Node 24.18.0。探针在 artifacts 下保存私有运行副本与详细报告；固定向量不能证明 embedding 质量。候选存储验证只证明这条原始失败请求的事务协调，不能替代全新模型生成、完整背景灌入、生命周期全套验收或总体准确率。

## 后续

当前回归仍继续，失败题保留原状态，候选不重写旧结果。待其结束后，验证真人角色解释和本候选的真实请求表现，再决定合入及全新灌入。独立人工标签、新 LoCoMo 数据、三次配对检索重复、封存确认及最终交付继续开放。
