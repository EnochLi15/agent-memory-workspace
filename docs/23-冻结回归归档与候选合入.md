# 冻结回归归档与候选合入

2026-09-06。`round2-joint-87d67b1-dev-r1`已结束；runner记录两个评测进程退出0，原服务和评测进程均已退出。退出0只代表脚本执行完成。

## 完整结果未通过

[最终记录](../reports/round2-joint-87d67b1-final.json)：LoCoMo30题、MemOps30题，实际判分0题，全部60题为service_error。七次终止写入导致7个样本没有进入问答，此前共199次独立写入成功。这不是新答题准确率，不能覆盖历史331/1000。

[完整服务请求审计](../reports/round2-joint-87d67b1-final-audit.json)核对了两套数据哈希、服务端请求内容、题号覆盖和终止结果：所有计划题号都有唯一终止记录，保存的服务请求符合原数据chunk或问题白名单。审计通过只证明记录及边界完整，不代表记忆语义或质量通过。没有成功检索，因此检索记录文件缺失被明确标为null；有judged结果却没有对应检索记录时审计会拒绝。

最后的[E10 remember第38段](../reports/round2-E10-remember-87d67b1-repair-failure.json)先成功修复一次，随后第二次修复连续两次连接失败。失败前数据库、请求、全部模型包及哈希已封存，原结果保持失败。[候选完整HTTP控制](../reports/round2-fresh-failed-add-2chC4Q.json)在失败前副本重新生成所有模型阶段后返回200，事务/回执2/2通过；它仍不能替代全背景新灌入。

## 合入范围

确认原进程全部结束后，service `improvement/round2`快进到`bfec674`，eval快进到`11da2a6`，两者重新构建成功。

服务包含缺数组进入既有修复、历史删除范围不重复覆盖保留原文、显式事实编号、分片传输错误分类，以及受原截止时间控制的传输等待。315项服务测试和真实控制见之前报告。

评测包含人工标签校准工具和报告修正，24项Node测试、17项Python测试通过。新增question_status_counts、unrecorded_questions，来源覆盖为空时不再声称数据集缺标注。新manifest保存sample_groups；group_outcomes把相同背景的全部变体放在一起重采样。缺分组信息、尚有未记录题或不足两个背景时不提供背景区间。原sample_bootstrap_95保留但明确标为旧样本ID统计，不重写历史文件。

人工表仍未由独立评审填写，新工具没有生成或替代人工标签。

## 下一轮仍须从空库验证

使用独立`round2-resilient-source-quality.json`配置，显式允许最多三次暂时性传输尝试，保持原请求截止时间、语义修复轮数、32条/6000token及固定Answer/Judge。额外调用属于可用性和成本变量，不宣称算法准确率收益。

不能复用这次不完整的灌入。下一轮使用全新namespace和数据目录，仍完整执行当前已曝光的两段LoCoMo对话及五个MemOps变体。只有完成写入后才能进入合法的检索配对；完整质量阶梯、三次重复、独立人工校准、150题封存确认、新LoCoMo/隐藏数据和交付门槛继续开放。
