# 目标绑定与定向修复：真实失败驱动的下一阶段设计

状态：部分实施，完整设计仍未验收。稳定同chunk句柄、局部补丁修复、逐事实核验及确定性目标解析已落地；操作级结构化/词法候选扩展已实现，独立语义目标分类与向量/原文目标扩展仍待实施。证据为第二轮四个原始失败前缀，均属于已曝光回归数据。

当前核心问题是提取器同时负责“识别发生了什么”与“决定修改哪一条记录”。当不存在对应旧记录时，模型会把纠错绑定到可见但无关的m0；属性守卫可以阻止破坏，但整份JSON重生成仍可能重复相同错误。来源引用、同chunk顺序与删除范围又混在同一次生成中，使修复容易互相干扰。

```mermaid
flowchart LR
    A[原始消息和来源位置] --> B[事实与操作意图提取]
    B --> C[按主体、属性、对象生成候选]
    C --> D[独立目标解析]
    D --> E[来源与语义核验]
    E --> F[原子提交事实、原文可见性与事件]
    D --> G[类型化失败与定向修复]
    E --> G
    G --> C
```

## 需要改变的边界

1. **提取阶段不直接授予数据库修改权。** 操作意图包含动作、人物、对象、属性、范围和精确来源；在目标解析完成前，模型生成的ID只是候选建议。避免把“可见第一条记录”隐式当作默认目标。
2. **目标解析返回明确结果。** `resolved`给出租户内实际目标及逐目标依据；`unrecorded_correction`只适用于已证明为未入库助手错误的纠正，保留有用户原文支持的新事实；`ambiguous`、`missing_evidence`继续修复或拒绝。不得把所有找不到目标的forget都变为成功no-op。
3. **候选扩展按真实意图触发。** 首先查主体/属性/实体，必要时扩展词法与向量候选；只把确实相关的事实和原文送给绑定器。原文尚未形成事实时需要创建可追溯的来源目标，不能用一个experience大桶代替。扩展受统一请求预算与最大次数限制。
4. **稳定同chunk句柄。** 新事实句柄在拆分与修复中保持稳定，只有先于操作的来源可被绑定。修复不能重排数组后把new:N指向别的事实；未知/未来引用是类型化错误。
5. **定向修复不重生成无关内容。** 分开修复来源位置、候选引用、范围与遗漏事实。已验证的事实保持不变；修改的事实、依赖及操作再核验。核验格式错误可以有限重试，语义失败不能靠反复重判取通过结果。
6. **语义核验覆盖每条新事实。** 除已有的操作与消息覆盖检查，还要验证新事实是否由用户原文支持、是否把计划当已发生、是否把泛泛问题当个人属性。来源出现了同一个词不构成语义支持。

## 四个失败的验收边界

| 前缀 | 必须验证的实际状态 |
|---|---|
| D24_reflect | 无关投资计划保持原样；Outback的开放感/窗户及RAV4压抑感保留为有用户来源的事实；被否定的AWD主因不作为当前原因返回 |
| E03_reflect | 跨段人物、组织、交接和反思来源完整；未知new:N不静默丢弃；保留合法事件历史 |
| C30_update | auto-invest的旧/新金额、标的及相关转移事件分别正确；旅行计划不能挤掉前面的金融信息；真实历史不被纠错删除 |
| D17_remember | 平板及同步问题退出所有返回路径；工作电脑Firefox、手机Safari及其他人的Brave信息仍按各自主体/设备保留；不能删除整个experience属性 |

每个前缀需要原始HTTP重放、实际持久状态、当前/历史/操作查询及原文可见性证据。补充否定、引用、换主体、同值不同设备、未入库目标及多指令变体。HTTP成功率、语义状态通过率、离线降级率、模型调用次数与p95延迟分开报告。

## 发布与评测门槛

保留所有失败运行及源码hash。四前缀和变体未通过时不以80项单元测试作为上线依据，也不更新整体准确率。通过后冻结服务/提取模型/Answer/Judge及预算，在曝光审计之外选择新背景组；同一组输入做至少三次完整配对运行，分别测量写入、召回、最终回答和成本。条件扩展与选择性重排再按消融结果决定是否启用。


## 本轮对照新增约束

D24同源码下，gpt-5.5通过写入、持久状态及两次真实问答，mini在目标绑定处失败；同时总写入约81秒对36秒。E03/D17仍触及80秒模型预算，C30降级仍留有错误当前状态。这说明模型选择值得纳入消融，但不能替代绑定与修复架构。

后续可比较“主提取mini、困难绑定或定向修复使用更强模型”与统一模型方案；模型路由必须显式记录，不能把升级模型的收益归为纯代码收益。先去掉重复生成整份提案的成本，保留相同接口时限；核验输入压缩须保留实际说话人、照应上下文与来源支持，不能为压缩token删掉决定授权的上下文。


## 确定性目标解析已实施，长段核验仍需减小重复工作

`binding.ts` 返回resolved、missing_evidence或ambiguous。显式ID、属性选择器、重复表示及property遗忘边界统一解析；每个解析目标附带选择依据。提取后先解析并把完整目标交给独立语义核验，提交时用相同规则复核，不能在核验之后悄悄扩展出未检查的目标。缺失/未来目标不作为成功no-op。恢复或初始更新的空目标仍须满足原有授权/新事实要求。

目标找不到或范围不匹配时，按该操作的主体、属性、scope与引用文本，从租户快照追加至多32个候选，而不只复用原先按整段对话排名的120条。候选只是给定向补丁看的建议，不自动改绑ID。当前单操作核验目标上限为256条，超出时拒绝；大范围分组核验尚未实现。尚未接入语义向量扩展、原文目标，以及单独的unrecorded_correction证明类型。

性质不同的遗忘已分开：property边界覆盖同一主体/属性/scope的历史值与当前值；value边界只扩展同值重复表示。错误纠正的扩展以已选旧目标为准，不使用操作字段中的新值。源位置判断还拒绝同消息内不唯一的短引用。

真实失败说明，局部补丁之后仍全量核验会耗尽预算：C30只改一个content限定语，第二次核验仍重查整段；E03补回计划时又写错模态。下一步实施请求内的核验记录复用，必须满足：

1. 记录绑定请求原文、模型/协议版本、事实/操作内容及引用目标的哈希；不能放在多个租户共用的Models实例上。
2. 只复用结构合法、来源合法且语义通过的逐项结果。失败、歧义及格式错误不是通过记录。
3. 修改事实后，重验该事实、依赖/替换关系、受影响操作与消息覆盖；删除或重编号须使旧引用失效。所有新项目必须核验。
4. 为第二次核验传入完整原文上下文和提案，但只要求返回失效项目的检查；合并已通过记录后仍检查完整覆盖。不以省token为由删掉授权上下文。
5. 测试跨请求/租户隔离、记录伪复用、依赖变更、缺项与格式失败，再用原C30/E03失败输入验证。不能提高接口时限或只报告模型最好的一次结果。

### 请求内增量核验实现

`Extractor.prepare` 每次创建独立 `VerificationSession`。记录绑定请求全文、模型/协议身份、事实及操作内容、来源、目标和传递依赖的哈希；embedding变化不改变语义证据。修改或删除事实、调整索引、改变目标/依赖、添加项目都会使相应检查及消息覆盖失效。缺失或循环依赖明确拒绝，旧的并发计划不能向新计划提交核验结果。

只有完整协议校验成功后，逐项通过结果才进入私有内存表。协议错误不产生通过记录，已确认的语义拒绝仍保留；未修改的拒绝不能重抽样取得通过。`CHECK_SCOPE` 仅限制本次输出范围，完整原文、提案及传递目标上下文仍发送给模型。将本次结果和复用记录合并后，再运行唯一性、引文及完整覆盖检查，重复输出缓存项目或缺少新项目都会拒绝。

`MEMORY_INCREMENTAL_VERIFICATION=false` 关闭正向复用，用于同协议消融；拒绝记录和来源约束仍保留。补丁新增事实现在必须显式填写modality，提示说明未来意图即使表达坚定也保持tentative；局部编辑不把未提供的字段重置为默认值。

112项服务测试覆盖上述边界及真实Extractor调用链。[原C30记录的离线机制重放](../reports/round2-recorded-C30-verification-scope.json)中，修正fact0会连带重验fact1、纠错操作、替换关系和消息10/14：36项中6项重新检查，30项可复用。该探针没有新模型调用，不能据此声明真实延迟收益；最新完整前缀及对照结果另记于阶段状态报告。

### 首轮生成成本与有界修复

[5355177真实复测](../reports/round2-incremental-verification-live-review.json)证明首次检查仍可能耗尽预算：E03第7段有实际复用，但第8段仅提取就花67.8秒；C30第14段提取与首轮核验合计约88秒。增量复用不能直接消除首次检查成本。

已实现显式的extraction/verification/repair模型配置，核验记录绑定实际核验模型，成功/失败审计都记录实际路由。默认仍用统一配置；模型失败不会自动切换其它模型。[一轮补丁的路由对照](../reports/round2-stage-routing-live-review.json)也未完成原前缀：较快提取之后仍可能留下来源/覆盖问题，不能只凭生成速度推导端到端提升。

进一步提供`MEMORY_MAX_REPAIR_ROUNDS=1|2`，默认1。目标范围与能独立发现的来源问题合并反馈；两轮模式给首次结构修复或新增遗漏之后留下另一轮有界补丁机会，默认模型总预算90秒、写入115秒均不延长。未变化的语义失败不重新判分；第二轮改变的事实、引用与消息覆盖照常失效重查。该参数仍属实验配置，不能用增加轮数代替覆盖标准校准。最新验证见[阶段状态](ROUND2-STATUS.md)。

### 紧凑核验与引用错误分类

`MEMORY_VERIFICATION_FORMAT=compact`使用固定元组，只传回布尔结论、事实来源槽位和替换目标编号。服务将编号解析回本次请求的精确来源与目标，再经过同一个规范校验器；完整消息、提案与目标上下文仍发送。默认verbose保持不变。格式错误不生成通过记录；有效语义拒绝即使位于错误字段之后也必须保留。

原C30真实输出把fact3列为message10的覆盖依据，但fact3实际来自message12。message10本身已有fact0/1/2的正确来源链接，因此这只能证明核验引用错误，不能证明该消息缺少记忆。`441cc48`提供服务器计算的`MESSAGE_SOURCE_LINKS`：有合法来源项却填错引用时属于协议错误；完全没有来源项却声称represented时仍属于覆盖失败。来源链接只证明出处，不能证明语义完整。模型必须重新判断全部内容，服务不能通过裁掉非法引用直接放行。

[原始输出红绿重放](../reports/round2-recorded-coverage-references.json)保留了旧版077aaf8与新版441cc48：旧版记录message2/4/6/10失败，新版只记录2/4/6真实遗漏，原样再核验不会改变拒绝。回归同时验证错误协议不产生正向缓存，修复真实遗漏后必须重新核验其余缺少有效记录的项目。129项服务测试通过；真实完整前缀结果另记。


### 明确的引用恢复、模态和核验格式

来源恢复先要求唯一匹配：跨消息只恢复唯一的大小写敏感空白差异；大小写恢复只在声明消息中且必须唯一。恢复后保存原始子串，并保留语义核验。未落实的坚定未来行动为tentative，真实已完成事件与当前偏好仍可confirmed。

compact核验可选严格JSON Schema外层四数组；本地继续检查元组语义、引用及检查范围，协议/模型/响应格式变化使请求内记录失效。严格格式不能消除事实遗漏或核验器的语义分歧。[完整E03失败](../reports/round2-E03-strict-prefix-review.json)说明仍需解决稳定覆盖。

### 操作效果必须满足用户请求

实际D17回归表明：来源和目标均匹配也不足以证明删除执行。`retract`会保留合法历史，`forget`才清除记忆及对应来源。把停止保留信息生成成retract时，进入有界修复；事务也拒绝相同错误。省略检测不再把该retract当作满足请求。只有明确从当前关系列表移除且边界current_relation，才允许保留历史。普通事件结束不自行授权清除；明确停止保留覆盖的多个属性仍需完整绑定。

这条规则只锁住已识别的直接指令效果，不宣称覆盖任意自然语言意图，也不证明模型永远选全所有目标。[机制重放](../reports/round2-recorded-retirement-effect.json)与[状态/回答复核](../reports/round2-source-order-state-answer-review.json)分开保存。


### v3：独立的删除范围判定

B05说明，`subject + scope`字符串相等不足以定义删除边界：同一个已删除名字被写成新的removal_reason或baby name list，就可能绕过标记；反过来，全局屏蔽同名词会误删同事或其它项目。新增`erasure.ts`把这一步从普通事实核验中分离：

```mermaid
flowchart LR
    A[事实与操作通过核验] --> B[已授权删除边界与持久值指纹]
    B --> C[发现同值候选]
    C --> D[核验模型区分改述与独立信息]
    D --> E[请求与事实指纹绑定的判定]
    E --> F[事务内重查完整性]
    F --> G[清除改述与来源 或保留独立证据]
```

候选来自本次事实及既有未清除事实，与本次forget及历史删除值指纹比较。确定的同属性重放直接清除；其它候选逐项返回erase/retain/uncertain及原始来源引文。新scope名称不是独立性的证据，家庭共同主体也可能表达同一记录；真正不同主体、实体、属性、项目可以保留。判定最多处理64个歧义候选对，缺项、重复项、不确定、无效引文均拒绝写入，无离线放行或语义重抽样。

判定绑定原请求、前置事实、操作、提案及边界；提交时重算并验证，防止跨请求或旧提案复用。事务会清除被判为同信息改述的事实、向量和来源；独立信息的保留证据仅绑定该事实与精确引文，不允许它豁免整句原文。未保护的同值回述按原始偏移裁剪，原文向量失效，独立且未提取的邻居片段保留。旧助手回述没有事实链接，也会在forget时从原始可检索历史和会话尾部清理。事务失败同时回滚删除与保留判定；v3提交使用提案副本，防止回滚过程改坏调用方的指纹化提案。

开关`MEMORY_ERASURE_BINDING=true`启用v3，默认关闭；必须用新目录重新灌入，不能混写v2。该阶段使用核验模型，审计purpose为erasure_binding，与提取、修复、事实核验共享原90秒预算。严格格式使用专门的erasure_scope_v1 JSON Schema，不套用四数组核验协议。

局限必须保留：候选发现基于规范化字面值指纹，不等于能发现完全不含该值的所有语义改述；相同字面值与独立上下文的区分仍依赖核验模型，五个控制不能证明普遍正确。原文裁剪保障已定位值及事实来源的可见性，不承担磁盘物理安全删除。完整前缀、后续回述、同名保留与最终Answer必须继续独立验收。

[146项服务测试及实际控制/检查点证据](../reports/round2-erasure-binding-review.json)覆盖B05原提案两个改述、一次新模型检查点、D17、五类遗忘变体、五个同值语义控制。最初克隆仍残留三条旧助手回述，失败记录保留；最终交易复测没有重新抽取一个更有利的模型判定。克隆中的无删除标记v2检查点只用于明确标注的诊断性移植，不代表支持生产迁移，也不替代完整新前缀。


### 新完整开发集对设计边界的约束

保存的B05诊断说明：即使原有forget目标正确，覆盖修复仍可能给新承诺增加指向备用名字的错误supersedes。应将“补充缺少含义”和“改变已有状态”分开授权：补丁新增替换边需要独立的同属性目标与用户来源证据；不能因为在同一条消息或同一名字决策中就认为可替换。现有提交前校验会拒绝，但太晚发现会耗尽两轮修复；需要在给出修复范围时就附可用的类型化关系约束，不自动裁掉非法关系后放行。

E10的直接“you don't need to keep track … anymore”漏识别已经补齐；否定遗忘、条件、转述和引用仍按原守卫拦截。这只补一个有证据的自然语言缺口，不能代替完整意图分类。

修复后E10的真实私有提交仍残留助手原文中的预约改述。长值指纹包含office，而改述省略了该词；这给此前“非字面改述未覆盖”的局限提供了实际反例。后续原文目标绑定必须覆盖没有事实链接的助手句段：候选发现与授权分离，逐片段返回删除/保留/不确定，判定绑定原请求、原始偏移、租户revision、授权目标及保留邻居。混合段落中短信、咖啡、礼物信息不能随预约一起被清除。还要验证存储raw、会话尾部、索引、事件、后续再次提取及最终Answer；当前没有实施这项扩展。


### v4原文范围已接入事务

源码`source-erasure.ts`与可选`MEMORY_SOURCE_ERASURE`已实施上述原文判定的一部分：读取租户全部原始消息用于候选发现；完整字面值与目标词项哈希仅提名，不授予删除权。送入核验模型的是相关整条消息和事实，消息输出须按原样全文分为erase/retain片段，事实保持单一判定；任何不确定都中止。以整条消息覆盖紧邻代词回述，避免仅截取匹配句时遗漏余下上下文。

v4快照附带原文和删除标记，判定指纹绑定全部源文本、请求、前置与新增事实、操作和边界。提交前重算，随后空白遮盖原文以保持原始UTF-16偏移，裁剪片段并使向量失效，清理保留事实的来源，引文全部被清除时拒绝保留无依据事实。原文、事实、索引、标记与回执一起回滚。后续消息用持久词项哈希提名回述。

模型提示分别要求“同记录证据足以删除”“独立主体/上下文证据足以保留”；原被删除值已不可见时，明确的同事/他人且非用户记录仍可证明独立。孤立姓名没有证明主体不同，仍可不确定。155项测试及真实控制/前缀证据见阶段状态。

这仍不是完整原文目标解析：forget首先须绑定已有或同段临时事实；只有原文而没有可绑定事实时尚不能正确执行。完全不同词项改述和跨消息指代的候选发现仍不足；超过64项或64000候选字符时明确拒绝。后续要让原文证据能作为独立目标，并为未结构化个人信息提供可检索覆盖，避免把每个非关键抽取遗漏都变成整个背景写入失败，同时保留操作完整性与来源隔离。


### 修复范围与隐式状态变更的进一步边界

9726881已实施最新提案范围重算与REPLACEMENT_TARGET_GROUPS；事实/操作追加后的新索引能被下一轮修复，无关项仍不可改。覆盖补充默认没有替换边，同类型分组只提供必要条件，不能自动授权。

E10_remember的真实五段写入与实际Answer显示，仅核验显式supersedes不够：storage对不同字面值的同槽单值事实自动按时间隐藏旧事实。兼容的“黑滴滤咖啡”和“喜欢简单咖啡日常”被误判成状态变化。后续需把这些自动转换也作为待核验关系，覆盖旧值、当前来源、拟执行效果及revision；区分兼容补充、等价重述、替换和冲突，提交只执行经核验的变化。原日期/历史状态、正确纠错、遗忘与再次授权须保留回归。当前只完成诊断与最小复现，尚未实施该转换计划。


### v5隐式关系计划已实施

8ef8829将存储即将发生的隐式单值比较显式列为候选，由核验模型判断compatible/exclusive/uncertain。同主题或较晚时间不再自动证明替换；来源与事实不变时计划才可在事务内执行。兼容项跳过自动隐藏，互斥项使用现有时序规则；显式操作/替换仍走原核验。没有候选不调用模型，但仍生成指纹计划；64对/64000字符上限直接拒绝，不截断。新v5格式禁止混用旧灌入。

该实现防止了已复现的咖啡细节自动丢失，但二元关系不提供主偏好与背景日常的展示层次。真实HTTP更新后，存储已正确保留拿铁并隐藏旧饮品，Answer仍将通用日常当作冲突。后续须验证关系证据和打包策略能解释兼容含义，不能直接删除真实日常或改变Answer来掩盖问题。
