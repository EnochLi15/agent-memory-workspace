# 实施状态

更新于 2026-09-05。工程主体已实现；公开保留集与完整消融仍在运行，尚未完成最终交付验收。

- [x] 三个独立 Git 仓库、精确 submodule、权威静态契约及仅三个运行接口
- [x] mem0 源码来源、许可、完整原版快照和 U1 方法体等价证明
- [x] 同步 SQLite 事务、隔离、来源校验、更新/纠错/冲突/遗忘/恢复及依赖传播
- [x] 候选联合、时序视图、关系扩展、证据预算、受限重排、本地向量及离线回退
- [x] 37 项服务行为测试、8 项Node评测测试、2项Python统计回归、2 项对照适配器并发/幂等测试
- [x] 最新运行候选的 11 类增强模式 HTTP 合约检查
- [x] 全新卷、全断网冷启动、容器重建、持久恢复及事务中途 SIGKILL
- [x] 独立 service/eval Docker 镜像；合成端到端 smoke 4/4
- [x] 固定公开数据、分组互斥划分、唯一问题实例、上游 refined Judge 和 MemOps 诊断脚本
- [x] 5000 事实/16 并发在实际资源争用环境下完成；时延条件详见 performance-v6-contended.json
- [x] 最新功能提交已完成干净递归克隆复验：37项服务、8项Node评测、2项Python统计和2项基线保护测试；U0/U1等价、独立容器contract与smoke 4/4，镜像内断网Python统计测试通过
- [ ] 最终离线交付包、完整实验摘要及文件校验和
- [x] LoCoMo 500题全部判定：156/500，无运行错误；8对话分组区间约20.6%–36.1%
- [ ] MemOps 500题完整公开保留集
- [ ] 全部 U0–U3、B0–B6、逐项消融、错误清单、使用量与最终报告
- [ ] 并行评测结束后，补充无其他实验负载的性能测量

## 当前运行与冻结边界

生产运行源码冻结在 service `910b3dd`。之后的 service 提交只修改/增加测试、U0/U1 对照 HTTP 适配器与可选性能测量脚本，生产 `src` Git tree 完全一致；见 `reports/runtime-equivalence-v6.json`。最终保留集为 `holdout-v2-U3-*`，完整候选开发对照为 `dev-v6`，修正重试行为后的原版/机械拆分对照为 `baseline-v7`。

独立的合成容器 smoke 发现同一句更新与遗忘混合时，离线提取错误丢弃剩余子句。修复覆盖同 chunk 新事实撤销和推断依赖可见性；没有依据保留集答案调参。旧 holdout-v1 被明确标记为 superseded，保留原始产物。

干净克隆第一次验证中，80ms 请求超时测试把 Fastify 首次路由启动算入 250ms 的请求计时断言；服务仍返回预期 503。测试显式等待 ready 后，隔离重测及 35+8 全套测试通过，运行代码未改变。初次失败日志保留，不把该次克隆直接声明为全项通过。

U0/U1 原版适配器此前未合并执行中的相同 request_id，超时重试产生并发重复灌入。现有请求共享同一执行 Promise，按用户顺序灌入，成功后保存进程内回执；上游算法、提示词、方法体均不变。旧 dev-v6 U0/U1 已标记为 superseded，新 baseline-v7 使用全新命名空间。

## 结果使用边界

正式平台 Answer、500+500 选择器与 MemOps 精确二值映射未提供；本项目结果属于明确配置的公开复现，不能当作正式平台成绩。本地 Qwen3:14b 为 Q4_K_M 量化版，embedding 为已安装的 nomic-embed-text 768维，模型 digest 已记录。

早期 dev-v1–v4 的 MemOps 题号重复，不能与修复后的题数或准确率直接比较。修复后的公开归一化数据包含 LoCoMo 10个样本/1382题、MemOps 403个样本/4012个唯一题实例。开发集各30题，保留集各500题；完整对话噪声保留。

远端生成/判分失败保留在计划分母；错误阶段与共同完成判分的子集另行报告。来源 ID 覆盖率只是证据诊断，不等于语义支持率。全局哈希遗忘匹配较保守，不能据有限测试声称任意同值场景都不会误删。

当前完整候选开发集：LoCoMo 17/30（30题已判定），MemOps rubric 代理12/30（29题已判定）；相同 MemOps 保存答案的上游诊断为23/30。判分协议不同，不能合并为同一个成绩。保留集与完整消融尚在执行。

最新补充：实际丢失写入成功响应后的重启重试、options 隔离和8租户并行测试已通过。合成增强问答 acceptance-v1 为8/9，跨 chunk 指代为0/1；对应证据已召回，但固定 Answer 分别未完成相对日期换算与指代解析，失败保留。详见验收矩阵及两个合成报告。

开发集 B1 原文加向量配置已完成：LoCoMo 18/30、MemOps 20/30，均全部判定。当前完整候选分别为17/30和12/30，尚不能声称复杂方案优于简单基线；完整配对与消融报告仍待全部实验结束。
