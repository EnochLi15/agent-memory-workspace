# 固定小集收敛与原文对照

按用户最新要求停止新功能和新实验工具开发。服务冻结于98c12e5，评测保持11da2a6；旧MemOps长回归主动中断，已有16题service_error、14题未执行，LoCoMo已结束且30题service_error。日志、原manifest和历史331/1000均保留。服务主工作树快进候选并构建，只用于新的隔离测试目录，不代表生产验收通过。

## 当前恢复机制的完成边界

1. 既有338项服务测试及真实本地HTTP/SDK断连测试证明：同请求最多3次准备，保留拒绝及已完成响应，成功只提交一次；语义拒绝、永久错误、缓存失效、快照变化和重启均明确停止，不允许离线成功或重新投票。
2. 冻结的2组10题、5次写入，从空库执行写入→检索→回答→判分；候选须5次逻辑写入全部成功、10题全部完成判分、无服务降级。执行中失败不得更换请求ID重抽以补分。
3. 单独核对遗忘内容从可读事实、原文、来源及返回证据中消失；更新后的当前状态正确；同句邻居和不同主体邻居仍保留；引用/条件/否定指令不误删。答案正确不能代替存储检查。
4. 原文B1使用同一数据、相同最终32条/6000估算token上限、相同本地embedding、Answer/Judge模型、提示词及推理重试上限，独立空库。写入模型调用量、实际证据量及耗时另报，不能声称总计算成本相等。
5. 先由当前助手逐条对照原文独立复核，不冒充人工标签。用户暂时无暇审核，独立人工校准保持待办。小集通过仅关闭功能性收敛，不证明真实长历史稳定性或可推广准确率；真实网关若未触发恢复必须明确注明。

## 冻结配置

- 数据：[round2-small-closed-loop-v1.json](../configs/round2-small-closed-loop-v1.json)。由已有smoke/lifecycle合成控制组成，增加固定的遗忘与保留评分条件；不是隐藏集，未接触封存150题。
- 配置：[round2-small-pair-v1.json](../configs/round2-small-pair-v1.json)。只有candidate和raw两个配置；rerank、coverage packing、event view、多跳和反思均关闭。保留必要的生命周期、时间和来源核验。
- 原文B1复用现有rawOnly路径，逐消息原文分块与混合检索，无抽取模型、更新或删除操作；保留删除请求文本，由Answer自行理解。即使回答拒绝泄露，原文仍保留代码也不算存储遗忘通过。
- 复用现有run-experiment、HTTP审计与报告流程，不新增实验执行器。先跑原文，再跑候选，避免模型竞争并尽早得到对照。

## 完成结果

两次运行均从空库完成，service98c12e5、eval11da2a6、workspace0bb9e4b启动时干净，数据及配置在第一次回答前已提交。两边均为5/5次写入、10/10次检索、10/10题判分及答对，HTTP白名单审计通过，无降级、无重试。没有更换失败请求ID补分，也没有扩大数据或重复运行。

| 观测项 | 原文B1 | 冻结候选 |
|---|---:|---:|
| 小集问答 | 10/10 | 10/10 |
| 写入中位耗时 | 51.9毫秒 | 12.33秒 |
| 完整闭环耗时 | 33.05秒 | 107.08秒 |
| 写入阶段生成调用 | 0 | 11 |
| 写入阶段报告的token | 0 | 53,990 |
| 平均返回证据估算token | 478 | 732.5 |
| 遗忘后仍返回验证码的查询 | 5/5 | 0/5 |

最终证据上限同为32条/6000估算token，Answer/Judge的模型、提示词、输出上限和重试一致。候选实际包含5次抽取、5次核验、1次来源删除调用；没有重排或其它附加推理。表中生成token仅为服务模型审计，当前评测器未记录Answer/Judge用量，不能把它当成总成本；串行单次耗时也不代表统计显著的性能差异。

候选20项存储检查通过：验证码从正常可读事实、全部消息、来源、操作及FTS中消失，擦除事实无向量/引文；Seattle和Firefox已superseded，Portland/Safari仍active；Clara、Kevin/Brave、Maya/Chrome、Olivia/Edge以及具名真人Andrew/Boston/painting均保留。两个租户各有且仅有3、2条回执，无待恢复状态。引用、条件和否定没有产生未授权forget事件。这些检查不包含磁盘取证删除或保留原输入的私有实验日志。

原文B1仍把验证码保存在事实、消息和FTS中，并在本组5个查询中全部返回。Answer遵守后来的遗忘文字，没有在最终答案中泄露，所以问答仍10/10。**答对与完成遗忘是两项不同验收条件。**

当前助手在隐藏配置名称及Judge结果后逐条复核20个答案，全部判对，与自动Judge一致。材料保留于`artifacts/round2-small-98c12e5-v1/review/`，含原文、完整返回证据、独立评分理由和揭盲对照；[逐条复核报告](../reports/round2-small-model-review-v1.json)纳入版本记录。由于当前助手参与了数据编写且证据形式可能暴露方案，这只是模型复核，不是外部盲审或人工校准；人工标签仍为0，原199条人工表未改变。

## 收敛决定

- **本次固定小集功能门槛通过。** 恢复实现冻结，不增加功能或工具，不再因这10题继续调参。既有故障注入测试覆盖有界恢复、拒绝保留和单次提交；本次真实网关没有故障，因此不能宣称跨HTTP恢复在真实网关已被验证。长历史恢复可靠性仍开放。
- **未证明问答增益。** 两边均10/10、只有2组合成背景和1次运行，不能给出稳定提升、置信区间结论或替换历史331/1000。
- **关闭附加复杂度。** rerank、coverage packing、event view、多跳和反思继续关闭；保留已观察到必要性的精确遗忘、当前状态更新和邻居保留。当前原文基线不能替代有遗忘要求的记忆服务。
- 扩样和多次重复暂缓。后续若继续恢复验收，只处理已保存的真实中断现场，复用既有工具；人工校准仍待真人参与，不由本次模型复核替代。

完整证据与哈希见[本次结果报告](../reports/round2-small-closed-loop-result-v1.json)。旧长回归保留为30题LoCoMo服务错误、16题MemOps服务错误、14题MemOps主动中断未记录；不得视为新质量成绩。
