# 第二轮实施与验收记录

基线：workspace `6ae63c7`、service `842b130`、eval `8f5be51`。
完整范围见 [第二轮改进方案](06-第二轮改进方案.md)。旧交付包与8088服务保留作对照。

最新服务检查点`32cca16`：119项测试通过；请求内核验复用、显式阶段模型路由及最多两轮补丁实验参数已落地。本轮六次原始前缀尝试全部结束，尚无完整E03/C30通过；最新两轮配置分别为7/8、13/14，失败末段均未提交。详见文末“增量核验、阶段路由与修复预算”。33.1%历史成绩未改变。

| 批次 | 状态 | 完成所需证据 |
|---|---|---|
| E0 | 199条独立审计已完成：194有效、5协议错误；共同有效150条有24条分歧，判据边界待处理 | 独立评分校准集、175分歧及一致样本复核、协议版本与分类误判报告 |
| E1 | 实施中 | 意图/绑定/提交分离、同chunk引用、范围与片段删除、4失败前缀和变体、HTTP/事务/崩溃/隔离/并发检查 |
| E2 | 核心实现与本地验证完成，质量对照待运行 | 安全来源与事实混合召回、各路径删除验证、同预算对照 |
| E3 | 核心实现与确定性验证完成，质量对照待运行 | 候选重排在最终打包前、覆盖选择、阶段来源诊断与逐题对照 |
| E4 | 核心实现与合成专项验证完成，质量对照待运行 | 时间精度与锚点、安全事件投影、时间/操作/轨迹专项验证 |
| E5 | 阶段模型路由与请求内核验复用已实现；同预算质量/成本验收未完成 | 条件扩展与选择性重排、至少3次配对推理、质量/成本/延迟测量 |
| 最终交付 | 待实施 | 新数据曝光审计、版本冻结、干净构建与部署、GitHub推送、独立归档验证和完整报告 |

已看过的旧1000题仅作回归，不能声称为新的未见验证。每个验收项独立记录证据；单元测试通过不等同于自然语言或正式平台成绩达标。

## E1 第一批实现

- 独立指令片段模块保留原始偏移，识别直接遗忘和停止存储表达，区分否定、引用、条件与转述；一段引语不再否定其它句子的直接请求。
- 对识别出的每条遗忘指令检查操作覆盖。空模型结果、只覆盖第一条指令或用整段引用冒充多个指令覆盖时，进入有界修复/确定性恢复；不能解析的操作失败，不返回空操作成功。
- `new:N` 引用由服务生成持久ID，并检查新事实的来源先于操作或依赖事实。同消息中的未来事实也不能通过空ID的属性选择器被提前删除。
- 操作绑定检查主体与范围。同值但不同明确scope的记录不再被全局值匹配连带删除；混合引用不能携带已删除上下文。重复的已删除属性可证明为no-op，未知新值不能借此通过。
- 修复额外发现：提取事实的对象展开曾把临时 `sources` 数组写入数据库，该字段不在 `Fact` 类型中，遗忘时未被清理。现在提取阶段不复制该字段，存储写入也移除它；已遗忘事实清空原始时间表达。新版本须使用新数据目录，旧数据未在原地迁移。
- 评测器将新增语义错误码列为不可按传输故障重试，失败仍留在完整计划分母。

验证：新增10组回归在实现前的首批8组全部失败，修改后服务全部47项测试通过（含真实SIGKILL事务回滚、HTTP、幂等、隔离和重启）；评测器8项Node及2项Python测试通过。日志见 [服务](../reports/round2-E1-service-tests.txt)、[评测器](../reports/round2-eval-tests.txt)。这些是确定性桩和本地HTTP验证，尚不是远程模型质量评测。

尚未完成E1：完整的结构化语义核验与定向修复、候选扩展、4个真实失败前缀复现、来源位置持久化与细粒度片段可见性、并发重排删除的新版本验证。当前规则只覆盖已定义的指令表达，不能宣称任意自然语言删除意图均不丢失。E0、E2–E5及最终交付仍需继续。

源码核查修正：基线Worker在重排开启时已把内部预算放大到最多64条/18000 token，但数量仍受请求top_k约束；Engine最终裁到32条/6000 token。因此下一步要真正分离候选召回与最终打包，不能把预算放大本身算作新改进。

## E2/E3 核心管线与 E0 阶段诊断

- 事实持久化原始来源位置；新增 `passages` 与独立FTS表。按原文句子切片并限制每片900字符，保留说话人、外部来源ID和UTF-16位置。原文与事实一起调用本地embedding，分路语义/词法召回；原文不是摘要或最终答案。
- 遗忘事务按照来源区间裁去对应内容，同时清理向量与FTS。片段裁剪后不沿用旧向量，剩余独立细节仍可词法召回。未提取的旧值重放也受标记检查；保留事实、原文片段和依赖可见性一起过滤。原文引用不能通过历史查询恢复已遗忘值。
- 新格式明确标记为 `dual-source-v1` 或 `facts-only-v1`；切换表示需要新目录和重新灌入，删除-only请求也不能绕过检查。不会为每次写入重建未变化原文的FTS。
- `collectCandidates → compactCandidates → packEvidence` 已分离。内部候选池默认200，重排最多80，均不受请求top_k限制；只在最后扩展来源上下文并执行返回数量/token预算。列表/轨迹选择偏好新条目和新来源，公开score仍降序。模型给0分或未选择的候选不再挤占最终预算。
- Worker在一个任务内检查版本、重新召回并打包；重排期间发生修改时，丢弃过时排序，按新版本可见性返回。没有增加HTTP路由、请求gold字段或最终答案生成逻辑。
- 可选 `MEMORY_RETRIEVAL_AUDIT` 输出各路ID、候选池、重排选择、预算/数量淘汰和最终选择。查询、租户、外部来源ID只记录hash，不记录证据正文。独立 `eval/python/diagnose_retrieval_stages.py` 再与gold来源ID比较；没有标注、多条同键trace或trace缺失单列，不假装召回失败。诊断仍只是来源ID丢失，不是语义蕴含或生命周期过滤错误的证明。
- 补强重复删除测试后，发现原操作提示事实可能被误当作被删除属性。现排除该候选，重复删除还要匹配已有删除标记及边界，不能以“某目标已删除”证明任意新值请求已经完成。

验证证据：

- [服务56项测试](../reports/round2-E2-E3-service-tests.txt)：原37项、前批10项和本批9项；包含选择第32位之外候选、重排中并发删除、同句裁剪保留未提取细节、长消息、纯语义原文召回、状态/纠错过滤、格式切换拒绝；真实SIGKILL检查扩展到了原文表与FTS。
- [评测器8项Node＋4项Python](../reports/round2-stage-eval-tests.txt)：新增阶段丢失归因及缺失/歧义处理检查。
- [实际本地embedding探针](../reports/round2-source-index-local-embedding.json)：固定模型digest，真实768维向量、语义召回、删除/FTS、旧向量失效、独立细节重启保留及外部来源ID保留6项通过。提取器使用确定性fixture，不是远程LLM端到端质量评测。

新增参数：`MEMORY_SOURCE_INDEX`（默认true）、`MEMORY_CANDIDATE_LIMIT`（200）、`MEMORY_RERANK_CANDIDATES`（80）、`MEMORY_COVERAGE_PACKING`（true）；终端预算仍由原参数控制。`MEMORY_RAW_FALLBACK=false`关闭原文召回，可在同表示灌入上做检索消融；关闭SOURCE_INDEX则改变写入表示，需要重新灌入。

仍待验证/实现：E2/E3的固定Answer/Judge逐题对照及至少3次重复；原文中未形成事实的目标如何通过语义解析安全绑定（当前无法绑定时明确失败）；E1完整语义核验、候选扩展和4个原始失败前缀；E0人工/独立评分校准；E4时间与完整事件视图；E5条件扩展/重排和成本；新数据曝光审计、部署与归档交付。56项测试不能替代这些验收项。

## E4 时间与事件核心实现

- 按消息解析并跨chunk保留明确会话日期；普通时间戳随新消息推进。事件日期与消息观察日期分开，支持明确日/月/年和部分相对时间、周区间；月份不补造具体日，合成排序时间不能作为相对日期锚点。歧义范围、多个锚点或不支持的表达保留未解析。
- 新格式升至 `dual-source-v2` / `facts-only-v2`，必须新目录重新灌入。单值状态比较精度区间；月份内的未知更新边界不强选某个状态。过去事件、将来计划、错误纠正与真实历史分开处理。
- 在原子事务中记录 remember/update/correct/retract/forget/restore/reflection 事件，持久化只存闭集类别、顺序、来源和状态ID，不复制旧值。查询按现有状态与依赖可见性动态投影，删除后事件不携带已忘值；操作发生时间不继承其目标所描述的历史日期。`MEMORY_EVENT_VIEW` 控制事件召回。
- 修复 `Correction:`、`Remember again:` 等指令前缀被当成人名的问题。重新记忆必须绑定实际用户属性与删除标记，不能靠错误主体绕过授权。
- 时间证据单独显示事件日期或月份，保留原文供核验；重排紧凑输入同样带时间说明。普通Answer提示词保持原哈希。

验证及限制：

- [服务66项测试](../reports/round2-E4-service-tests.txt)通过，包括时间锚点/精度、跨chunk、计划、事件生命周期、事务和崩溃回滚。另有2项探针判定回归覆盖实际误判及否定请求。
- [首次真实链路](../reports/round2-temporal-live-v1.json)原检查3/5；[定向复核](../reports/round2-temporal-live-v1-review.json)发现拒答检查漏认 `do not know`，同时操作问题确实未答完整。
- [第二次真实链路](../reports/round2-temporal-live-v2.json)原检查4/5；[复核记录](../reports/round2-temporal-live-v2-review.json)确认只有3题符合要求：闰日题把消息日期当事件日期；遗忘题仍说不知道请求，但旧检查器只匹配到 forgotten 误判通过。不能以这次4/5证明修复有效。
- [v3专项验收](../reports/round2-temporal-live.json)：9次真实HTTP写入全部200；远端抽取/重排、本地embedding、固定Answer，5组证据每组连续回答3次，15/15符合新版检查并经逐条查看。三次是对同一组返回证据重复Answer，**不是三次完整灌入/召回重复，也不是5/5基准准确率**。前两次结果完整保留，三版有代码或判定变化，不能直接计算算法提升幅度。

E4尚待：真实时序/过程专项及普通单跳的固定预算配对对照、至少3次完整推理重复、显式操作有效边界与多主体过程视图的进一步验证。有限时间规则和合成用例不证明任意自然语言都已覆盖。生产8088和旧交付包仍为原基线。

## E0 判分复核材料

[选择清单与哈希](../reports/round2-judge-calibration-selection.json)记录独立协议 `memops-judge-audit-selection-v1`。464条保存答案逐一核对与上游输入完全相同；选入全部175条分歧，以及六类题型×正/负一致判定×2条，共199条。所有分层都有可用样本；原36条无答案的失败仍计入500题计划分母。

生成器将不含原判分的材料、原判分对照和待复核表分文件保存，输出目录拒绝覆盖，输入/输出均记录SHA-256。新增2项Python回归验证答案一致、重复ID拒绝、判分隐藏及失败分母。**当前独立复核完成数为0**；这一步只是选样，不是199条已校准，不改变33.1%历史成绩。仍需预先制定逐条件判据、六类控制样例、三个已知反例及199条复核，并报告语义错误与字面泄漏诊断。

## 2026-09-06：E0审计、E1真实失败诊断与曝光审计

### E0：结果可追溯，尚未完成评分校准

- 新增独立于普通Answer/旧Judge的 `semantic-criteria-audit-v2`，逐项检查reference、must_include、must_not_include及harmful_extra。要求答案中可验证的引用，缺失/重复条件、伪造引用和语义歧义分别保留。没有覆盖旧成绩。
- 37个自编控制样例覆盖六类题型、同义改写、遗漏、否定/泄漏和允许历史上下文。v1为36/37，其中一个操作同义表达误拒；修改后v2为37/37，三个已知反例为3/3。这是协议开发控制集，不能当作独立测试集或准确率提升。
- 首轮199条因网关故障有153条传输失败。恢复后仅重试这153条，验证父结果hash、协议、模型及输入身份，已判定/歧义/协议错误均未重采样。新增熔断记录未尝试项并保留完整分母。
- [合并报告](../reports/round2-judge-audit199-v2-merged.json)：153条有效判定（21通过、132不通过），40条协议错误（24条引用不在答案中、16条缺少必需引用），6条语义未决。样本为175分歧加24一致样本，不能以21/199计算全量新准确率。
- [六条未决的助手复核](../reports/round2-judge-uncertain-assistant-review.json)另记4条不通过、1条通过、1条rubric边界仍不明确；没有替换原始输出。助手复核不等于独立人工真值，40条协议错误仍需处理。
- 普通Answer提示词及1800输出预算不变，审计独用6000预算。语义写入拒绝 `EVIDENCE_VALIDATION` 不再被当作HTTP503传输故障反复重采样；受影响问题仍全部计入分母。

### E1：防止错提交，不代表四个失败已恢复

- 把主体/属性/范围守卫前移到提取修复前，并在事务内重复执行；事实上的supersedes也不能绕过属性绑定。模型改写一个无关属性名仍需独立语义核验。
- 新增提交前核验及有界修复：逐操作授权、目标含义、替换关系和逐用户消息覆盖；机器时间/Source id元数据不能授权纠错。覆盖状态明确为not_memorable/represented/missing，represented必须引用具有对应用户来源的提案项。核验格式可修复一次；真实语义拒绝直接进入提案修复，不能重采样核验直到通过。调用共享原请求时间预算。
- 修复上下文包含被拒提案及实际错误目标，并使用结构化JSON与明确的系统级修复指令。规则遗漏报警只作为语义核验提示，避免把泛泛提问强制存成用户事实。
- 补充“No need to track…”等退休表达及其否定/引用边界；离线模式不再把无精确值的experience总桶当作可整属性删除的目标。模型不可用时仍可能离线降级，因此HTTP200不是远程语义路径通过。
- 新探针按原始消息和chunk边界重放到原四个失败点，使用新命名空间、本地真实embedding及远程提取；诊断原始提案仅写入忽略的artifacts，报告保存源码hash。每次结果独立留存。
- [v7四前缀结果](../reports/round2-failure-prefixes-context-v7.json)为0/4通过：D24在第4段属性/范围绑定失败；E03在第6段未知目标；C30在第14段属性/范围绑定失败；D17在第1段未知目标。此前出现HTTP200的降级/错误删除也保留，不能计作成功恢复。
- [D24结构化修复v8](../reports/round2-failure-prefixes-D24-structured-v8.json)仍在第4段失败。`gpt-5.4`隔离对照请求返回400（网关表示该账户不支持此模型）；四段虽200但全部离线降级，不能证明换模型改善。生产配置未切换。

验证：[服务79项测试](../reports/round2-verification-disposition-service-tests.txt)及[评测器14项Node＋9项Python](../reports/round2-criterion-audit-final-eval-tests.txt)通过。新增回归先复现了空覆盖、跨消息假覆盖、泛化删除和语义错误被重试等问题；确定性模型桩测试不代表真实提取质量。当前分支是实验检查点，未替换8088基线。

下一步的必要工作：把意图提取、候选选择与目标解析拆开；用已保存失败前缀验证定向绑定/来源修复，不能继续只依赖整份JSON重生成。仍需逐事实语义支持/模态核验及同chunk多属性删除边界。详见 [目标绑定与定向修复设计](07-目标绑定与定向修复设计.md)。四失败及变体通过前不启动正式新集对照，避免以大量服务错误消耗未见数据。

### 新数据准备

[曝光审计](../reports/round2-exposure-audit-v1.json)扫描303份执行记录：MemOps共403个文件、100个背景组，46组已有记录、54组无记录。任一变体曝光则整背景组排除；未读取候选题目正文。结果仅表示“在给定记录中未发现曝光”，不能排除未记录的人工查看或预训练曝光。尚未选择或评测新题。E2–E5质量对照、三次完整配对重复、正式部署与归档交付仍待完成。历史33.1%保持不变。

### 补充：同代码模型对照与仍未通过的长段写入

- [D24模型对照](../reports/round2-D24-model-comparison.json)核对了两次运行的全部服务源码hash一致。mini失败，gpt-5.5四段均200、8次提取/核验调用均成功，无离线降级；总写入耗时约81秒，mini约36秒。两次都只跑了一遍，提取和核验模型同时变化，不是三次配对实验。
- [D24持久状态复核](../reports/round2-D24-gpt55-state-review.json)确认三条投资计划仍为第2版本的active/tentative事实；开放感/窗户、RAV4压抑感都有用户原文；未产生针对投资记录的纠错。[两条真实检索/固定mini Answer](../reports/round2-D24-gpt55-retrieval-review.json)也分别保留了正确购车理由和投资计划。
- gpt-5.5并未解决全部失败：E03第8段仍因核验超时失败；C30第14段离线降级后HTTP200，但$200旧记录和$250纠正同时active，不能认定生命周期恢复；D17多次触及80秒模型预算。新提示词已澄清“同chunk先描述再遗忘”的临时new:N目标，实际模型开始生成可绑定的平板目标，但完整核验/提交尚未通过。
- 新回归发现，首次语义拒绝后，第二次提取因网络/预算失败曾可能进入离线降级。现在该路径明确拒绝，不以降级绕过核验。最新服务为[80项测试通过](../reports/round2-verification-disposition-service-tests.txt)，真实D17仍未通过，不能因测试增加而宣称修复完成。
- 并行E03/C30启动恰好落在同一毫秒，旧探针按时间命名导致报告/模型调用日志共享目录；两个租户数据库分别保存，但调用级成本不可按目录直接归因。已保留[E03报告快照](../reports/round2-shared-directory-report-snapshot.json)，并改为mkdtemp原子创建唯一目录。这两次运行不作为隔离配对性能证据；D24单独运行的对照不受影响。

这些结果支持先实施定向绑定/来源修复、减少整份提案重复生成，再验证模型路由与成本；不能简单放宽验收或延长赛题接口时限。默认模型和8088基线均未替换。下一阶段仍需重新完成四前缀及变体，之后才冻结新数据评测配置。


## 2026-09-06：定向补丁、纠错提交修复与逐事实核验

### 已落地的服务改动

- `repair.ts` 的补丁只能修改指定事实/操作及受影响来源。删除数组元素后按原始槽位重映射全部 `new:N` 引用；悬空引用拒绝提交，不能悄悄转向下一条记录。语义拒绝定位到单条fact时，同消息内的无关fact也不允许修改。完整结构仍会重新核验，尚未实施增量核验证书。
- 来源部分恢复也通过稳定句柄重映射。旧版本的真实回归复现了“移除无效事实后把薪资删除指向经理”的错误；新测试保证薪资清除且经理保留。
- 新增逐事实 `supported`、`modality_supported` 和声明来源内的真实引文核验。每条事实必须有独立且唯一的判断，消息级represented不能代替。拒绝“计划当已发生”“他人当本人”“否定变肯定”及无关引文支持；返回的语义问题进入局部修复。
- 可显式设置 `MEMORY_LLM_REASONING_EFFORT=low|medium|high`，未设置时沿用提供方默认。内部模型预算由原80秒调整到默认90秒（addTimeout减25秒，最多95秒），外层115秒及赛题120秒限制未变。模型、effort和预算变化均记录，不能将收益全部归为代码。
- [93项服务测试](../reports/round2-fact-verification-service-tests.txt)通过；[六个真实模型核验控制](../reports/round2-fact-verification-model-controls.json)6/6符合预期。它们不是总体准确率或完整长段推理成绩。

### 原始失败重放的实际结论

- [D17状态及问答复核](../reports/round2-D17-targeted-repair-review.json)：gpt-5.5/low、90秒内部预算的一次原始chunk成功；平板及同步信息清除，用户Firefox/Safari与Kevin的Brave保留，两条实际查询符合要求。这次未调用repair，不能归因于局部补丁。该运行早于逐事实核验的新版本。
- [五组真实生命周期变体](../reports/round2-lifecycle-variants-2026-09-06T02-59-13-447Z-WLOTNe.json)全部通过：直接删除、否定、引用、条件和他人转述。检查了主体、浏览器保留及原文可见性；这也是逐事实核验之前的版本，不能直接当作最新版本验收。
- [E03回答阶段复核](../reports/round2-E03-answer-stage-review.json)：八段均HTTP200，Martin/Rebecca/交接/群体场景都进入最终32条证据。原始Answer漏掉Martin与交接；对完全相同证据另问两次均覆盖了这些内容，三次中2次完整。保留原失败，不更改Answer提示词或预算，不把两次重复回答当作三次完整灌入/召回。
- [C30提交错误](../reports/round2-C30-commit-failure-review.json)：十四段虽均200，但旧$200与新$250都被retracted。真实提案明确指向旧$200，数据库却用operation.value中的新$250扩展重复目标，误撤销了新状态。现在只按已选目标的实际旧值、主体、属性和scope扩展。固定模型提案的[完整原chunk重放](../reports/round2-C30-recorded-correction-replay.json)4项通过：旧值撤回、新值active、事件只指向旧值、新金额可检索；此重放没有重新调用语义模型，新的真实长前缀另行运行。
- 加入逐事实核验后，D17的新运行在90秒内仍未完成。首轮核验指出两处来源不足，但同时生成了一个不存在的replacement检查，随后格式修复超时。不能沿用此前D17通过记录宣称当前版本已通过；需要保留已发现语义失败并减少无效的格式往返。

### E0与尚未完成的范围

[gpt-5.5控制集及哈希](../reports/round2-judge-gpt55-controls.json)为40/40，包括37控制与3已知反例；采用冻结的同一逐条件协议。199条独立审计正在运行，与mini结果分别保留；对照脚本验证输入、协议与完整分母，并将分歧及任一侧未决列入复核队列，不选择较高分覆盖旧判定。

完整E1独立目标解析、候选扩展、四前缀及更全变体仍需验收。E2–E5固定预算质量消融、至少三次完整配对、新背景评测、正式部署及归档仍未完成。默认模型、8088基线和33.1%历史成绩均未改变。


### 最新补充：更严格核验暴露的失败与C30真实恢复

- 服务检查点 `576706f` 的C30原始14段真实重放全部200，14次提取、15次核验、1次定向补丁及14次本地embedding均成功。[持久状态与查询复核](../reports/round2-C30-fresh-state-review.json)确认旧$200已retracted，新$250为active，1日/15日安排仍active。固定Answer已正确给出金额、基金和纠错，但省略明确的1日/15日日期，最终回答尚不能记作完整通过。
- 同一检查点的E03在第7段被正确拒绝：修复补回了听力技巧计划，却写成confirmed而非tentative；第二次核验拦下了模态错误。原来的八段HTTP通过不适用于这个更严格版本。
- D17首轮核验中的有效fact拒绝，不应因后来出现格式错误被重判覆盖。新回归先证明原路径会二次判分；现在格式异常携带已确认的拒绝，直接进入提案修复。修复后的提案仍需完整核验。新增明确的替换目标清单，空supersedes不能制造replacement检查；提取提示强调来源必须覆盖结论中的限定条件。
- [94项服务测试](../reports/round2-semantic-protocol-service-tests.txt)通过。最新D17及D24重放运行中；尚未验收，不将之前版本的成功移用为新版本结果。


### 检查点92ae3bd：D17、D24已复核；其余门槛保留

- [D17最新复核](../reports/round2-D17-fact-verified-review.json)：原始chunk在77.1秒完成，实际走过1次提取、1次定向补丁、1次逐事实核验及本地embedding；两个平板目标清空，工作电脑Firefox、手机Safari及Kevin的Brave保留。两条实际问答均符合预期。
- [D24最新复核](../reports/round2-D24-fact-verified-review.json)：原始四段全部成功，投资计划仍为第2版本且tentative；开放感/窗户、RAV4压抑感和AWD非主因均正确，两条实际问答通过。两项均为一次完整前缀诊断，不是三次重复或总体准确率。
- [阶段调用观察](../reports/round2-prefix-call-observations.json)分列成功、失败、报告token与缺失usage；C30逐事实核验版14段总耗时约311秒，之前约265秒。代码及随机生成同时影响结果，不能将差值归因于单一模块，也没有按未知价格估算费用。
- [三条判分分歧助手复核](../reports/round2-assistant-disagreement-review.json)确认一条漏掉明确必要条件，另两条保留reference充分性/隐含过程表达的判据歧义。没有覆盖原始模型判分，独立人工真值仍为0。
- 相同92ae3bd源码的E03/C30新重放及199条gpt-5.5独立审计仍在执行。完成后先检查日志、实际状态和固定Answer，再记录结果；不要重启已有运行或把旧版本通过项拼成一次完整验收。


## 2026-09-06：目标解析前移及独立审计完成

### E1：核验必须看到真正将被修改的目标

- 新增独立的确定性目标解析模块，返回resolved/missing_evidence/ambiguous。属性选择器在提交前转成具体目标ID；同值重复表示与property遗忘范围一并交给语义核验，提交层复用相同选择规则。
- 修复此前“删掉当前值但历史值仍可返回”的property遗忘缺口。以92ae3bd冻结运行时复现旧状态未清除，新版区分property与value边界并保留其他人的记录和其他设备scope。
- 操作找不到目标时，按操作的主体/属性/scope/来源追加最多32条候选，不再受整段对话前120条候选的遮挡。新候选仅进入修复输入，仍需模型明确选择和独立核验。
- [99项服务测试](../reports/round2-binding-service-tests.txt)通过，新增检查覆盖实际目标进入核验、重复表示与主体/设备隔离、120条之外目标修复、property与value历史边界。[真实模型HTTP验证](../reports/round2-binding-lifecycle-live.json)两组均通过，[原文与状态复核](../reports/round2-binding-lifecycle-review.json)确认property清除Seattle/Boston，value只清除Boston并保留Seattle历史。
- [原92ae3bd长前缀重放](../reports/round2-long-prefix-verification-failures.json)仍失败：E03第7段补回计划却标成confirmed；C30第14段修复一个不受该引用支持的content限定语后，全量二次核验超时。事务未提交失败段。新目标模块不等于已解决这两个问题。

### E0：完整199条执行结束，但不能直接重写成绩

- [gpt-5.5完整独立审计](../reports/round2-judge-audit199-gpt55-v2.json)：194条有效（32通过、162不通过），5条协议错误，无传输失败。40条控制样例保持此前40/40记录。
- [两模型配对对照](../reports/round2-judge-independent-comparison-v1.json)验证原始输入、模型之外的协议和完整199分母：共同有效150条，其中126一致、24分歧；任一侧未决或分歧的73条仍单列，不取较高分。
- [5条协议错误的助手语义复核](../reports/round2-gpt55-protocol-error-review.json)均发现缺项，不覆盖原始审计。其中3条因大小写/Markdown引文不逐字一致；另2条暴露判据协议问题：harmful_extra本身是“遗漏某事”，此类违反无法提供被遗漏内容的答案引文。后续须显式区分存在型与遗漏型证据，另立协议版本与控制集，不修改冻结v2结果来取好分。

剩余：请求内核验记录复用与局部失效传播；独立语义目标分类及向量/原文目标扩展；四前缀完整重放、全部变体和三次配对；E0判据边界、E2–E5新背景质量/成本消融、部署与归档。完整目标保持进行中，33.1%历史成绩和8088基线未变。

## 2026-09-06：增量核验、阶段路由与修复预算

### 已完成的实现与确定性证据

- `5355177`：每次`prepare`拥有私有核验记录，绑定请求/租户/模型/协议及事实、来源、目标和传递依赖。只复用通过记录；修改、追加、删除、重编号及依赖变更触发失效。合并后仍校验完整覆盖、唯一性和真实来源。关闭正向复用也不会解锁未修改的失败项。
- [112项测试](../reports/round2-incremental-verification-service-tests.txt)覆盖请求/租户隔离、传递依赖、伪复用、缺项、格式错误和真实Extractor调用链。[原C30离线机制重放](../reports/round2-recorded-C30-verification-scope.json)将36项检查缩为6项，保留30项；fact0的变化仍传播到纠错事实、操作、替换关系和两条消息。这不是实时模型延迟测量。
- 补丁新增事实必须显式给出modality；局部编辑保留未修改字段，不能因省略字段把计划变成confirmed。原缺省确认问题有先失败后通过的回归。
- `3a5b2cb`：增加extraction/verification/repair独立模型配置。结构错误的整份重提取按repair路由；核验格式修复仍用verification模型。审计记录实际阶段与实际模型，核验记录绑定实际核验模型；不支持的模型不会触发其它模型的隐式替换。[116项测试](../reports/round2-stage-routing-service-tests.txt)通过。
- `32cca16`：目标范围与独立来源错误合并反馈。`MEMORY_MAX_REPAIR_ROUNDS`默认1，可显式设2；所有补丁共用90秒默认模型预算，外层115秒不变。新增回归覆盖第二次遗漏修复、缓存传播及无变化失败不会重判，[119项测试](../reports/round2-repair-budget-service-tests.txt)通过。

### 六次真实前缀尝试的完整结果

均为新目录、新灌入、原始消息与chunk边界；本地embedding保持相同digest。gpt-5.5使用low，内部预算90秒。各组只有一次，生成内容和路径会变化；不能作为三次配对实验或只按较好结果选分。

| 配置 | E03 | C30 | 结论 |
|---|---|---|---|
| `5355177`，统一gpt-5.5，一轮补丁 | 7/8，第8段失败 | 13/14，第14段失败 | E03第7段确有21项复用，二次核验9.9秒；末段首次生成/核验仍耗尽预算 |
| `3a5b2cb`，mini提取、5.5核验/修复，一轮补丁 | 6/8，第7段失败 | 0/14，第1段失败 | 更快生成仍有来源/覆盖缺失；结构修复或新增遗漏耗掉唯一补丁机会 |
| `32cca16`，同一模型路由，两轮补丁 | 7/8，第8段失败 | 13/14，第14段失败 | E03第7段实际两轮修复成功，末次重查2项、复用17项；两者末段仍耗尽预算 |

完整阶段调用、原始结果哈希及失败原因分别见[增量核验复核](../reports/round2-incremental-verification-live-review.json)、[模型路由复核](../reports/round2-stage-routing-live-review.json)、[两轮补丁复核](../reports/round2-two-repairs-live-review.json)。所有失败保留，未用重跑覆盖旧记录；失败末段均没有提交，也没有对不完整前缀生成“恢复后回答”来宣称通过。

最新E03末段：提取38.4秒、结构修复5.5秒、首轮核验41.3秒，剩余4.7秒不足以补回情绪/职业语境及修复模态。最新C30末段：提取30.2秒、结构修复5.0秒、首轮核验42.5秒，剩余12.3秒不足以完成多项来源与具体旅行/转账遗漏修复。这说明有界补丁和复用已生效，但不足以完成当前首轮工作量。

### 后续工作保持完整

优先验证紧凑核验输出：用受约束的来源/目标引用编号减少重复字段、长ID和引文生成；保留完整输入、逐事实含义/模态、授权、替换和消息覆盖检查。必须先锁定格式版本与独立控制，再验证错误编号、来源角色、缺项、格式错误中的已有拒绝及缓存失效；不能通过减少必检内容或放宽时限取得通过。

覆盖标准中的边界另行校准；本轮失败还包括真实遗漏，不能把所有拒绝都归为核验过严。独立目标语义分类、向量/原文目标扩展、四前缀及变体在同一最新配置上的验收、至少三次完整配对仍未完成。E2/E3固定预算质量对照可以在冻结实验配置上并行开展，失败请求保留在分母中；E0判据协议、新背景评测、E4/E5质量成本实验及最终部署/归档仍属于完整目标。

三个服务提交均已推送私密仓库。生产8088健康、基线未替换；本轮未重跑总体评测，33.1%仍仅是历史成绩。
