# 第二轮实施与验收记录

基线：workspace `6ae63c7`、service `842b130`、eval `8f5be51`。
完整范围见 [第二轮改进方案](06-第二轮改进方案.md)。旧交付包与8088服务保留作对照。

最新服务检查点`f12b2b4`：v10独立原文覆盖与索引提交检查已实现，262项服务测试通过。真实覆盖/实体控制首轮7/8，补充实体核验后12/12，最终新目录HTTP9/9；B05原文优先快照仍在90秒内失败，不能宣称吞吐或质量提升。详见[原文覆盖与长请求诊断](12-原文覆盖与长请求诊断.md)。

上一服务检查点`6771326`：普通操作前置分流与v9提交覆盖检查已实现，255项服务测试通过。最终真实分流8/8、新目录HTTP7/7、31.4万字符长历史删除6/6通过；E10失败快照两次准备成功，B05仍失败，完整质量门槛未通过。见[分流设计与结果](11-普通操作分流与写入检查.md)。8088未更新。

上一服务检查点`0ad738e`：E5条件关系扩展、选择性重排和整数索引协议已实现，247项测试通过。合成HTTP控制中重排调用6→2，必要回答均通过检查；不能据此宣称基准质量提升。冻结91782c6五背景仍在运行，E10已在第26段写入失败，其余背景继续；不再将该轮列为完整通过候选。详见[查询策略方案](10-查询策略与重排协议.md)与文末。

上一写入检查点`91782c6`：236项服务测试通过；v8完整历史分批、跨批全局目标与逐单元闭包已实现。31.4万字符合成长历史的真实删除准备/提交通过6/6，准备耗时55.95秒；保留语义真实核验控制3/3通过。v6 ee66153完整E10已结束，50会话/52个写入请求最终均成功，6题答对4题；它是单个已曝光开发背景，不是v8或总体新准确率。完整五背景、新数据和三次配对重复仍未通过；历史331/1000与8088不变。详见文末。

上一检查点9726881的159项测试与B05定向补丁/副本提交证据保留；多轮修复范围问题已修复，B05从头抽取仍有失败，原文独立目标绑定等完整范围仍未完成。

此前v4原文范围删除：E10_reflect在1f77f5a新目录原失败前缀26/26通过；862399d四步HTTP生命周期通过。完整开发集旧d8e264b已结束：6/30（20%），21题因三个背景写入失败保留service_error；不是新版本成绩。E03、B05完整重跑、隐式状态覆盖、正式新集及三个完整配对仍未完成。

旧`441cc48`的30题开发集已结束：150次add成功、4次失败，25题service_error，5题完成判分、2题正确，完整分母为2/30。该结果是已曝光开发集诊断，不能替换历史331/1000，也不是最新版本成绩。完整结果及失败原因见[开发集最终报告](../reports/round2-quality-dev-441cc48-r1-final.json)。

| 批次 | 状态 | 完成所需证据 |
|---|---|---|
| E0 | v2审计194有效/5协议错误；v3控制47/48、追加10/11，旧5条协议错误均可判分，人称歧义同输入结论波动；尚未扩大199条v3审计 | 独立评分校准集、175分歧及一致样本复核、协议版本与分类误判报告 |
| E1 | 实施中 | 意图/绑定/提交分离、同chunk引用、范围与片段删除、4失败前缀和变体、HTTP/事务/崩溃/隔离/并发检查 |
| E2 | 核心实现与本地验证完成；固定预算开发集首轮结束、写入失败阻止配对复用 | 安全来源与事实混合召回、各路径删除验证、同预算对照 |
| E3 | 核心实现与确定性验证完成，质量对照待运行 | 候选重排在最终打包前、覆盖选择、阶段来源诊断与逐题对照 |
| E4 | 核心实现与合成专项验证完成，质量对照待运行 | 时间精度与锚点、安全事件投影、时间/操作/轨迹专项验证 |
| E5 | 阶段路由、核验复用、条件关系扩展、选择性/紧凑重排已实现；完整质量/成本验收未完成 | 条件扩展与选择性重排、至少3次配对推理、质量/成本/延迟测量 |
| 最终交付 | 待实施 | 新数据曝光审计、版本冻结、干净构建与部署、GitHub推送、独立归档验证和完整报告 |

已看过的旧1000题仅作回归，不能声称为新的未见验证。每个验收项独立记录证据；单元测试通过不等同于自然语言或正式平台成绩达标。

## E1 第一批实现

- 独立指令片段模块保留原始偏移，识别直接遗忘和停止存储表达，区分否定、引用、条件与转述；一段引语不再否定其它句子的直接请求。
- 对识别出的每条遗忘指令检查操作覆盖。空模型结果、只覆盖第一条指令或用整段引用冒充多个指令覆盖时，进入有界修复/确定性恢复；不能解析的操作失败，不返回空操作成功。
- `new:N` 引用由服务生成持久ID，并检查新事实的来源先于操作或依赖事实。同消息中的未来事实也不能通过空ID的属性选择器被提前删除。
- 操作绑定检查主体与范围。同值但不同明确scope的记录不再被全局值匹配连带删除；混合引用不能携带已删除上下文。重复的已删除属性可证明为no-op，未知新值不能借此通过。
- 修复额外发现：提取事实的对象展开曾把临时 `sources` 数组写入数据库，该字段不在 `Fact` 类型中，遗忘时未被清理。现在提取阶段不复制该字段，存储写入也移除它；已遗忘事实清空原始时间表达。新版本须使用新数据目录，旧数据未在原地迁移。
- 评测器将新增语义错误码列为不可按传输故障重试，失败仍留在完整计划分母。

验证：新增10组回归在实现前的首批8组全部失败，修改后服务全部47项测试通过（含真实SIGKILL事务回滚、HTTP、幂等、隔离和重启）；评测器8项Node及2项Python测试通过。日志见 [服务](../reports/round2-E1-service-tests.txt)、[评测器](../reports/round2-eval-tests.txt)。这些是确定性桩和本地HTTP验证，尚不是远程模型质量评测。

尚未完成E1：完整的结构化语义核验与定向修复、候选扩展、4个真实失败前缀复现、来源位置持久化与细粒度片段可见性、并发重排删除的新版本验证。当前规则只覆盖已定义的指令表达，不能宣称任意自然语言删除意图均不丢失。E0、E2–E5及最终交付仍需继续。

源码核查修正：基线Worker在重排开启时已把内部预算放大到最多64条/18000 token，但数量仍受请求top_k约束；Engine最终裁到32条/6000 token。因此下一步要真正分离候选召回与最终打包，不能把预算放大本身算作新改进。

## E2/E3 核心管线与 E0 阶段诊断

- 事实持久化原始来源位置；新增 `passages` 与独立FTS表。按原文句子切片并限制每片900字符，保留说话人、外部来源ID和UTF-16位置。原文与事实一起调用本地embedding，分路语义/词法召回；原文不是摘要或最终答案。
- 遗忘事务按照来源区间裁去对应内容，同时清理向量与FTS。片段裁剪后不沿用旧向量，剩余独立细节仍可词法召回。未提取的旧值重放也受标记检查；保留事实、原文片段和依赖可见性一起过滤。原文引用不能通过历史查询恢复已遗忘值。
- 新格式明确标记为 `dual-source-v1` 或 `facts-only-v1`；切换表示需要新目录和重新灌入，删除-only请求也不能绕过检查。不会为每次写入重建未变化原文的FTS。
- `collectCandidates → compactCandidates → packEvidence` 已分离。内部候选池默认200，重排最多80，均不受请求top_k限制；只在最后扩展来源上下文并执行返回数量/token预算。列表/轨迹选择偏好新条目和新来源，公开score仍降序。模型给0分或未选择的候选不再挤占最终预算。
- Worker在一个任务内检查版本、重新召回并打包；重排期间发生修改时，丢弃过时排序，按新版本可见性返回。没有增加HTTP路由、请求gold字段或最终答案生成逻辑。
- 可选 `MEMORY_RETRIEVAL_AUDIT` 输出各路ID、候选池、重排选择、预算/数量淘汰和最终选择。查询、租户、外部来源ID只记录hash，不记录证据正文。独立 `eval/python/diagnose_retrieval_stages.py` 再与gold来源ID比较；没有标注、多条同键trace或trace缺失单列，不假装召回失败。诊断仍只是来源ID丢失，不是语义蕴含或生命周期过滤错误的证明。
- 补强重复删除测试后，发现原操作提示事实可能被误当作被删除属性。现排除该候选，重复删除还要匹配已有删除标记及边界，不能以“某目标已删除”证明任意新值请求已经完成。

验证证据：

- [服务56项测试](../reports/round2-E2-E3-service-tests.txt)：原37项、前批10项和本批9项；包含选择第32位之外候选、重排中并发删除、同句裁剪保留未提取细节、长消息、纯语义原文召回、状态/纠错过滤、格式切换拒绝；真实SIGKILL检查扩展到了原文表与FTS。
- [评测器8项Node＋4项Python](../reports/round2-stage-eval-tests.txt)：新增阶段丢失归因及缺失/歧义处理检查。
- [实际本地embedding探针](../reports/round2-source-index-local-embedding.json)：固定模型digest，真实768维向量、语义召回、删除/FTS、旧向量失效、独立细节重启保留及外部来源ID保留6项通过。提取器使用确定性fixture，不是远程LLM端到端质量评测。

新增参数：`MEMORY_SOURCE_INDEX`（默认true）、`MEMORY_CANDIDATE_LIMIT`（200）、`MEMORY_RERANK_CANDIDATES`（80）、`MEMORY_COVERAGE_PACKING`（true）；终端预算仍由原参数控制。`MEMORY_RAW_FALLBACK=false`关闭原文召回，可在同表示灌入上做检索消融；关闭SOURCE_INDEX则改变写入表示，需要重新灌入。

仍待验证/实现：E2/E3的固定Answer/Judge逐题对照及至少3次重复；原文中未形成事实的目标如何通过语义解析安全绑定（当前无法绑定时明确失败）；E1完整语义核验、候选扩展和4个原始失败前缀；E0人工/独立评分校准；E4时间与完整事件视图；E5条件扩展/重排和成本；新数据曝光审计、部署与归档交付。56项测试不能替代这些验收项。

## E4 时间与事件核心实现

- 按消息解析并跨chunk保留明确会话日期；普通时间戳随新消息推进。事件日期与消息观察日期分开，支持明确日/月/年和部分相对时间、周区间；月份不补造具体日，合成排序时间不能作为相对日期锚点。歧义范围、多个锚点或不支持的表达保留未解析。
- 新格式升至 `dual-source-v2` / `facts-only-v2`，必须新目录重新灌入。单值状态比较精度区间；月份内的未知更新边界不强选某个状态。过去事件、将来计划、错误纠正与真实历史分开处理。
- 在原子事务中记录 remember/update/correct/retract/forget/restore/reflection 事件，持久化只存闭集类别、顺序、来源和状态ID，不复制旧值。查询按现有状态与依赖可见性动态投影，删除后事件不携带已忘值；操作发生时间不继承其目标所描述的历史日期。`MEMORY_EVENT_VIEW` 控制事件召回。
- 修复 `Correction:`、`Remember again:` 等指令前缀被当成人名的问题。重新记忆必须绑定实际用户属性与删除标记，不能靠错误主体绕过授权。
- 时间证据单独显示事件日期或月份，保留原文供核验；重排紧凑输入同样带时间说明。普通Answer提示词保持原哈希。

验证及限制：

- [服务66项测试](../reports/round2-E4-service-tests.txt)通过，包括时间锚点/精度、跨chunk、计划、事件生命周期、事务和崩溃回滚。另有2项探针判定回归覆盖实际误判及否定请求。
- [首次真实链路](../reports/round2-temporal-live-v1.json)原检查3/5；[定向复核](../reports/round2-temporal-live-v1-review.json)发现拒答检查漏认 `do not know`，同时操作问题确实未答完整。
- [第二次真实链路](../reports/round2-temporal-live-v2.json)原检查4/5；[复核记录](../reports/round2-temporal-live-v2-review.json)确认只有3题符合要求：闰日题把消息日期当事件日期；遗忘题仍说不知道请求，但旧检查器只匹配到 forgotten 误判通过。不能以这次4/5证明修复有效。
- [v3专项验收](../reports/round2-temporal-live.json)：9次真实HTTP写入全部200；远端抽取/重排、本地embedding、固定Answer，5组证据每组连续回答3次，15/15符合新版检查并经逐条查看。三次是对同一组返回证据重复Answer，**不是三次完整灌入/召回重复，也不是5/5基准准确率**。前两次结果完整保留，三版有代码或判定变化，不能直接计算算法提升幅度。

E4尚待：真实时序/过程专项及普通单跳的固定预算配对对照、至少3次完整推理重复、显式操作有效边界与多主体过程视图的进一步验证。有限时间规则和合成用例不证明任意自然语言都已覆盖。生产8088和旧交付包仍为原基线。

## E0 判分复核材料

[选择清单与哈希](../reports/round2-judge-calibration-selection.json)记录独立协议 `memops-judge-audit-selection-v1`。464条保存答案逐一核对与上游输入完全相同；选入全部175条分歧，以及六类题型×正/负一致判定×2条，共199条。所有分层都有可用样本；原36条无答案的失败仍计入500题计划分母。

生成器将不含原判分的材料、原判分对照和待复核表分文件保存，输出目录拒绝覆盖，输入/输出均记录SHA-256。新增2项Python回归验证答案一致、重复ID拒绝、判分隐藏及失败分母。**当前独立复核完成数为0**；这一步只是选样，不是199条已校准，不改变33.1%历史成绩。仍需预先制定逐条件判据、六类控制样例、三个已知反例及199条复核，并报告语义错误与字面泄漏诊断。

## 2026-09-06：E0审计、E1真实失败诊断与曝光审计

### E0：结果可追溯，尚未完成评分校准

- 新增独立于普通Answer/旧Judge的 `semantic-criteria-audit-v2`，逐项检查reference、must_include、must_not_include及harmful_extra。要求答案中可验证的引用，缺失/重复条件、伪造引用和语义歧义分别保留。没有覆盖旧成绩。
- 37个自编控制样例覆盖六类题型、同义改写、遗漏、否定/泄漏和允许历史上下文。v1为36/37，其中一个操作同义表达误拒；修改后v2为37/37，三个已知反例为3/3。这是协议开发控制集，不能当作独立测试集或准确率提升。
- 首轮199条因网关故障有153条传输失败。恢复后仅重试这153条，验证父结果hash、协议、模型及输入身份，已判定/歧义/协议错误均未重采样。新增熔断记录未尝试项并保留完整分母。
- [合并报告](../reports/round2-judge-audit199-v2-merged.json)：153条有效判定（21通过、132不通过），40条协议错误（24条引用不在答案中、16条缺少必需引用），6条语义未决。样本为175分歧加24一致样本，不能以21/199计算全量新准确率。
- [六条未决的助手复核](../reports/round2-judge-uncertain-assistant-review.json)另记4条不通过、1条通过、1条rubric边界仍不明确；没有替换原始输出。助手复核不等于独立人工真值，40条协议错误仍需处理。
- 普通Answer提示词及1800输出预算不变，审计独用6000预算。语义写入拒绝 `EVIDENCE_VALIDATION` 不再被当作HTTP503传输故障反复重采样；受影响问题仍全部计入分母。

### E1：防止错提交，不代表四个失败已恢复

- 把主体/属性/范围守卫前移到提取修复前，并在事务内重复执行；事实上的supersedes也不能绕过属性绑定。模型改写一个无关属性名仍需独立语义核验。
- 新增提交前核验及有界修复：逐操作授权、目标含义、替换关系和逐用户消息覆盖；机器时间/Source id元数据不能授权纠错。覆盖状态明确为not_memorable/represented/missing，represented必须引用具有对应用户来源的提案项。核验格式可修复一次；真实语义拒绝直接进入提案修复，不能重采样核验直到通过。调用共享原请求时间预算。
- 修复上下文包含被拒提案及实际错误目标，并使用结构化JSON与明确的系统级修复指令。规则遗漏报警只作为语义核验提示，避免把泛泛提问强制存成用户事实。
- 补充“No need to track…”等退休表达及其否定/引用边界；离线模式不再把无精确值的experience总桶当作可整属性删除的目标。模型不可用时仍可能离线降级，因此HTTP200不是远程语义路径通过。
- 新探针按原始消息和chunk边界重放到原四个失败点，使用新命名空间、本地真实embedding及远程提取；诊断原始提案仅写入忽略的artifacts，报告保存源码hash。每次结果独立留存。
- [v7四前缀结果](../reports/round2-failure-prefixes-context-v7.json)为0/4通过：D24在第4段属性/范围绑定失败；E03在第6段未知目标；C30在第14段属性/范围绑定失败；D17在第1段未知目标。此前出现HTTP200的降级/错误删除也保留，不能计作成功恢复。
- [D24结构化修复v8](../reports/round2-failure-prefixes-D24-structured-v8.json)仍在第4段失败。`gpt-5.4`隔离对照请求返回400（网关表示该账户不支持此模型）；四段虽200但全部离线降级，不能证明换模型改善。生产配置未切换。

验证：[服务79项测试](../reports/round2-verification-disposition-service-tests.txt)及[评测器14项Node＋9项Python](../reports/round2-criterion-audit-final-eval-tests.txt)通过。新增回归先复现了空覆盖、跨消息假覆盖、泛化删除和语义错误被重试等问题；确定性模型桩测试不代表真实提取质量。当前分支是实验检查点，未替换8088基线。

下一步的必要工作：把意图提取、候选选择与目标解析拆开；用已保存失败前缀验证定向绑定/来源修复，不能继续只依赖整份JSON重生成。仍需逐事实语义支持/模态核验及同chunk多属性删除边界。详见 [目标绑定与定向修复设计](07-目标绑定与定向修复设计.md)。四失败及变体通过前不启动正式新集对照，避免以大量服务错误消耗未见数据。

### 新数据准备

[曝光审计](../reports/round2-exposure-audit-v1.json)扫描303份执行记录：MemOps共403个文件、100个背景组，46组已有记录、54组无记录。任一变体曝光则整背景组排除；未读取候选题目正文。结果仅表示“在给定记录中未发现曝光”，不能排除未记录的人工查看或预训练曝光。尚未选择或评测新题。E2–E5质量对照、三次完整配对重复、正式部署与归档交付仍待完成。历史33.1%保持不变。

### 补充：同代码模型对照与仍未通过的长段写入

- [D24模型对照](../reports/round2-D24-model-comparison.json)核对了两次运行的全部服务源码hash一致。mini失败，gpt-5.5四段均200、8次提取/核验调用均成功，无离线降级；总写入耗时约81秒，mini约36秒。两次都只跑了一遍，提取和核验模型同时变化，不是三次配对实验。
- [D24持久状态复核](../reports/round2-D24-gpt55-state-review.json)确认三条投资计划仍为第2版本的active/tentative事实；开放感/窗户、RAV4压抑感都有用户原文；未产生针对投资记录的纠错。[两条真实检索/固定mini Answer](../reports/round2-D24-gpt55-retrieval-review.json)也分别保留了正确购车理由和投资计划。
- gpt-5.5并未解决全部失败：E03第8段仍因核验超时失败；C30第14段离线降级后HTTP200，但$200旧记录和$250纠正同时active，不能认定生命周期恢复；D17多次触及80秒模型预算。新提示词已澄清“同chunk先描述再遗忘”的临时new:N目标，实际模型开始生成可绑定的平板目标，但完整核验/提交尚未通过。
- 新回归发现，首次语义拒绝后，第二次提取因网络/预算失败曾可能进入离线降级。现在该路径明确拒绝，不以降级绕过核验。最新服务为[80项测试通过](../reports/round2-verification-disposition-service-tests.txt)，真实D17仍未通过，不能因测试增加而宣称修复完成。
- 并行E03/C30启动恰好落在同一毫秒，旧探针按时间命名导致报告/模型调用日志共享目录；两个租户数据库分别保存，但调用级成本不可按目录直接归因。已保留[E03报告快照](../reports/round2-shared-directory-report-snapshot.json)，并改为mkdtemp原子创建唯一目录。这两次运行不作为隔离配对性能证据；D24单独运行的对照不受影响。

这些结果支持先实施定向绑定/来源修复、减少整份提案重复生成，再验证模型路由与成本；不能简单放宽验收或延长赛题接口时限。默认模型和8088基线均未替换。下一阶段仍需重新完成四前缀及变体，之后才冻结新数据评测配置。


## 2026-09-06：定向补丁、纠错提交修复与逐事实核验

### 已落地的服务改动

- `repair.ts` 的补丁只能修改指定事实/操作及受影响来源。删除数组元素后按原始槽位重映射全部 `new:N` 引用；悬空引用拒绝提交，不能悄悄转向下一条记录。语义拒绝定位到单条fact时，同消息内的无关fact也不允许修改。完整结构仍会重新核验，尚未实施增量核验证书。
- 来源部分恢复也通过稳定句柄重映射。旧版本的真实回归复现了“移除无效事实后把薪资删除指向经理”的错误；新测试保证薪资清除且经理保留。
- 新增逐事实 `supported`、`modality_supported` 和声明来源内的真实引文核验。每条事实必须有独立且唯一的判断，消息级represented不能代替。拒绝“计划当已发生”“他人当本人”“否定变肯定”及无关引文支持；返回的语义问题进入局部修复。
- 可显式设置 `MEMORY_LLM_REASONING_EFFORT=low|medium|high`，未设置时沿用提供方默认。内部模型预算由原80秒调整到默认90秒（addTimeout减25秒，最多95秒），外层115秒及赛题120秒限制未变。模型、effort和预算变化均记录，不能将收益全部归为代码。
- [93项服务测试](../reports/round2-fact-verification-service-tests.txt)通过；[六个真实模型核验控制](../reports/round2-fact-verification-model-controls.json)6/6符合预期。它们不是总体准确率或完整长段推理成绩。

### 原始失败重放的实际结论

- [D17状态及问答复核](../reports/round2-D17-targeted-repair-review.json)：gpt-5.5/low、90秒内部预算的一次原始chunk成功；平板及同步信息清除，用户Firefox/Safari与Kevin的Brave保留，两条实际查询符合要求。这次未调用repair，不能归因于局部补丁。该运行早于逐事实核验的新版本。
- [五组真实生命周期变体](../reports/round2-lifecycle-variants-2026-09-06T02-59-13-447Z-WLOTNe.json)全部通过：直接删除、否定、引用、条件和他人转述。检查了主体、浏览器保留及原文可见性；这也是逐事实核验之前的版本，不能直接当作最新版本验收。
- [E03回答阶段复核](../reports/round2-E03-answer-stage-review.json)：八段均HTTP200，Martin/Rebecca/交接/群体场景都进入最终32条证据。原始Answer漏掉Martin与交接；对完全相同证据另问两次均覆盖了这些内容，三次中2次完整。保留原失败，不更改Answer提示词或预算，不把两次重复回答当作三次完整灌入/召回。
- [C30提交错误](../reports/round2-C30-commit-failure-review.json)：十四段虽均200，但旧$200与新$250都被retracted。真实提案明确指向旧$200，数据库却用operation.value中的新$250扩展重复目标，误撤销了新状态。现在只按已选目标的实际旧值、主体、属性和scope扩展。固定模型提案的[完整原chunk重放](../reports/round2-C30-recorded-correction-replay.json)4项通过：旧值撤回、新值active、事件只指向旧值、新金额可检索；此重放没有重新调用语义模型，新的真实长前缀另行运行。
- 加入逐事实核验后，D17的新运行在90秒内仍未完成。首轮核验指出两处来源不足，但同时生成了一个不存在的replacement检查，随后格式修复超时。不能沿用此前D17通过记录宣称当前版本已通过；需要保留已发现语义失败并减少无效的格式往返。

### E0与尚未完成的范围

[gpt-5.5控制集及哈希](../reports/round2-judge-gpt55-controls.json)为40/40，包括37控制与3已知反例；采用冻结的同一逐条件协议。199条独立审计正在运行，与mini结果分别保留；对照脚本验证输入、协议与完整分母，并将分歧及任一侧未决列入复核队列，不选择较高分覆盖旧判定。

完整E1独立目标解析、候选扩展、四前缀及更全变体仍需验收。E2–E5固定预算质量消融、至少三次完整配对、新背景评测、正式部署及归档仍未完成。默认模型、8088基线和33.1%历史成绩均未改变。


### 最新补充：更严格核验暴露的失败与C30真实恢复

- 服务检查点 `576706f` 的C30原始14段真实重放全部200，14次提取、15次核验、1次定向补丁及14次本地embedding均成功。[持久状态与查询复核](../reports/round2-C30-fresh-state-review.json)确认旧$200已retracted，新$250为active，1日/15日安排仍active。固定Answer已正确给出金额、基金和纠错，但省略明确的1日/15日日期，最终回答尚不能记作完整通过。
- 同一检查点的E03在第7段被正确拒绝：修复补回了听力技巧计划，却写成confirmed而非tentative；第二次核验拦下了模态错误。原来的八段HTTP通过不适用于这个更严格版本。
- D17首轮核验中的有效fact拒绝，不应因后来出现格式错误被重判覆盖。新回归先证明原路径会二次判分；现在格式异常携带已确认的拒绝，直接进入提案修复。修复后的提案仍需完整核验。新增明确的替换目标清单，空supersedes不能制造replacement检查；提取提示强调来源必须覆盖结论中的限定条件。
- [94项服务测试](../reports/round2-semantic-protocol-service-tests.txt)通过。最新D17及D24重放运行中；尚未验收，不将之前版本的成功移用为新版本结果。


### 检查点92ae3bd：D17、D24已复核；其余门槛保留

- [D17最新复核](../reports/round2-D17-fact-verified-review.json)：原始chunk在77.1秒完成，实际走过1次提取、1次定向补丁、1次逐事实核验及本地embedding；两个平板目标清空，工作电脑Firefox、手机Safari及Kevin的Brave保留。两条实际问答均符合预期。
- [D24最新复核](../reports/round2-D24-fact-verified-review.json)：原始四段全部成功，投资计划仍为第2版本且tentative；开放感/窗户、RAV4压抑感和AWD非主因均正确，两条实际问答通过。两项均为一次完整前缀诊断，不是三次重复或总体准确率。
- [阶段调用观察](../reports/round2-prefix-call-observations.json)分列成功、失败、报告token与缺失usage；C30逐事实核验版14段总耗时约311秒，之前约265秒。代码及随机生成同时影响结果，不能将差值归因于单一模块，也没有按未知价格估算费用。
- [三条判分分歧助手复核](../reports/round2-assistant-disagreement-review.json)确认一条漏掉明确必要条件，另两条保留reference充分性/隐含过程表达的判据歧义。没有覆盖原始模型判分，独立人工真值仍为0。
- 相同92ae3bd源码的E03/C30新重放及199条gpt-5.5独立审计仍在执行。完成后先检查日志、实际状态和固定Answer，再记录结果；不要重启已有运行或把旧版本通过项拼成一次完整验收。


## 2026-09-06：目标解析前移及独立审计完成

### E1：核验必须看到真正将被修改的目标

- 新增独立的确定性目标解析模块，返回resolved/missing_evidence/ambiguous。属性选择器在提交前转成具体目标ID；同值重复表示与property遗忘范围一并交给语义核验，提交层复用相同选择规则。
- 修复此前“删掉当前值但历史值仍可返回”的property遗忘缺口。以92ae3bd冻结运行时复现旧状态未清除，新版区分property与value边界并保留其他人的记录和其他设备scope。
- 操作找不到目标时，按操作的主体/属性/scope/来源追加最多32条候选，不再受整段对话前120条候选的遮挡。新候选仅进入修复输入，仍需模型明确选择和独立核验。
- [99项服务测试](../reports/round2-binding-service-tests.txt)通过，新增检查覆盖实际目标进入核验、重复表示与主体/设备隔离、120条之外目标修复、property与value历史边界。[真实模型HTTP验证](../reports/round2-binding-lifecycle-live.json)两组均通过，[原文与状态复核](../reports/round2-binding-lifecycle-review.json)确认property清除Seattle/Boston，value只清除Boston并保留Seattle历史。
- [原92ae3bd长前缀重放](../reports/round2-long-prefix-verification-failures.json)仍失败：E03第7段补回计划却标成confirmed；C30第14段修复一个不受该引用支持的content限定语后，全量二次核验超时。事务未提交失败段。新目标模块不等于已解决这两个问题。

### E0：完整199条执行结束，但不能直接重写成绩

- [gpt-5.5完整独立审计](../reports/round2-judge-audit199-gpt55-v2.json)：194条有效（32通过、162不通过），5条协议错误，无传输失败。40条控制样例保持此前40/40记录。
- [两模型配对对照](../reports/round2-judge-independent-comparison-v1.json)验证原始输入、模型之外的协议和完整199分母：共同有效150条，其中126一致、24分歧；任一侧未决或分歧的73条仍单列，不取较高分。
- [5条协议错误的助手语义复核](../reports/round2-gpt55-protocol-error-review.json)均发现缺项，不覆盖原始审计。其中3条因大小写/Markdown引文不逐字一致；另2条暴露判据协议问题：harmful_extra本身是“遗漏某事”，此类违反无法提供被遗漏内容的答案引文。后续须显式区分存在型与遗漏型证据，另立协议版本与控制集，不修改冻结v2结果来取好分。

剩余：请求内核验记录复用与局部失效传播；独立语义目标分类及向量/原文目标扩展；四前缀完整重放、全部变体和三次配对；E0判据边界、E2–E5新背景质量/成本消融、部署与归档。完整目标保持进行中，33.1%历史成绩和8088基线未变。

## 2026-09-06：增量核验、阶段路由与修复预算

### 已完成的实现与确定性证据

- `5355177`：每次`prepare`拥有私有核验记录，绑定请求/租户/模型/协议及事实、来源、目标和传递依赖。只复用通过记录；修改、追加、删除、重编号及依赖变更触发失效。合并后仍校验完整覆盖、唯一性和真实来源。关闭正向复用也不会解锁未修改的失败项。
- [112项测试](../reports/round2-incremental-verification-service-tests.txt)覆盖请求/租户隔离、传递依赖、伪复用、缺项、格式错误和真实Extractor调用链。[原C30离线机制重放](../reports/round2-recorded-C30-verification-scope.json)将36项检查缩为6项，保留30项；fact0的变化仍传播到纠错事实、操作、替换关系和两条消息。这不是实时模型延迟测量。
- 补丁新增事实必须显式给出modality；局部编辑保留未修改字段，不能因省略字段把计划变成confirmed。原缺省确认问题有先失败后通过的回归。
- `3a5b2cb`：增加extraction/verification/repair独立模型配置。结构错误的整份重提取按repair路由；核验格式修复仍用verification模型。审计记录实际阶段与实际模型，核验记录绑定实际核验模型；不支持的模型不会触发其它模型的隐式替换。[116项测试](../reports/round2-stage-routing-service-tests.txt)通过。
- `32cca16`：目标范围与独立来源错误合并反馈。`MEMORY_MAX_REPAIR_ROUNDS`默认1，可显式设2；所有补丁共用90秒默认模型预算，外层115秒不变。新增回归覆盖第二次遗漏修复、缓存传播及无变化失败不会重判，[119项测试](../reports/round2-repair-budget-service-tests.txt)通过。

### 六次真实前缀尝试的完整结果

均为新目录、新灌入、原始消息与chunk边界；本地embedding保持相同digest。gpt-5.5使用low，内部预算90秒。各组只有一次，生成内容和路径会变化；不能作为三次配对实验或只按较好结果选分。

| 配置 | E03 | C30 | 结论 |
|---|---|---|---|
| `5355177`，统一gpt-5.5，一轮补丁 | 7/8，第8段失败 | 13/14，第14段失败 | E03第7段确有21项复用，二次核验9.9秒；末段首次生成/核验仍耗尽预算 |
| `3a5b2cb`，mini提取、5.5核验/修复，一轮补丁 | 6/8，第7段失败 | 0/14，第1段失败 | 更快生成仍有来源/覆盖缺失；结构修复或新增遗漏耗掉唯一补丁机会 |
| `32cca16`，同一模型路由，两轮补丁 | 7/8，第8段失败 | 13/14，第14段失败 | E03第7段实际两轮修复成功，末次重查2项、复用17项；两者末段仍耗尽预算 |

完整阶段调用、原始结果哈希及失败原因分别见[增量核验复核](../reports/round2-incremental-verification-live-review.json)、[模型路由复核](../reports/round2-stage-routing-live-review.json)、[两轮补丁复核](../reports/round2-two-repairs-live-review.json)。所有失败保留，未用重跑覆盖旧记录；失败末段均没有提交，也没有对不完整前缀生成“恢复后回答”来宣称通过。

最新E03末段：提取38.4秒、结构修复5.5秒、首轮核验41.3秒，剩余4.7秒不足以补回情绪/职业语境及修复模态。最新C30末段：提取30.2秒、结构修复5.0秒、首轮核验42.5秒，剩余12.3秒不足以完成多项来源与具体旅行/转账遗漏修复。这说明有界补丁和复用已生效，但不足以完成当前首轮工作量。

### 后续工作保持完整

优先验证紧凑核验输出：用受约束的来源/目标引用编号减少重复字段、长ID和引文生成；保留完整输入、逐事实含义/模态、授权、替换和消息覆盖检查。必须先锁定格式版本与独立控制，再验证错误编号、来源角色、缺项、格式错误中的已有拒绝及缓存失效；不能通过减少必检内容或放宽时限取得通过。

覆盖标准中的边界另行校准；本轮失败还包括真实遗漏，不能把所有拒绝都归为核验过严。独立目标语义分类、向量/原文目标扩展、四前缀及变体在同一最新配置上的验收、至少三次完整配对仍未完成。E2/E3固定预算质量对照可以在冻结实验配置上并行开展，失败请求保留在分母中；E0判据协议、新背景评测、E4/E5质量成本实验及最终部署/归档仍属于完整目标。

三个服务提交均已推送私密仓库。生产8088健康、基线未替换；本轮未重跑总体评测，33.1%仍仅是历史成绩。


## 紧凑核验、引用分类与四前缀完整复测

### 已实施及其证据

- `077aaf8`新增可选compact核验编码。来源槽位、操作索引和替换目标编号在服务端解析回精确证据；事实/操作/替换/消息均经过原规范校验。格式错误不产生通过记录，格式错误之后的有效语义拒绝也不会丢失。默认仍为verbose。
- [32次真实核验控制](../reports/round2-compact-protocol-paired-review.json)全部符合预期：13个控制各跑两种格式，另对同一已拒绝长提案做三组配对核验。没有格式重试，规范化后的16组输入一致。长提案中位核验耗时43.8秒→20.2秒、completion token2326→1026。这仅是同一固定提案的核验调用，不能外推为完整写入提速或总体准确率提升。
- [077aaf8的原始E03/C30尝试](../reports/round2-compact-prefix-live-review.json)仍分别7/8、13/14。E03耗尽90秒；C30在79.4秒因覆盖引用错误被错误地记作语义遗漏而失败。
- `441cc48`新增MESSAGE_SOURCE_LINKS并区分引用协议错误与真实遗漏。[原始C30输出红绿重放](../reports/round2-recorded-coverage-references.json)证明旧版错误锁定message10，新版仅保留message2/4/6真实遗漏。错误引用没有被裁掉后直接视为通过；修复后的提案仍须完成核验。
- [129项服务测试](../reports/round2-coverage-reference-service-tests.txt)通过，覆盖有限协议纠错、保留真实拒绝、错误协议不生成通过记录及完整原有事务/隔离/并发检查。服务已推送私密仓库，生产8088未替换。

### 相同配置的四前缀与变体

配置统一为mini提取、gpt-5.5核验/修复、low、compact、最多两轮补丁、正向复用；模型90秒，写入115秒。[完整报告](../reports/round2-source-links-prefix-live-review.json)保留全部尝试、源码与探针hash和调用记录。

| 前缀 | 本次HTTP结果 | 状态与实际问答复核 |
|---|---|---|
| D17 | 1/1，末段61.8秒 | 平板3条目标清除，工作电脑Firefox、手机Safari、Kevin Brave保留；两条实际回答符合对应检查 |
| D24 | 4/4，末段43.0秒 | 投资计划及购车理由保留，两条回答符合要求；其中investment_plan被存成confirmed/single，违反当前未落实计划应为tentative的约定，语义验收未过 |
| C30 | 14/14，末段75.9秒 | 当前250、VTSAX/Fidelity、1日/15日和周一150转账均保留，200不再是当前投资事实；回答仍将记忆纠错表述为实际配置变更，完整语义验收未过 |
| E03 | 7/8，末段90.0秒拒绝 | 提取28.4秒、来源修复4.6秒、核验28.5秒、补丁26.6秒，最后11事实/4消息核验只剩1.9秒；末段未提交 |

[状态与Answer逐项复核](../reports/round2-source-links-state-answer-review.json)由助手检查，不是独立人工真值。三次HTTP前缀成功不能写成三次语义通过，更不是三个完整独立重复。并行探针/检索/开发集负载存在，延迟只报告观察值，不能单独归因于源码变化。

五个独立合成变体（直接遗忘、否定、引用、条件、转述）使用同一写入配置，各两次真实HTTP写入，**5/5通过**。均检查Firefox及Kevin/Brave保留、Chrome的预期保留/清除、遗忘事件及安全来源。结果位于[变体报告](../reports/round2-lifecycle-variants-2026-09-06T04-44-13-452Z-9ldcqX.json)。它们是已曝光的开发控制，不是新基准成绩。

### 接下来的执行顺序

1. E03：减少首轮遗漏及错误操作目标。末段实际漏了11项个人细节，单纯缓存不能省掉首次检查与必要的新事实核验；不能再以延长预算或跳过核验来追求HTTP200。
2. 统一计划模态：明确“已确定表达的意向”与“已经发生的状态”的字段约定，加入坚定未来计划和真实已完成事件的成对控制，检查提取和核验是否共同误判。
3. 纠错与真实历史：保留有来源的纠错事件/状态说明，使“记错200，实际250”不被Answer解释为已执行过200；固定Answer及其预算，保留本次失败答案。
4. E2/E3：已冻结[四组相邻单因素配置](08-固定预算质量对照.md)，启动`round2-quality-dev-441cc48-r1-facts-memops`，5个已曝光样本、30道题、并发2，端口8096。当前仍在灌入；E10_reflect第12段已因EVIDENCE_VALIDATION失败，6题保留为service_error，见[阶段快照](../reports/round2-quality-dev-441cc48-r1-progress.json)。其余样本继续，此快照不是最终成绩。本次灌入已不满足纯检索配对复用条件，不能删去失败背景来制造收益。
5. E0边界校准、E4/E5固定预算消融、三个完整重复、新背景曝光审计和最终部署/归档继续保留。新集尚未选择，生产与历史33.1%不变。

实验执行器补强：复用灌入时校验数据hash、服务/评测commit与干净源码、写入模型/表示/修复配置；允许纯检索配置变化。缺少身份记录或改变写入配置均拒绝，相关[4项回归](../reports/round2-experiment-identity-tests.txt)通过。此改动仅约束后续复用，不改变已在运行的首轮。


## 来源修复、计划模态、严格格式与删除效果复核

- `407abc3`只恢复唯一的空白/大小写引用抄写差异，保留真实原文与声明说话人，并继续独立语义核验。重复出现或跨说话人的大小写匹配仍要求修复。[固定原始E03重放](../reports/round2-recorded-source-copy.json)省去一次来源修复调用；不代表语义或全前缀已通过。
- `c8cbcae`明确未落实的坚定未来行动仍为tentative。六个相同语义控制旧版4/6、新版6/6；先旧后新且各一次，不能做延迟对照。[D24状态和Answer](../reports/round2-D24-plan-ontology-review.json)中两条投资计划仍revision2/tentative，购车原因保留，两条实际Answer通过。
- `ef8d236`新增可选`MEMORY_VERIFICATION_RESPONSE_FORMAT=json_schema`，只用于compact核验。要求四个数组的严格外层格式，元组槽位、索引、来源、完整性及语义仍由服务校验；网关不支持时不自动降级。默认json_object不变。四次固定长提案的交错对照中两种格式都完整，但语义覆盖结论不同，不能声称协议错误率或准确率已经提高。
- [E03完整严格格式复测](../reports/round2-E03-strict-prefix-review.json)仍7/8。末段一次来源修复、一次感知反应修复后，检查器又发现情绪/忘名字细节遗漏，80.2秒以EVIDENCE_VALIDATION拒绝；两次完成的核验均含四个数组。此前单一保存检查点82.2秒prepare通过，不替代这个完整失败结果。
- [C30/D17新状态与Answer复核](../reports/round2-source-order-state-answer-review.json)：C30旧200为retracted，250及1日/15日日程active；Answer能说清记忆纠错，但仅说每两周，遗漏具体日程。D17虽然HTTP200，仍保留平板问题与售出历史，并在Answer中复述；完整语义验收失败。

### 删除效果缺口及修复

D17原模型生成retract，仅撤下issue_resolved。原省略检测接受任意来源匹配的forget/retract，核验模型也同意，于是平板事件及来源残留。`4bca000`在抽取修复前与事务内都检查操作效果：停止保留信息不能由retract满足；只有明确从当前同事/联系人列表移除，且边界为current_relation，才允许以retract保留历史。离线路径也使用相同的当前关系边界。提示词同步区分“事情结束”与“停止记住”，但实际目标仍须绑定并独立核验。

[固定已接受提案重放](../reports/round2-recorded-retirement-effect.json)不调用模型、不写库：同一原始提案旧版遗漏报警为0，新版为1。[138项服务测试](../reports/round2-retirement-effect-service-tests.txt)包含错误效果进入修复、未修复拒绝、全部选定实体事实/来源清除、邻居保留和事务拒绝回滚。真实原始D17写入73.2秒、HTTP200，三条平板事实erased且无残留平板来源；完整语义及变体结果以本节后续报告为准。

### 开发集最终结果与后续边界

旧441cc48五个样本共30题：E10_reflect第12段、B05_forget第18段、F07_trajectory_ops第22段、F07_update第45段失败；前述四个背景的25题全部保留service_error。E10_remember完成5题、代理Judge判对2题。失败灌入不可复用到纯检索配对实验，不能仅留下可答题子集。

F07保存的revision21检查点在ef8d236严格配置下39.95秒prepare成功，8事实、0操作、无降级。独立数据库克隆通过真实commit得到revision22，原55事实语义字段全部保留。原快照没有用户自己的治疗事实，故不能声称“保留了已有每周治疗信息”；已另记最初复核假设错误。该克隆不修改原失败运行，不移除5道失败题，也不是新的完整F07通过。

E0判据边界、E1稳定全前缀、E2/E3纯检索配对、E4/E5专项、三次完整重复、新背景与最终部署仍未完成。生产8088保留原基线，最新健康检查200。下一步集中处理输入覆盖与目标绑定的可解释错误，再冻结可完成整批写入的版本。


### 4bca000真实复测及新发现

[删除效果复测](../reports/round2-retirement-effect-live-review.json)：D17原始完整单段73.2秒HTTP200，三条平板事实及来源清除，两条固定mini回答未泄漏已删除细节，Firefox/Safari/Kevin Brave保留；第二条Answer转述剩余浏览器信息，没有直接说明平板记录不可用，表达相关性仍有限。五类合成遗忘控制各两次add全部通过，5/5。这里只验收这条删除缺口，不代表所有生命周期路径已恢复。

[保存检查点与克隆事务复核](../reports/round2-development-checkpoint-state-review.json)：E10_reflect在原revision11状态上35.3秒prepare成功、9事实0操作、无降级；克隆commit到revision12，原事实语义未改变。B05_forget在原revision18状态上77.6秒prepare成功、28事实2操作、无降级；克隆commit到revision19，但语义验收失败。旧Iris备用名字和偏好记录已清除，新生成的“移除Iris”“保留Iris是犹豫”等事实仍active且原文/词法检索可见。

B05暴露了独立缺口：删除标记仅按subject/scope匹配，新的baby name list/Iris scope被当成不同场景，联合主体记录又只留下长值指纹。新事实的链接随后使原文片段绕过删除。下一阶段必须把改述绑定回真实实体与删除边界；不能简单全局屏蔽名字，否则会误删无关主体或真实不同场景。两次检查点不更新原开发集失败分母，也不是完整样本恢复。


## v3删除范围与当前验证进展

实现与边界见[目标绑定设计](07-目标绑定与定向修复设计.md)及[证据复核](../reports/round2-erasure-binding-review.json)。该功能为独立开关，使用核验模型及共享90秒预算；146项服务测试包括跨scope改述、后续回述、同名同事、其它项目同值、混合原文邻居、明确重新授权、旧助手回述清理、陈旧判定拒绝及事务回滚。

- 固定B05旧提案：两个候选由真实模型均判erase，5.8秒。首个克隆当前事实/片段无残留，但三条旧助手回述仍可raw检索，记录保留。最后只重放相同判定并修复交易清理，四条检查（当前事实、原文片段、raw、检索）均无已删除名字；Elara及June/Mae保留。
- 新B05检查点：重新抽取、来源修复、核验、补丁、再核验、范围判定、本地embedding共75.1秒，28事实2操作、无降级；范围阶段3.3秒。最终交易复核复用这次已保存判定，未重新采样。检查点及无标记旧状态在私有克隆中的诊断性格式移植不替代新目录完整灌入。
- D17新v3完整前缀49.8秒通过，删除两条平板目标；实际回答正确保留Firefox/Safari/Kevin Brave，对平板问题表示没有相关记忆，但附带了一句不必要的通用书签同步说明。本次没有歧义候选，未调用范围模型，因此不能算作范围分类器的真实正例。
- 五个真实范围控制均符合预期：改述和家庭共同表述清除；同名同事、另一项目同值、混合引文中的独立Elara信息保留。另五类真实遗忘变体5/5，均为已曝光合成控制，不是基准准确率。
- C30日程已进入保存的Answer输入，遗漏发生在Answer采用阶段；不能归因于该日程没有召回。后续证据组织对照另做。

B05原失败前缀正使用d8e264b及新v3目录重放。即使前缀通过，仍需完整背景后续消息和10道题验证；原30题开发集2/30及历史331/1000均未改变。生产8088仍为旧基线，E0评分校准、E1其它长前缀、E2–E5质量/成本、三次完整重复与最终部署归档继续保留。


### B05新目录完整原失败前缀已通过

[新目录完整前缀复核](../reports/round2-B05-erasure-full-prefix-review.json)：19/19 add成功，原失败段71.5秒，190条消息正文均无已删除名字；当前事实、原文片段、实际检索证据及两条固定mini回答也无该名字。Answer明确保留“不需要备用名”的决定，首名Elara，中间名June/Mae，以及Priya女儿Violet Mae的主体关系。

本次提取没有产生歧义同值改述候选，范围模型调用数为0，不能把成功全部归因于分类器。分类器作用由固定失败提案及五个独立语义控制另证。该前缀只到原失败点，B05完整背景实际有50个会话和10道题；下一轮应重新完整灌入已曝光开发集五个50会话背景、全部30题，不复用此诊断目录，也不覆盖旧2/30。


### 完整旧开发集重跑已启动

`round2-quality-dev-d8e264b-r1-facts-memops`使用服务d8e264b、评测器94d58f6、erasure候选配置，在8096端口以并发2运行。五个已曝光背景各50会话、全部30题；新目录重新写入，不复用失败状态。启动后已核对实际服务/评测进程及模型阶段配置，初始局部进展另存[运行快照](../reports/round2-quality-dev-d8e264b-r1-progress.json)。模型及评测进程加载的版本固定，后续源码变化不应被记成该运行使用了新版本。此运行尚无最终成绩，不能更新33.1%或以初始通过量推算成功率。


## v3审计与完整重跑新增失败（2026-09-06）

评测器`b6c8125`新增可选`--protocol v3`：区分必须出现与禁止出现的含义；模型返回答案片段编号，程序按原始UTF-16位置还原引文，避免要求遗漏内容提供引文或重抄Markdown。默认v2和普通Answer/Judge不变。20项TypeScript、9项Python测试通过，包含控制标签不进入模型输入的HTTP测试。

[48项控制](../reports/round2-calibration-v3-controls48-review.json)全部返回有效协议，47项符合原助手标签；新增8项遗漏/禁止内容控制全部符合预期。[追加11项](../reports/round2-calibration-v3-actor-errors11-review.json)中，旧5条协议错误均有效判为false；6项人称对照有1项与挑战标签不同。争议项在[完全相同模型输入](../reports/round2-calibration-v3-actor-disagreement.json)下先判false后判true：未注明代写时“我现在住在Leeds”究竟指谁，尚不稳定。保留两次结果和原标签，不能以协议有效替代语义校准，也未启动完整199条v3审计。

[完整开发集运行中快照](../reports/round2-quality-dev-d8e264b-r1-failures-progress.json)记录B05原失败段仍OPERATION_TARGET、E10越过旧失败点后在第26段EVIDENCE_VALIDATION。16道相关题保留service_error，其他背景继续运行。该运行固定使用d8e264b/94d58f6，后续代码与构建没有热加载进去。

[两次保存检查点诊断与修复复核](../reports/round2-tracking-retirement-checkpoint-review.json)：B05新模型修复在补充Elara确定选择时错误supersedes不同属性的Iris备用名，63.1秒拒绝；E10旧规则误拒绝“you don't need to keep track of that anymore”，要求删掉合法操作，后续补丁又把目标引文扩展到遗忘来源之后，63.3秒拒绝。原完整运行没有保存提案正文，这些是新模型诊断，不能当成原失败的逐调用重放。

服务bdf5d48补上直接第二人称停止记录识别。两项新增测试旧版失败、新版通过；[同一保存提案的无模型对照](../reports/round2-recorded-tracking-retirement.json)由不授权/无义务变为授权/一条义务。新E10检查点60.2秒prepare成功，11事实1操作无降级；私有v3副本从revision25提交到26，原113事实不变。但一条可搜索的助手原文仍保留预约改述（省略office导致长值指纹不匹配），完整语义删除验收仍失败。当前事实/原文片段/本次词法结果无预约，不足以掩盖raw残留。没有新Answer/Judge或完整前缀通过声明。

下一步先处理补丁中不同属性替换和原文改述的目标绑定，再考虑扩大检索；E03、E0语义判据、E2–E5完整配对、新背景、三个完整重复和部署归档继续保留。历史331/1000未改变。


## v4原文范围判定与本轮最终证据

[实现及复核](../reports/round2-source-erasure-review.json)：`MEMORY_SOURCE_ERASURE=true`须与erasure binding一起启用，使用新v4目录。事实内容和整条原文消息先由字面值/词项哈希发现候选，再由核验模型区分删除与独立邻居；精确子串分区必须覆盖原文全文，UTF-16位置保留。提交前重新绑定全部来源、事实和操作；raw/尾部采用空白遮盖，片段裁剪、向量失效、来源引文更新一起回滚。缺失、改写、不确定或超限均拒绝。该阶段共用原90秒预算。

原E10已验证提案的私有诊断副本：首次句级候选6.4秒清除残留；改为整条消息后8.6秒，两条消息的预约及回述清除，SMS/咖啡/礼物信息保留。两次都只新增范围判定，未重抽取事实，不是支持生产v3迁移。

1f77f5a的新四步HTTP控制前三步通过，明确同事Kevin的独立预约被误判不确定而拒绝。862399d澄清“已证明不同主体就是保留依据”，新目录四步全部通过；原失败保留。相同旧五候选输入的新判定均为retain。六项单阶段控制初始自动4/6，另附[标签/分区复核](../reports/round2-source-owner-control-label-review.json)：孤立姓名Kevin并未证明不是用户；任一片段不确定已足以拒绝整次计划，不要求其它无害片段都不确定。复核没有重写原模型输出，也不算新的六项完整成功运行。五类真实遗忘变体5/5。

[E10新目录完整原失败前缀](../reports/round2-E10-source-erasure-full-prefix-review.json)使用冻结1f77f5a，26/26成功，末段73.0秒、revision26。实际Answer对预约明确没有具体记录；对当前设置回答黑滴滤咖啡、Kevin生日的Coltrane《A Love Supreme》黑胶及短信/在线预约偏好。这里只覆盖原segment26之前的输入和两条诊断问题，不是50会话全部六题，也不混算为后续862399d的整轮成绩。

[旧d8e264b完整开发集最终报告](../reports/round2-quality-dev-d8e264b-r1-final.json)：152次add成功、3次终止失败，另有一次VERIFICATION_UNAVAILABLE重试记录。B05第18段、E10_reflect第26段、E10_remember第5段失败；21题service_error，9题正常判分、6题正确，完整分母6/30。F07两个背景完成，并不满足整批灌入复用门槛。该结果不更新历史331/1000，也不是最新v4质量。已保存三个失败背景的终止快照，新增E10_remember revision4供后续绑定诊断。

后续优先解决原文尚未形成事实时的操作目标解析与覆盖策略，以及B05/E10_remember修复中的错误替换/引用。当前原文候选仍依赖词项重合，跨消息照应、完全改写及大范围分组尚待实现/验收。E0校准、E2–E5固定预算质量/成本、三个完整重复、新背景、部署与归档范围保持不变。


## 多轮修复范围与隐式覆盖诊断（2026-09-06）

服务9726881修复了可确定复现的旧范围问题：缺失消息修复新增事实/操作后，后续绑定错误须按新提案重新定位；旧范围不包含新索引，导致有效补丁被拒。四项新增回归覆盖新事实、新操作、无关项保护和替换分组，全部159项通过。最小回归修改前报Invalid target/scope repair patch。

修复输入增加REPLACEMENT_TARGET_GROUPS，明确补充覆盖不等于替换授权。相同主体、属性和范围只是结构兼容，还要用户来源证明实际替换；不能为通过校验重命名属性。B05原保存补丁将新名字承诺指向备用名m23；新单次补丁不再增加该引用，保留三个forget操作。把这份提案送入新的完整语义核验，18.5秒准备通过，无降级；私有v3副本revision18→19，四条目标旧事实清除，其余94条旧事实不变。初始抽取是保存提案，不能合并不同阶段耗时为端到端成绩。

同版本另一轮B05从头抽取78.6秒失败：第一次修复返回后仍有绑定问题，下一修复两次模型调用均报Error，未返回提案；离线恢复无法证明遗忘范围而被拒。当前日志不能进一步确定是网络还是网关等原因。完整B05可靠性仍未解决。

E10_remember新目录按原边界执行五次HTTP add全部200，末段48.4秒；真实mini抽取5次、55核验7次、55修复2次、本地embedding5次，另有1次核验调用错误重试。固定mini实际回答：改期规则完整，预约渠道正确，咖啡细节遗漏。冻结9726881运行时与灌入源码hash一致。

咖啡遗漏在存储阶段发生：重述黑滴滤咖啡时合并来源但保留旧观察时间；随后同条消息中的“喜欢简单咖啡日常”也被抽成单值coffee_preference，存储按时间自动将前者supersede。没有显式替换边，独立核验没有审到这次自动状态改变；当前检索因此不返回黑滴滤及无奶无糖。确定性最小重现见[隐式覆盖复现](../reports/round2-implicit-replacement-reproduction.json)，这是尚未修复的错误，不是通过用例。

下一步：将潜在隐式状态变更纳入核验和提交计划，区分重述、兼容补充、真实替换与冲突；核验须覆盖实际将隐藏的旧证据。不得以全部改多值或重新暴露所有旧事实绕开真实update/forget边界。仍无新完整准确率，历史33.1%与旧开发集20%保留原口径。


## 隐式状态转换核验与完整开发集启动（2026-09-06）

服务8ef8829新增`transitions.ts`与可选`MEMORY_SEMANTIC_TRANSITIONS`，必须开启原文删除并新建v5目录。核验看见即将比较的旧/新事实、来源引文与当前消息，输出compatible/exclusive/uncertain及双侧见证。兼容信息不再仅因同槽单值和较晚时间而隐藏旧值；互斥值仍遵循原时间与冲突规则。不确定、无效、遗漏或超容量均拒绝。计划绑定原请求、全部前置与有序新增事实和操作，提交时重算；显式操作/替换沿用已有独立核验。非空候选额外调用既定核验模型，共享90秒总预算，没有另加预算。

全部170项测试通过，11项新增覆盖兼容补充、重复来源、真实更新、历史与冲突、原子回滚、计划篡改、格式切换、见证、容量、纠错/计划/遗忘/恢复。六个自编实际模型控制6/6符合预设含义，标签未传给模型；它们不是独立真值或准确率。

固定抽取提案的真实HTTP三步验证：先存咖啡偏好，再补充日常，再真实改变饮品，3/3写入成功。关系判定/独立核验/embedding/存储/Answer均为实际调用。补充后Answer完整说出黑滴滤、无奶无糖；更换后旧饮品superseded、拿铁active。但第二条Answer仍把保留的通用日常当作冲突，不能记为回答全部通过。下一步需要证据关系与组织方式，保持旧值生命周期边界及固定Answer。

新目录E10_remember五段真实重放5/5，末段61.0秒，两条诊断Answer完整。该次抽取使用多值咖啡事实及独立routine属性，没有触发state_transition调用；成功与前一版的差异含模型抽取波动，不能宣称分类器单独带来提升。

已启动完整开发集`round2-quality-dev-8ef8829-r1-facts-memops`：服务8ef8829、eval b6c8125、8096端口、并发2，五个已曝光50会话背景及全部30题，新v5目录，固定mini Answer/Judge。服务/评测源码干净，启动时root尚含本轮报告与配置未提交变更，manifest保留该状态及配置hash。必须观察实际进程与最终结果，不能用局部前缀推算成绩；仅全部add成功时才允许纯检索配置复用。E0–E5、新集、三个完整配对及最终部署仍未完成。


## v5完整开发集失败与关系协议v3（2026-09-06）

[最终报告](../reports/round2-quality-dev-8ef8829-r1-final.json)：固定8ef8829运行结束，62次add成功、5次终止失败；五个背景均未完成，30题全部service_error，完整分母0/30，正常判分0题。四个背景报v1通用隐式转换证据错误，B05报修复预算不足。原运行未保存模型正文，不能将四次错误进一步归因为不确定关系或引文复制失败。五个终止前快照只读保存，禁止以新抽取结果替换原失败。历史331/1000与旧d8开发集6/30仍保留各自口径。

候选协议改为state_transition_v3：模型选择双侧source_quotes索引，程序核验索引及全量覆盖，避免重抄引文。已核验陈述之间的uncertain关系保留双方，并标为当前状态未决，不选胜者、不隐式隐藏。删除范围不确定仍拒绝；显式纠错可在去掉不同候选后使重复合并的剩余值恢复active。174项服务测试通过，覆盖未决保留、混合消息中独立遗忘、非法见证、纠错解除未决。测试桩的整段删除问题已改为明确保留咖啡子句、删除授权子句，未放松服务的来源保护。

六个最终v3真实关系控制6/6；固定抽取提案、真实核验/关系/embedding/HTTP三次写入均成功。补充日常后Answer完整；真实改为拿铁后，Answer仍误把保留的普通日常当作饮品冲突。存储成功不等于回答质量通过。此前仅追加状态事件说明的六次Answer诊断，基线和注释各只有1/3明确答对，没有稳定收益，因此不集成该注释。

新增可选私有模型trace，保存逐阶段输入、输出、失败部分流及请求关联，文件0600，普通用量日志只关联trace_id；不记录凭证/HTTP头。实验启动器须显式--trace-models。新候选配置单独保存，原配置及原运行结果不覆盖。当前仍未通过完整质量门槛，生产8088未部署。后续完整重跑、E0–E5固定预算对照、新背景、三个完整配对及部署归档均待完成。


### B05删除见证修复范围

新检查点fawfE4从头抽取58.4秒后失败，私有trace完整记录：四条forget中只有operation1引用了非命令说明；旧反馈未指出索引，第一次补丁移除全部四条，第二次又将不同属性目标合并。服务7910120把授权见证问题限定到失败操作索引；可针对同一目标修正实际授权原句，没有授权则只移除该操作。其它合法操作禁止被补丁修改，后续目标/独立语义核验保留。新增最小回归旧版暴露两个可编辑操作、新版仅一个；176项服务测试通过。以上原因适用于本次保存trace，不冒充旧完整运行的逐调用复现。

实现与实际模型证据见[关系协议v3复核](../reports/round2-transition-v3-review.json)。当前正重放相同B05初始提案验证修复，初始提取不重新采样；另对E10_remember旧失败前快照做一次新抽取诊断。两者都不是整段背景完成或基准成绩。


### 原文分批核验与预算瓶颈

B05固定初始提案重放4m2YZv在39.1秒完成定向见证修复、另一轮遗漏补充及独立核验，随后因原文候选上限失败；旧失败完整保留。新增有界分批，单批最多64候选/64000输入字符，总工作最多256候选/256000候选字符。批内索引映射回完整计划，全部批次共用原请求期限，任何遗漏、不确定或失败都不能提交部分删除。179项服务测试通过，新增100候选完整覆盖与输入限制、70条旧助手回述的提交及末批失败回滚检查。

E10_remember新检查点v2G6kp在90秒失败：实际抽取、三次核验、两次修复后，state_transition返回不完整输出。前两次核验分别补充了拖延/家务干扰/在线预约渠道，以及电话造成疲惫和异步思考的原因。失败已保存，不能按测试通过或其它前缀成功忽略它。当前需完整测量写入阶段预算和失败率，仍没有新的完整通过成绩。


B05分批诊断JJ206D严格重放六条已有上游模型输出，消除再次抽取/修复的波动，仅调用此前被容量拦截的原文阶段。扣除前次39.1秒后剩余50.9秒，首批25候选、62040输入字符，模型已输出12435字符仍未完成，最终按原预算失败。分批解决了容量拒绝，但逐字分区输出仍昂贵；后续应验证压缩全保留/全删除判定或索引化分区，不能用增加超时假装通过。首次诊断工具因浮点timeout报错，未调用任何模型；该记录也保留。最终服务2542f2a及冻结运行时已保存，完整可靠性与回答质量均未宣称通过。


### 新完整开发集运行中

`round2-quality-dev-2542f2a-r1-facts-memops`于2026-09-06T07:38:07Z启动；服务2542f2a、评测b6c8125、工作区b9865b5，启动时三库均干净。使用新目录、source-batches配置、固定mini Answer/Judge、五个已曝光背景30题、8096端口、并发2，并开启0600私有逐阶段模型trace。进展见[运行快照](../reports/round2-quality-dev-2542f2a-r1-progress.json)。这是进行中运行，不能据初始add成功量推算分数，也不能热加载后续修改或复用失败灌入。


## 原文判定压缩与跨阶段一致性（2026-09-06，96f40bd）

本批改动及全部失败变体见[原文流水线复核](../reports/round2-source-pipeline-review.json)。没有增加90秒模型总预算，也未改变固定Answer/Judge或原评测分母。

- `1730960`：全保留/全删除只返回效果；混合原文只返回需删除的唯一精确引文，程序重建完整分区。首轮25候选耗时44.5秒，第二批剩余6.2秒后失败，不能据首批成功宣称恢复。
- `71b9789`：相同原文、删除边界无损去重成引用表；35候选实际输入由85630缩至27210字符。自由理由版本仍超时；固定理由类别版本43.5秒返回，但有不确定判定，仍失败。
- `96f40bd`：已由前级核验删除的事实不再被后级重复判定；全部原文仍独立审查。目标范围与非授权引文错误合并进同一次定向修复。关系见证显式标注slot，非法索引继续拒绝。源文本同时带待保留及已核验删除的事实见证，提交重验精确计划身份。
- 原文引文可最多修复一次、最多8处，修复模型只能改变无效引文，不能改判定或候选覆盖；全部语义项必须已确定。共享原期限，不延长预算。第一次实际修复错误选择另一处表述而重叠，拒绝；加入固定兄弟引文上下文后，第二版3.6秒完成合法修复。
- 私有副本QlWmpc事务revision18→19成功，保住新“无需备选”和不再更改的承诺、Elara、June/Mae及Priya/Violet。但已删除的Iris说明仍残留原文和可检索片段，**语义遗忘验收失败**。因此新增一致性守卫：已删除事实的全部已定位原始见证不能原封不动留下；只拒绝矛盾，不自动扩大删除。此条件必要但不充分，不能证明全部同义回述都已清除。
- 新源上下文cRRGIT在46.7秒返回，对一段泛指备选/中间名的助手问题给出uncertain_scope，整体拒绝。未重采样该语义判定。B05完整可靠性仍未解决。

[198项服务测试](../reports/round2-source-consistency-service-tests.txt)通过；[一致性缺陷红测试](../reports/round2-erased-witness-consistency-red.txt)在修复前明确缺少应有异常。新覆盖包括保持补丁边界、拒绝不确定/不完整/重叠引文、跨批仅一次修复及同一deadline、HTTP模型路由、删除事实但保留其完整原文的事务回滚。测试不代替真实质量。

显式编号后F07原失败19对关系的独立模型调用均通过协议校验，不是19题答对或整背景恢复。两个早期真实source生命周期探针均四次HTTP成功；packed历史主体控制曾5/6，保留该失败，不能只报另一版本6/6。

完整`2542f2a`运行的[阶段成本快照](../reports/round2-quality-dev-2542f2a-r1-stages-progress.json)与[失败链分析](../reports/round2-quality-dev-2542f2a-r1-failure-analysis.json)分别保存。F07_update反复暴露早期消息遗漏，两轮修复后仍不完整；E10_reflect对助手编造的健身记录提出无可绑定目标的forget，修复删除该操作后覆盖仍失败。新B05完全重新抽取JTtJQK在抽取、遗漏修复、核验后只剩约2.7秒给原文阶段，因此输出压缩本身不能解决完整链路预算。

下一步以[写入可靠性后续设计](09-写入可靠性后续设计.md)为顺序。当前代码已冻结，8088继续原基线；E0、E2–E5完整配对、新背景、三次完整重复、正式部署和归档仍未完成。历史331/1000保持原口径。


### 2542f2a完整开发集结束与最新真实生命周期检查

[完整最终报告](../reports/round2-quality-dev-2542f2a-r1-final.json)：119次add成功、5次终止失败、1条中途重试记录；30题全部service_error，正常判分0，正确0，完整分母0/30。最后E10_remember在第35段失败：独立核验发现较早消息的个人设置遗漏，定向修复连续两次生成失败且没有返回文本。整个add仅26.7秒，原错误文案提到预算不代表实际耗尽90秒；原日志只有Error类型，无法区分网络、网关或其他提供方原因。五个失败数据库已只读取终止快照，原运行和失败保持不变。服务与评测进程已正常退出，禁止将此灌入用于正式检索配对。

[最终阶段统计](../reports/round2-quality-dev-2542f2a-r1-stages-final.json)：125次抽取、178次核验、70次修复、9次关系判定、1次原文核验尝试；所有生成尝试可关联请求。错误缺失usage不算零成本，存在并行诊断负载，不作为隔离性能对照。

最新96f40bd[真实HTTP生命周期EgdnDe](../reports/round2-source-lifecycle-round2-source-lifecycle-EgdnDe.json)：四次实际写入均200，耗时7.3/26.8/10.0/16.2秒；保留SMS偏好、删除用户预约、抑制后续回述、保留明确属于Kevin的独立预约，检查均通过。全部生成、核验、embedding及提交实际执行，但属于自编诊断，不能替代B05完整失败或0/30开发集结果。当前仍不部署新版本或启动已知不满足门槛的正式新集配对。


## 逐消息分组抽取与错误分类（9784668）

[本批复核](../reports/round2-message-groups-review.json)记录全部五次新准备、三个私有副本提交及一次失败调用重放。

新增可选`MEMORY_EXTRACTION_FORMAT=message_groups`，默认仍flat。每条参与者消息必须对应一个分组，允许空组但仍须独立核验；程序按原消息顺序展平，解析`new:<message_index>:<local_fact_index>`，防止模型计数错位。缺组、重复组、伪装成助手组、跨组来源、未知引用均明确拒绝；语义补丁沿用原扁平PATCH协议及两轮预算。格式齐全不是语义覆盖证明，不支持根据空组跳过个人信息核验。

真实mini分组F07准备65.5秒、6次生成调用（抽取＋3次核验＋2次修复），私有副本revision5→6，无旧事实变化；仍有上线前测试目标未形成事实。mini分组B05在65.2秒失败，补丁给名字承诺增加了不匹配的替换引用。两例不能证明分组格式单独改善稳定性。

相同分组格式、预算及核验/修复模型，仅配置上把抽取改为gpt-5.5：F07准备35.0秒、抽取＋核验两次调用，13条事实，私有副本5→6；通知不能扰人、按偏好搜索和上线前测试细节得到表达。但一条推广目标事实仅引用“offer them relevant promotions”，没有引用支撑“过去购买和兴趣”的前文，独立核验未拒绝，因此来源绑定仍不充分。mini与55运行源码另有错误日志/文案变化、各仅一次且有并发诊断负载，不能把耗时差严格归因为模型选择。

55分组B05抽取64.3秒、修复9.0秒，核验在90秒总期限结束，仍失败。55分组E10_remember第35段在21.7秒内准备通过，两次生成、6条事实、无降级，私有副本revision35→36且旧事实未变。上述只是在旧前缀的保存快照上重做一个chunk；没有把新结果替换进旧0/30运行，也不是从头完整灌入。

模型错误日志现在只增加闭集类别、HTTP状态、是否开始流、已知结束原因、拒绝标记、已知连接码和取消状态，不记录提供方消息/headers/request ID。区分HTTP失败、连接、中断、不完整JSON/输出、过滤、输出限制和请求到期。新B05暴露到期流被先归为不完整输出的问题，现优先检查取消信号并增加回归；旧日志不覆盖。服务不再把所有修复失败都说成预算耗尽，只有实际取消到期才这样描述。

E10原失败修复的准确system/input/model重放0xF37e在6.1秒返回；原两次均无文本，允许单独复现该调用，但新成功不证明原始错误究竟来自哪一层。新[206项服务测试](../reports/round2-message-groups-audit-service-tests.txt)包含完整分组、局部引用、空组独立核验、无意义提问、修复轮数、HTTP429/流结束分类及凭据不泄漏检查。

下一步不直接全局换大模型：优先将完整原文见证改成可定位的内部引用以减小复制成本，验证引用的限定条件；接着实现原文断言的独立操作目标，解决错误助手断言没有事实ID的情况。保持真实遗忘、主体范围、时序、同步提交、隔离和可检索性，不能用放宽校验或只统计成功chunk充当完整改进。


## 完整原文引用 b4cdd3c

`MEMORY_EXTRACTION_FORMAT=source_refs`在服务端按句建立完整、无损的原文表，模型仅返回消息/片段编号。服务端展开确切文字和UTF-16位置，引用不存在、越界、跨未来消息、借用助手身份或混用格式均拒绝；语义核验仍独立执行。同消息新增后遗忘允许受控的唯一精确子句，避免整句引用跨过授权位置。补丁保留原消息与未修改事实的偏移。普通flat与message_groups格式仍兼容，默认未切换。

新增8项回归，全部214项通过，涵盖长句/中文/emoji无损划分、完整限定条件、错误来源拒绝、重复原文精确选择、补丁、准备及事务双层先后关系，以及同句遗忘保留邻近事实。桩测试不代表任意语义删除均正确。

真实55抽取、55核验/修复和本地embedding，固定90秒模型预算：

|样例|本轮结果|边界|
|---|---|---|
|F07_update，suX4DE|26.624秒，12事实，2次逻辑调用，无降级；私有副本uqzXwm提交5→6，旧事实无变化|促销事实的来源完整包含购买/兴趣限定词；包含全量发布前测试意愿。搜索准确性诉求仍未形成单独事实，但安全原文片段保持active，核验未报告遗漏。未运行完整背景或Answer/Judge|
|B05_forget，3O2i0t|37事实；抽取53.043秒；修复一次socket错误5.532秒后成功7.376秒；核验23.304秒；遗忘绑定仅余0.615秒并到期|90.010秒VERIFICATION_UNAVAILABLE，未提交。原失败保留；引用缩短输出仍未解决链路预算|

上一轮F07为35.015秒/13事实、B05输出12042字符/36事实，本轮分别为26.624秒/12事实与9953字符/37事实。单次尝试存在输出及负载差异，不能声称可复现的速度增益。mini配置已保存但本轮未测试。

证据：[复核报告](../reports/round2-source-refs-review.json)、[214项服务测试](../reports/round2-source-refs-service-tests.txt)。服务冻结于`artifacts/round2-runtime-b4cdd3c`，实验配置`round2-quality-source-refs[-gpt55].json`。新的完整质量评测尚未完成；下一步需原文独立操作目标与更经济的事实/来源职责划分，而非继续无改动重采样B05。


## 原文独立操作 ee66153

新增可选`MEMORY_SOURCE_OPERATIONS=true`，须增强模式、语义状态核验、生命周期开启和新v6目录。独立来源核验先把“不保存这条错误助手断言”绑定到本请求的确切助手原文及后续回述；事实ID操作保持独立。准备/提交二次校验身份、先后关系和事实见证冲突，原文/尾部/片段/索引/安全事件/receipt在同一事务修改。来源计划无法判定时拒绝，普通遗忘不能因返回ordinary而变成已完成空操作。

本轮224项测试全部通过，含10项新增回归：来源身份与授权、非法目标/裁剪、回滚/重启/隔离/幂等、JSON模式调用前提、核验上下文缓存身份、实际模型输入大小，以及负面事实回流。旧“不存储”测试由未经解析的离线空操作成功改为明确拒绝并检查状态不变。

真实专项采用本地新租户：先写入合成真实安排（Harbor Pool周一游泳、单独训练、Firefox），再提交E10原失败chunk，保持真实模型与90秒预算。

|运行|结果|诊断|
|---|---|---|
|VLkr8g|初始写入200；来源操作503|网关400，同输入复核确认提示缺少JSON关键词；修复提示并补传输回归|
|5LV9qJ|两次add均200，但仅4/6内容检查通过|原文已清掉，负面事实重新存入Peak Performance Gym和错误时间，检索仍泄漏；完整失败库保留|
|iBSBPj|两次add200，9.251秒/47.783秒；6/6内容检查通过|删除错误健身房/时间及回述，真实安排、浏览器、查询和安全事件保留；没有把HTTP200单独视为成功|
|kEBaAL|另起HTTP服务，4项重启/幂等检查通过|重复add约24.8毫秒返回原receipt，数据库不变，重新检索保留真实信息且无错误细节|

固定重放5LV9qJ的两条泄漏事实，新提交前检查均拒绝；没有再调用模型或修改原失败库。词汇守卫只防止把仅在被删助手原文出现的细节重新合成事实，独立用户信息可提供保留依据；它不是完整语义删除证明，核验模型也必须拒绝负面重述被拒绝值。

证据：[复核](../reports/round2-source-operations-review.json)、[固定泄漏回归](../reports/round2-source-reintroduction-regression.json)、[224项测试](../reports/round2-source-operations-service-tests.txt)。服务已冻结`artifacts/round2-runtime-ee66153`。历史原文目标、与已存事实联合清理、后续chunk回述的持久边界、普通遗忘的路由成本仍未完成；完整E10背景需从新目录重新灌入，不能把上述合成前置安排当原前缀。


## 历史原文目标 48d256b

v7可选`MEMORY_SOURCE_OPERATION_HISTORY=true`把所有租户内已存消息纳入来源目录，包含不可检索消息及其他会话。当前消息索引不变；历史消息稳定排序、分配槽位并绑定原持久ID、正文和顺序。准备/提交重新检查身份、先后关系和逐来源事实见证，旧助手断言和跨会话回述可在一次事务内清理。已有事实相连的目标仍需普通范围核验，不能任意裁去事实见证。

227项服务测试、21项Node评测测试和9项Python测试通过。评测器`ddfab86`新增容量错误不可重试检查，保留受影响问题的完整分母。

- M1qKcf：三次HTTP add均200（9.459/6.911/38.735秒），7/7检查通过；跨请求、跨会话的错误助手断言删除，真实游泳/浏览器及独立助手邻句保留，实际重启后检索正常。该次在逐来源缺失见证守卫补强前运行。
- GEkmSo：最终源码三次add均200（11.062/4.996/43.449秒），8/8检查通过；错误“用户在Peak Performance Gym上课”被清理，同名健身房中的兄弟真实记录保留。不是统一名字黑名单。核验修复另增加两条保留偏好，尚未解决不必要事实拆分。
- 容量审计：E10第44段之前43会话446条消息的原文309661字符，加该段6792字符，仅正文就316453字符，超过180000字符单调用上限。当前v7对这种长历史会明确拒绝，不能把两个小历史专项当作完整支持。下一步需完整分批与跨批消歧，不得静默裁短目录或把局部删除决定简单求并集。

证据：[历史来源复核](../reports/round2-history-source-review.json)、[容量审计](../reports/round2-history-source-capacity.json)。服务冻结于`artifacts/round2-runtime-48d256b`。普通请求的前置核验成本、已有事实联合清理、后续回述的持久边界、完整E0–E5验收仍开放。

同时，`round2-source-ops-ee66153-E10-r1-facts-memops`从空库运行原完整E10的50会话；进程启动时版本为service ee66153/eval b6c8125，正在进行的运行不受后续源码修改影响。该运行验证v6当前chunk链路，不能证明v7历史容量已解决。见[当前进度快照](../reports/round2-source-ops-ee66153-E10-r1-progress.json)；只有终结结果才能计算完整分数。


## 检查点91782c6：长历史分批完成，完整E10结果归档

v8在实际发送的JSON大小上限制每次来源输入；去掉目标文本重复后，过长历史依次经过完整筛选、全局目标核验和全部原文的闭包检查。每批最大64单元/64000来源字符，最多16批，最多三个并发调用，共享原90秒模型预算。全局与闭包完整性、错误/重复引用、目标所见上下文、必删授权见证都在准备与提交时检查，失败取消其余调用并保留事务原状。须新v8目录，默认开关保持关闭。

[长历史专项复核](../reports/round2-history-batches-review.json)保留四次实际运行：首次连接失败；保留成功筛选响应后的传输续跑在restore提交检查失败；仅修提示的版本虽提交通过，但独立控制揭示核验上下文缺失；最终补全现有active记录后，从头真实运行zHu8RC通过6/6。最后一次处理161条当前/历史来源、314015字符、6批，准备耗时55.95秒，最大单次来源输入67285字符。闭包阶段含一次已记录的传输重试，不能写成零失败尝试。种子160消息由确定性提取和占位向量生成；只有拒绝请求的来源核验、抽取、独立事实核验及必要embedding是真实模型，因此不计作完整HTTP灌入准确率。

保留语义修复包括两部分：restore必要措辞在核验前与提交时共享检查，避免昂贵准备完成后才发现无重新授权；核验器收到现有active记录（排除本请求新来源与拟修改目标），内容进入缓存身份，状态变化不能复用旧覆盖判定。[三项真实控制](../reports/round2-retention-controls-FhqF66.json)3/3：已有active记录保持不变、新的牙医事实必须记忆、缺少兄弟记录不能当作已保留。先前2/3失败控制完整保留。

[冻结v6完整E10报告](../reports/round2-source-ops-ee66153-E10-r1-final.json)：50原始会话拆成52个/add请求，共53次尝试，52个最终成功。第45段首次90.028秒核验不可用，重试91.187秒成功。全运行21分39秒，6题全部判分、4题正确（66.7%）。两道错题为操作轨迹与状态适用范围；必要的黑咖啡、沟通、安静健身、居家周末和动感单车例外已不同程度出现在32条返回证据中，Answer仍遗漏必要细节。证据噪声/排序和判据敏感性需继续配对诊断，不能把两题全归为未召回。只有一个背景，不引用退化的单样本bootstrap区间作为可靠置信区间；不能替换331/1000历史总体结果。

[236项测试](../reports/round2-history-batches-service-tests.txt)及构建通过。包括完整覆盖、首轮遗漏回述被闭包发现、同名不同主体、漏批/错误引用、并发取消与等待、提交篡改/事务回滚、模型路由及JSON要求、restore前置拒绝、保留上下文可见性和状态变化缓存失效。

完整五背景需在冻结91782c6新目录从头执行；仅E10原v6完成不能复用于v8，也不能算完整五背景配对。仍未完成的范围：超大相关全局上下文的分层处理、已有事实与来源联合清理、未来回述持久边界、E0独立评分校准、E2–E5固定预算质量对照、新背景、三个完整配对重复、部署与独立归档。

当前已从干净检查点启动五背景新目录评测`round2-history-batches-91782c6-dev-r1-facts-memops`，30题完整分母，端口8110、并发1，服务91782c6/eval ddfab86/workspace6153217。运行中快照见[进度](../reports/round2-history-batches-91782c6-dev-r1-progress.json)；不要把启动或首段成功当成质量通过。


## 检查点0ad738e：E5实现与完整写入回归

[查询策略与协议说明](10-查询策略与重排协议.md)记录本轮实现、限制、控制结果及失败。默认生产参数未启用新策略，8088未部署更新；整数slot仅用于传输，最终仍返回原事实/原文证据。服务247项、专项11项和总控身份4项测试通过，实际HTTP对照与固定包格式对照已分别归档。

冻结91782c6完整五背景中的E10在第26段终止：首次MODEL_OUTPUT超时，随后两次历史筛选连接失败。其前25段revision25、171条事实、254条消息已通过SQLite只读backup冻结，[失败复核](../reports/round2-E10-v8-segment26-failure.json)记录请求和数据库hash及全部阶段调用。真正的遗忘指令针对用户已完成的牙医预约；先做五批完整历史筛选，最后全局只返回ordinary，已消耗约26秒，再抽取36秒、修复和核验，留给source_erasure约13.6秒。两次后续传输失败单独记录，不归因成SQLite错误。

下一项写入改动应在完整历史筛选前独立核验普通事实操作与原文断言操作的类型。ordinary分流只进入既有目标绑定/删除执行流程，不能授权no-op或跳过事实/原文删除核验；不确定或助手断言指代仍走完整来源核验。分流证据需绑定请求、旧事实及提交检查，并覆盖误分流、普通与原文操作混合等反例。不能只凭关键词缩减历史或延长请求预算。该分流尚未实施。

B05随后也在第18段终止，revision18快照见[B05失败复核](../reports/round2-B05-v8-segment18-failure.json)。其三批历史筛选与全局ordinary判定约14.7秒，抽取56秒、首次修复12.8秒，第二次修复触及90秒总预算；更细的绑定问题保留原始响应待复核。其余未终止背景继续由原runner执行；当前不是最终30题成绩，也不能复用E10成功前缀做配对质量分数。E0、完整E1验收、固定预算全背景比较、新背景和三个完整配对重复、部署与独立归档继续开放。


## 检查点6771326：普通操作分流与提交覆盖

最新服务检查点`6771326`：普通操作前置分流与v9提交覆盖检查已实现，255项服务测试通过。最终真实分流8/8、新目录HTTP7/7、31.4万字符长历史删除6/6通过；E10失败快照两次准备成功，B05仍失败，完整质量门槛未通过。见[分流设计与结果](11-普通操作分流与写入检查.md)。8088未更新。

分流采用真实独立模型与逐指令原话证明；只有全普通操作免于前置历史扫描，混合/不确定操作仍完整审查。证明绑定请求、旧事实和全部历史，普通路由不能抵消遗忘义务，提交时复核无遗漏执行。须新v9目录，原90秒共享预算不变。

[完整复核](../reports/round2-source-routing-review.json)保留首版过严引用拒绝、原文目标错误与连接失败，以及最终通过的控制。E10准备64.07/86.27秒，未降级；这是复制v8快照上的准备，不是完整v9灌入或提交。B05第二次在替换事实匹配后两次修复连接失败，77.26秒终止，不能宣称写入问题全修复。原91782c6五背景仍由原runner继续，新增分流并未注入该运行。E0、完整E1、固定预算比较、新背景、三次完整配对、部署归档均继续开放。


## 检查点f12b2b4：原文覆盖进入提交协议

最新服务检查点`f12b2b4`：v10独立原文覆盖与索引提交检查已实现，262项服务测试通过。真实覆盖/实体控制首轮7/8，补充实体核验后12/12，最终新目录HTTP9/9；B05原文优先快照仍在90秒内失败，不能宣称吞吐或质量提升。详见[原文覆盖与长请求诊断](12-原文覆盖与长请求诊断.md)。

补充语境可由独立、可索引原文承载，必要偏好/状态/时间事件/关系/指令仍须结构化，缺失检查继续执行。原文见证在准备时绑定，并在事务内删除及索引更新后复查，丢失或未索引则整体回滚。新格式必须空库。

[实验复核](../reports/round2-source-first-review.json)保留所有误判和失败。大小及输出额度对照没有支持固定请求体上限或固定输出额度为根因，因此未引入相应生产改动。原91782c6五背景新增F07轨迹第48段终止，完整分母与原runner保留。新v10需从空库跑完整B05及后续完整配对；262项测试和小控制不替代这些门槛，E0、完整E1–E5及部署归档均未完成。

冻结f12b2b4完整B05已在新目录/8112/并发1启动，保留全部会话及10题，见[运行快照](../reports/round2-source-first-f12b2b4-B05-r1-progress.json)。原91782c6五背景中的F07_update完整结束、4题判分3题正确，原E10_remember仍运行；21道先前背景写入失败题保留。均不替代历史331/1000，也不是新v10总体成绩。
