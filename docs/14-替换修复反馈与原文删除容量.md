# 替换修复反馈与原文删除容量

2026-09-06，服务代码检查点 `7281a1d`，文档同步后的HEAD `773128f`，前置反馈提交 `2bc6c70`。本轮修复了两个可复现的问题；完整B05写入仍未通过，生产8088未更新。

## 替换关系反馈

B05失败提案把“不再需要备选名”替换“备选名字”、把“孩子的名字”替换“完整姓名”等不同属性混为一体。旧反馈只列错误事实编号，修复后常有一个编号仍未处理。

`replacementBindingProblems` 为每条错误关系列出新事实、所选旧目标和 subject/property/scope 中不一致的维度。目标使用现有短编号或稳定新事实句柄。原来的范围检查保持不变，反馈不改写事实、推断目标或批准替换；修复后继续独立核验。实际调用点测试先在旧代码失败，修改后通过。

[固定提案真实对照](../reports/round2-replacement-feedback-4xacYG.json)预选两份原失败包，完整system/input和修复范围不变，只增加反馈详情；第二份交换两组执行顺序。旧反馈0/2消除全部结构错误，新增反馈2/2消除全部结构错误。[补充复核](../reports/round2-replacement-feedback-controls-review.json)确认未引入未知目标，两份提案的37条事实均保留，内容、主体、属性、范围与来源完全不变，仅去掉了错误supersedes。调用中的连接失败与重试全部保留。样本很小，结构通过不能代替删除语义核验，也没有稳定延迟结论。

带新反馈从头真实回放v10 B05失败快照，约74.420秒后不再卡在替换检查，而是在原文删除容量检查失败：[1EfFCv](../reports/round2-source-routing-replay-1EfFCv.json)。这是新模型生成的提案，不能把控制组差异直接推广为成功写入。

## 容量检查的计量错误

[原始失败诊断](../reports/round2-source-erasure-capacity-2sDz5j.json)在独立复制的运行目录中，仅加入候选捕获点，按字节一致的请求复放7个保存的成功阶段响应，未调用新模型、未提交。

74个候选没有超过256个上限。真正触发的是未压缩内部JSON的281311字符，其中重复包含来源与授权上下文。已有无损分表协议实际只需两批：55个候选/62636字符、19个候选/27096字符，总计89732字符；全部候选完整保留。不能把错误信息中的74解释为候选数量上限。

现将检查统一放入实际分批函数，保留最多256个候选、单批最多64个候选/64000字符、所有批次传输内容合计最多256000字符。重复表在不同批次再次发送时也计入总量；无法容纳的单个候选仍拒绝，不截断原文。准备与提交重建使用同一规则。

276项服务测试通过，包括重复授权导致的旧检查误拒绝、总传输量超限、单候选超限、完整索引覆盖、漏批拒绝及跨批事务回滚。[固定原失败路径复核](../reports/round2-source-erasure-capacity-fixed-replay.json)确认同样7个成功前缀响应现在能够到达原文删除模型调用，第一批输入与原始74候选的无损分批完全一致；该项仍是零新模型调用的诊断。

修复后[完整真实准备Gez27e](../reports/round2-source-routing-replay-Gez27e.json)在约90.009秒失败。抽取、修复、独立核验、删除绑定均完成；第一批source_erasure在剩余15.654秒内未完成流式响应，共享预算中止。数据库与原始v10格式保持不变。容量误拒绝已解决，端到端吞吐问题未解决。

## 完整运行与后续工作

原91782c6五背景已结束：[完整报告](../reports/round2-history-batches-91782c6-dev-r1-final.json)。30题完整分母答对6题，21题因三个背景写入失败未判分。F07_update为3/4，E10_remember为3/5。6/9已判题成绩排除了失败，不能当作总体准确率，也不是最新7281a1d的成绩。

1d52e32完整B05配对见[进度与各组报告](../reports/round2-extraction-shards-1d52e32-B05-r1-progress.json)。并发组第18段失败，10题均service_error；串行第18段实际三次90秒超时后失败，同样10题service_error。两组完整比较未显示可用性改善。两组不混入本轮修改。

下一项吞吐工作应检查已完成事实删除绑定后的原文批次能否有界并发核验：批次共享截止时间，全部完成后统一映射索引，任何错误取消并等待其余调用，唯一的引文修复预算要跨所有批次统计；最终完整分区和事务检查继续执行。需要真实长请求重新从头验证，不能把保存的成功前缀当作新的完整成功。

E0人工盲审材料已由 `scripts/export-human-review.py` 生成到 `artifacts/round2-human-review/review.md`，保留原预选199条，隐藏既有模型结论，回复文件 `reviews.jsonl` 不覆盖已有填写。已询问用户评审安排，当前独立人工结论仍为0。完整E1–E5、新背景、三次完整配对和部署归档继续开放，历史331/1000保持不变。
