# 后续写入修复与人工校准导入

2026-09-06。完整回归仍固定service `87d67b1`、eval `ddfab86`。本轮在独立worktree实施候选，未改动正在运行的service/eval源码和构建产物。候选已推送私密仓库，尚未合入当前冻结分支。完整证据见[本轮复核](../reports/round2-write-continuation-review.json)。

## 服务候选 f642616

工作区`artifacts/round2-service-shard-repair`，分支`improvement/write-continuation`，包含分片字段修复`1f57577`和历史删除范围修复。独立运行全套302项测试通过。

### E10 第19段：缺少字段提前跳过了有界修复

[原失败](../reports/round2-E10-87d67b1-shard-failure.json)的三个抽取调用传输均成功，参与者索引完整。其中分片[4,10]的第4项缺少operations字段，旧分片检查误以“遗漏或虚构参与者分组”拒绝，未进入已有全局格式修复。3个完全相同模型包可[复现原拒绝](../reports/round2-E10-87d67b1-shard-reproduction.json)。

新分片检查仍要求所有分组恰好属于指定参与者，拒绝重复/遗漏/外来分组、非数组字段和外来句柄。仅将缺失facts/operations原样传给严格分组解码器；解码器继续拒绝并调用原有次数预算内的全局修复。不会擅自补空数组，不重新抽样单个失败分片。修复后仍须完整来源、绑定、语义核验和原子提交。

13项分片测试通过，包括缺少真实forget操作字段时必须修复后才能删除、兄弟同值记录保留、修复仍缺字段时不提交。实际3个保存包[确认进入修复](../reports/round2-shard-array-repair-KY4Aom.json)，缺失字段保持缺失，所有6个人类索引均在修复输入中。此控制故意在真实修复调用前停止，不把它称为成功写入；冻结评测结束后还需用`shard-array-repair.mjs --live`验证。

### B05 第19段：历史整段范围覆盖了此前保留的邻居

本轮B05已写过先前卡住的第18段，但第19段报[保留事实所有见证被删除](../reports/round2-B05-87d67b1-retained-failure.json)。[原守卫诊断](../reports/round2-retained-witness-diagnostic-niAyBo.json)捕获了实际拒绝的Elara首选名事实：上一事务留下的句子被本次重新应用的旧source_spans覆盖。当前模型原文核验只有一条新助手消息，并没有审核或修改这一旧来源。

原因在确定性fallback：它每次遍历全部历史erased事实，遇到本次未审核的旧来源，就重新删除历史原始整段区间。新事务的reviewedSources只表示当前审核，不能据其缺席否定旧事务已经完成的精确部分删除。

修复将fallback限制为本事务新进入erased状态的事实。历史erased IDs仍用于派生事实依赖传播；新发生的非词面值删除仍有确定性来源清理。保留事实及已删除事实见证守卫都未放宽。

新增混合来源用例先在旧实现[确定失败](../reports/round2-historical-span-red-test.txt)，修复后通过普通后续写入、助手回述、Firefox来源保留及数据库重开；非词面符号的新删除继续清理。22项来源专项通过。原B05的5个模型包不变，在新实现私有副本上[提交4/4通过](../reports/round2-retained-witness-diagnostic-eVCdfN.json)：一次revision、回执、Elara活动事实及旧安全原文原样保留。模型语义决定没有重新生成，这不是完整新HTTP或基准质量验证。

## 人工校准候选 07dc65f

工作区`artifacts/round2-eval-human-calibration`，分支`improvement/human-calibration`。新增`python/report_human_calibration.py`，17项Python测试通过，其中8项针对新工具。

工具校验冻结盲审文件、原判分、选择数量与输入哈希，拒绝重复/外来题号、模型身份、非布尔标签、伪造答案引文和总体/逐项结论矛盾。待审、缺失、不确定单列；reviewer roster中没有独立评审声明的标签不进入混淆矩阵。声明只能由评审者提供，本地工具不能认证真实身份。

按六类题型及原一致/分歧分层报告两种原Judge的误判方向和一致率。分歧样本过采样，报告不冒充总体Judge准确率。全流程不调用模型，不生成标签，不改历史分数。

[真实待审表导入](../reports/round2-human-calibration-pending-report.json)得到199条pending、0个二值人工标签、0次比较，一致率为空。独立人工校准仍未完成。分支README记录人工标签及独立声明的格式；当前reviews.jsonl保持未改动。

## 新的LoCoMo失败仍开放

[conv44第10会话失败](../reports/round2-LoCoMo-87d67b1-repair-failure.json)显示核验理由讨论fact62的“烹饪逐渐成为爱好”，却把拒绝编号写成63；fact63是另一条已confirmed的“烹饪令人放松”。第一轮按授权63修改confirmed是无变化，第二轮尝试改正确的62却超出授权范围，守卫正确拒绝。

当前核验/修复输入的事实数组没有显式编号，这可能增加长数组计数错误。下一步应验证服务生成的明确事实编号及编号/理由一致性，不能猜测偏移、扩大修复权限，或把合法范围外的修改直接接受。此问题尚未修复，27题保留service_error。

当前两套冻结评测仍继续。至少E10的6题、B05的10题及LoCoMo conv44的27题已因终止写入而未判分；剩余样本继续，最终报告须保留完整60题分母。完整生命周期、质量阶梯、三次配对、新集验证与交付门槛仍未完成。
