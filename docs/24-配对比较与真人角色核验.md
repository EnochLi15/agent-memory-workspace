# 配对比较与真人角色核验

## 当前结论

完整回归 `round2-resilient-bfec674-dev-r1` 仍使用 service `bfec674`、eval `11da2a6`。本次新增代码在独立候选工作树开发、测试、推送，主服务与主评测源码/构建不变。当前运行未完成，不能报告新的总体准确率。

## 可复核的离线比较

评测候选 `073bb44` 增加 `python/quality_comparison.py` 与 `scripts/compare-quality.py`。旧比较脚本保持原样。根目录 [比较计划](../configs/round2-query-comparisons.json) 显式列出 5 种检索配置、每种 3 次查询重复、2 个数据集，共 30 次运行和 44 组配对。

- 检查完整计划题号、样本/类别/背景身份、精确数据哈希、固定 Answer/Judge/提示/推理配置，以及最终证据预算。
- 所有 service_error、pipeline_error、judge_error 留在完整题目分母；双方成功判分的交集仅作为有条件的诊断，不能替代主指标。已完成判分不能再次投票覆盖。
- 共享写入需要相同干净 service/eval 版本、写入配置及 namespace，来源运行结束，HTTP 请求审计哈希有效，且全部计划写入均有成功记录。仅“已观察到的请求全部成功”不足以通过。
- rerank、coverage、events 的单组件比较只能改变一个对应开关。events→conditional 同时改变多个策略，标为组合比较，不能归因于其中一个功能。
- 按背景整体重采样，同一背景的变体保持一起；不足两个背景不输出区间。三次查询重复不增加独立背景数。目前输出逐次差值，尚未实现跨三次均值的综合估计。
- 缺失、运行中、无效与拒绝比较均保留在矩阵；报告 `complete` 明确区分完成状态。CLI 成功退出只表示报告生成成功。

验证：24 项 Node 测试通过；最后一次 Python 全套 27 项通过，其中新增 10 项覆盖分母、标签类型、重复判分、预算/配置变化、完整写入、过期审计和缺失运行。实际计划报告 [摘要](../reports/round2-query-comparison-plan.json) 为 `complete=false`，44 组均待比较。

运行方式（从根目录；输出目录必须不存在）：

```sh
python3 artifacts/round2-eval-human-calibration/scripts/compare-quality.py \
  --campaign round2-resilient-bfec674-dev-r1 \
  --plan configs/round2-query-comparisons.json \
  --artifacts eval/artifacts \
  --data eval/.data/locomo-dev.json --data eval/.data/memops-dev.json \
  --output artifacts/NEW-COMPARISON-REPORT
```

## 本轮真实失败与候选

LoCoMo conv-44 的 `session-2:1` 发生语义核验失败，影响 27 题。第一次核验指出代词被扩展为未充分支持的犬只/归属信息；修复这些内容后，核验将具名真人 Andrew 的陈述因传输角色 `assistant` 判成无效 AI 证据。第二轮修复删除两条 Andrew 事实，最终核验仍以该角色理由拒绝另外三条。

已有抽取协议明确支持具名真人使用 assistant 角色，但核验提示未同步说明。服务候选 `0bebd9b` 补充这一解释：按真人说话者核验，角色本身不能构成拒绝理由；普通未具名 AI 建议仍是上下文，具名参与者的引用、推测和涉及他人的操作仍需通常的归属/模态/授权检查。未增加自动通过、重试次数或修复轮数。315 项服务测试通过；尚未做新提示的真实模型验证，不能称为问题已解决。详见 [故障证据](../reports/round2-LoCoMo-bfec674-role-failure.json)。

MemOps E10_reflect 的 `segment-26:0` 在模型阶段后被事务守卫拒绝：`Accepted raw coverage was erased, linked to state, or not indexed`，影响 6 题。需离线区分被删、关联状态事实、未索引或片段身份变化等原因，暂不放宽守卫。详见 [故障证据](../reports/round2-E10-bfec674-coverage-failure.json)。

两个失败租户均使用 SQLite 只读连接备份，保存精确请求、8 个模型阶段及失败前数据库；失败请求均无提交回执。当前其余租户继续运行，无并行新增真实模型诊断。实时性必须重新检查进程，静态 [进度观察](../reports/round2-resilient-bfec674-dev-r1-progress.json) 仅记录观察时刻。

## 仍待完成

先完成 E10 事务失败的离线复现与定位，保留当前完整运行结果。当前运行结束后再验证真人角色候选，必要时从空库执行新版本完整回归；失败灌入不能用于配对检索提升的归因。

100–200 题开发探索、B1/U3 对照、候选池/重排网格、完整三次重复、独立人工校准、新 LoCoMo 背景、封存确认集及最终交付仍开放。199 条人工盲审仍无独立标签，150 题封存 MemOps 未用于开发；本次工具和单元测试不构成这些验收的替代。
