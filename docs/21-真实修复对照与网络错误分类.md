# 真实修复对照与网络错误分类

2026-09-06。真实控制运行在冻结候选 `8cb099f`；新增传输错误分类候选为 `97dcd51`，都位于 `improvement/write-continuation`。主服务 `87d67b1`、主评测 `ddfab86` 仍保持原状。汇总见[本轮复核](../reports/round2-live-controls-and-transport-review.json)。

## 三个失败前缀的后续阶段重新调用模型

- [LoCoMo conv44](../reports/round2-proposal-index-control-l7HHVQ.json)：精确重放原三个抽取包，新核验接受71条事实，私有副本提交，回执、单次revision、无降级3/3通过。新核验未触发修复，因此该次通过本身不能证明编号改动修正了判定。
- [E10第19段](../reports/round2-shard-array-repair-n8Djmq.json)：精确重放三个抽取包后，真实完整格式修复补齐缺失operations字段，随后真实核验和私有提交，3/3通过。原有三条泛泛的curiosity/interest/knowledge_state事实仍被保留；这些在原抽取中已经存在，不能据此认定长期记忆质量良好。
- [B05第19段](../reports/round2-retained-witness-diagnostic-JNyF0d.json)：精确重放三个抽取包，重新调用核验和来源删除两个模型阶段，私有提交5/5通过。Elara事实活动、此前已部分删除的旧Elara原文保持不变，无降级。保留事实守卫只增加诊断捕获，拒绝逻辑未改变。

这些控制为后续阶段重新提供完整预算，既不是从空库重新灌入，也不能报告整次请求时延或总体准确率。原数据库未被控制修改。

## 固定输入的三组编号对照

[六次核验](../reports/round2-verification-index-repeat-VPgUcP.json)按预定顺序 original/labeled、labeled/original、original/labeled 执行，保留所有结果。两种输入除服务编号和对应编号说明外完全相同，不包含新的事实信息，不修复或提交。

| 输入 | 协议有效 | 无语义拒绝 | 第63条被错误地描述为tentative |
|---|---:|---:|---:|
| 原始隐含编号 | 3/3 | 1/3 | 2/3 |
| 显式编号 | 3/3 | 3/3 | 0/3 |

事实63实际是confirmed的“烹饪令人放松”；事实62才是tentative的“逐渐成为爱好”。原始输入两次重复出现编号/理由错配，新增编号在三次中未出现。这是单个已曝光提案上的初步证据，不证明总体改善，也不能认证模型接受的每项语义都正确。不得挑选通过的结果替换原失败。

## F07网络故障被错误分类

[冻结F07轨迹样本第48段](../reports/round2-F07-87d67b1-transport-failure.json)的首个抽取分片连续两次 `APIConnectionError`，底层 `UND_ERR_SOCKET`，随后其它两个分片被取消。原分片层把一切异常包装成 `EVIDENCE_VALIDATION`，评测器因此不执行已有的有界HTTP重试。

候选 `97dcd51` 在等待所有分片退出后，对第一条异常按SDK类型和HTTP状态分类。连接/连接超时/429/5xx返回503 `EXTRACTION_UNAVAILABLE`，不输出供应商异常细节。分类使用调用者信号，避免内部取消信号遮蔽原连接错误。Extractor继续直接失败，不进入离线成功，也不发布任何部分结果。客户端可执行原有完整请求重试；服务未增加模型尝试次数。

结构错误、语义拒绝、未知异常和永久4xx不会因报错文本像网络故障而变为可重试。原first-failure与取消清理语义保留。

[红测试](../reports/round2-shard-transport-red.txt)在旧实现预期失败两项。[19项专项](../reports/round2-shard-transport-targeted.txt)、[309项服务测试](../reports/round2-shard-transport-service-tests.txt)通过。HTTP控制真实断开本地模拟网关连接，验证首次503无提交/回执，随后同请求成功一次，重复请求命中回执且不再调用模型。测试仅注入网络故障，不伪称真实网关恢复验证。

## 冻结回归的新失败仍保留

LoCoMo已结束，30题均为service_error。除conv44编号失败外，[conv48会话15第二块](../reports/round2-LoCoMo48-87d67b1-repair-failure.json)先被核验拒绝三项，再在修复调用中连续两次断连。后者保留已有语义拒绝是必要行为，不能直接改为允许重新抽取的成功或宽松重试。该问题与初次抽取连接失败的分类修复不同，仍开放。

MemOps仍在运行；已出现的E10、B05、F07失败都留在完整计划分母。由于诊断运行已有终止失败，本轮真实控制与其共享网关；重叠记录在原campaign的shared-gateway-overlap.jsonl。网络故障与重叠同时发生，不能据此断言共享负载是断连根因；重叠时段的延迟不作为无竞争性能结果。

[五类真实生命周期控制](../reports/round2-lifecycle-variants-2026-09-06T12-11-53-491Z-BrMZPH.json)在独立8cb099f运行时全部通过，共10次HTTP写入：明确实体删除、否定、引用、条件和转述请求。Firefox、Kevin的Brave身份与值均保留，Chrome保留/删除及forget事件符合预设。这是五个合成控制的存储检查，不覆盖全部检索路径、缓存和并发验收。候选尚未合入主服务，后续仍须完整新灌入、固定质量对照、独立人工校准及全部交付门槛。
